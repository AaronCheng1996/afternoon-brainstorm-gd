# 09 程式碼導覽（code review 入口）

> 目的：快速掌握每個目錄、每個檔案的職責與彼此關係。
> 行為規格見 `01`/`02`，AI 規格見 `03`，架構決策見 `00`/`04`，連線規格見 `10`，
> 各檔完成時的實作備註見 `歸檔_已完成任務.md`。
> 基準：2026-07-18（Phase 12 後段客端接線進行中：P12-2…P12-14 落地），**2483 個 headless 測試全綠**。
> 行數為當時概數，只是「review 這檔要花多少力氣」的參考。

---

## 1. 一張圖看懂資料流（Sim/View 分離，決策 D1）

```
玩家輸入（滑鼠/鍵盤）─┐
CPU AI（AIController）─┤   ←「action 來源」可替換：本機玩家 / AI / 回放檔 /（Phase 12）遠端玩家
回放（ReplayLog）───────┘
   │  組出 GameAction（純資料）
   ▼
GameCore.dispatch(action)          ←── script/core：純邏輯，RefCounted，零 Node 依賴
   ├─ 改狀態：PieceState / PlayerState / BoardState / Statistics
   ├─ 跑規則：TurnEngine（回合）、Combat（傷害管線）、能力系統（ability/）
   └─ 累積 GameEvent（含動畫延遲 delay）到內部佇列
   │
   ▼
場景呼叫 core.drain_events() 取走事件
   │
   ▼
CombatScheduler 依 delay 排時間軸播動畫（PieceView / Projectile / 飄字 / 擊殺鏡頭震動）
   │  is_busy()==true 期間鎖輸入（AI 與回放也等它）
   ▼
動畫播完 → 場景以 core 最終狀態「重建棋盤」重新同步 → 解鎖輸入
```

**鐵律**：`script/core` 下任何檔案不得 `extends Node`、呼叫 `get_tree()`、`load("res://scenes...")`。
`tests/test_integration.gd` 第 4 部分用靜態掃描守住這條線。
好處：headless 可測、AI 可整局模擬、回放（P11-2 已實現）與連線（Phase 12）直接搭在事件模型上。

**表現層鐵律**：瞬時模式（動畫關）必須與逐格播放**結果完全一致**且**不生成任何特效節點**
——多個測試守著這條（`test_piece_view`/`test_combat_scheduler` 的 instant 不變性案例）。

## 2. 目錄總覽

| 目錄 | 角色 |
|---|---|
| `script/core/` | 規則核心（回合、傷害、行動、選秀 BP、回放紀錄） |
| `script/core/ability/` | 能力系統（觸發點、沉默/附魔、10 色卡牌能力組裝） |
| `script/ai/` | CPU AI 四層（幾何查詢→評分→策略→控制器），純邏輯零 Node |
| `script/data/` | 平衡資料庫（autoload `Balance`）、設定持久化 |
| `script/view/` | 表現層協定（動畫排程、動作插槽、職業演出指派、倒數計時、佔位形狀） |
| `script/net/` | 連線層（Phase 12）：傳輸工廠/訊息/端點/伺服器/房間/會話/快照/編解碼，規格見 `10`（§8 詳述） |
| `scenes/` | 場景（Node 世界）：選單/選秀/對戰/終局/百科＋展示與示範場景 |
| `scenes/ui/` | 跨場景 UI 元件（關鍵字高亮/懸停備註） |
| `theme/` | 全域 Theme（`project.godot` 掛為專案預設主題）＋色盤說明 |
| `data/` | 平衡 JSON（由 Python 同步）、顯示文字、關鍵字表 |
| `setting/` | 舊版遺留的 `description.json`（僅作 gen_card_text 來源素材） |
| `img/` | 圖片素材（buff 狀態圖示、攻擊模式圖示使用中；其餘為美術備料） |
| `shader/` | `hit_flash.tres`（舊素材，未使用） |
| `tests/` | headless 測試（自製 runner，35 個測試檔，零外部依賴） |
| `tools/` | 平衡同步腳本、card_text 產生器 |
| `docs/rebuild/` | 本文件集 |

## 3. `script/core/` — 規則核心

### 3.1 狀態與資料模型

| 檔案 | 行數 | 職責 |
|---|---|---|
| `game_config.gd` | 23 | 鏡像 Python 硬編碼常數（棋盤 4×4、勝利門檻 ±10、初始手牌…）。**改值須與 Python 端人工同步**（05 §2.3）。 |
| `board_state.gd` | 26 | 4×4 格子佔用表：occupy/release/is_occupied，座標 (0,0)–(3,3)。 |
| `piece_state.gd` | 176 | 棋子純資料：HP/ATK/armor/extra_damage、`statuses` 字典（numbness/anger/moving/selected…）、`counters` 泛用暫存、升級旗標、鏡像欄位（`shadow_attack_types`、linker WeakRef）、`make()`/`make_shadow()` 工廠。 |
| `player_state.gd` | 36 | 玩家：deck/hand/牌庫/棄牌堆/revealed_deck；抽牌含「牌庫空→洗棄牌堆重抽」。 |
| `statistics.gd` | 56 | 對局統計（StatType＋score_history＋`export_for_charts()`）。key：全域類 `player1/player2`，卡牌類 `owner_卡ID`（如 `player1_ASSW`）。終局畫面/回放驗證的資料源。 |
| `rng_service.gd` | 38 | 一局一顆 seed 的隨機服務，所有隨機必須經它（決定性＝回放與連線的基礎）。D4：不追求與 Python 同 seed 同序列。 |

### 3.2 流程引擎與回放

| 檔案 | 行數 | 職責 |
|---|---|---|
| `game_core.gd` | 436 | **核心樞紐**。持有全部狀態＋事件佇列；`dispatch(action)` 分派全部行動；`_spawn_card`（含 Cyan price_check，deploy→occupy→append 對齊 Python）；`logic_step()` 消化死亡回收與補抽；勝負判定；`drain_events()` 對外吐事件。 |
| `turn_engine.gd` | 60 | 開局（先手 +1 攻擊次數、雙方抽 3）、回合開始（refresh 鉤子）、回合結束（**只結算當前玩家棋子**、numbness 清除、換手）。 |
| `combat.gd` | 281 | 傷害管線：`detection`（nearest/farthest 平手隨機）、十步 `damage_calculate`、`launch_attack`、`pending_attacks` 佇列（追加攻擊不遞迴）、動畫延遲游標（0.32s）。翻譯自 Python `cards/base.py`。 |
| `replay_log.gd` | 148 | **P11-2 對戰紀錄**：seed＋雙方牌組＋action 流的最小重現資料；JSONL 存讀（`user://replays/`）；`simulate(log, db)` 新開 GameCore 重跑全流＝決定性重播（測試以「錄一局→重播→分數/統計全等」驗證）。連線版（Phase 12）將沿用作 server 端錄影。 |

### 3.3 行動與事件（core 的輸入/輸出協定）

| 檔案 | 行數 | 職責 |
|---|---|---|
| `game_action.gd` | 15 | 表現層 → core 的指令資料（type＋參數）。回放/AI/未來網路都組這個。 |
| `action_result.gd` | 15 | dispatch 回傳：成功與否＋訊息（UI 顯示用）。 |
| `game_event.gd` | 55 | core → 表現層唯一輸出：SPAWN/ATTACK/HURT/DEATH/MOVE/CAST/STATUS/RESOURCE/FLOAT/SCORE…每筆帶 `delay`。Phase 12 將原樣走網路廣播。 |

### 3.4 派別資源引擎（純靜態，操作傳入的 core）

| 檔案 | 行數 | 職責 |
|---|---|---|
| `token_engine.gd` | 48 | Blue：token 累積、`got_token` 每次至多換 1 抽（門檻預設 3）。 |
| `totem_engine.gd` | 27 | DarkGreen：刻印 `times × 2^(場上 SPDKG 數)`。 |
| `coin_engine.gd` | 14 | Cyan：金幣累積上限 50；升級付費檢查在 `game_core._cyan_price_check`。 |
| （luck / shadow） | — | Green 運氣共用邏輯在 `ability/cards/green.gd` 的 `lucky_effects()`；Fuchsia 鏡像機制在 `ability/cards/fuchsia.gd`。 |

### 3.5 選秀 BP 核心

| 檔案 | 行數 | 職責 |
|---|---|---|
| `draft_state.gd` | 52 | 雙方牌組、三階段（p1_first6 → p2_pick12 → p1_last6 → done）、當前選手。 |
| `draft_action.gd` / `draft_result.gd` | 16/13 | BP 指令與結果資料。 |
| `draft_dispatcher.gd` | 68+ | 規則閘：回合閘、12 上限、同名單位 ≤2、魔法 ≤3、階段前進；`auto_fill_and_advance()`（P11-1 逾時自動補牌，Phase 12 由 server 權威執行）。 |

## 4. `script/core/ability/` — 能力系統

| 檔案 | 行數 | 職責 |
|---|---|---|
| `trigger.gd` | 29 | 觸發點 enum 全表（ON_DEPLOY/REFRESH/SETTLE/…/MOD_DAMAGE_BONUS/BLOCK_DAMAGE/ATTACK_OVERRIDE…），完整覆蓋 Python hook 集。 |
| `ability.gd` | 19 | 能力定義：觸發點＋效果＋Tag（FACTION 受沉默／PASSIVE 不受）＋priority。 |
| `ability_effect.gd` | 12 | 效果基底：`execute(ctx)`，回傳語義依觸發類別。 |
| `ability_context.gd` | 19 | 執行情境（core/source/target/value…），MOD 類 value 鏈式傳遞。 |
| `ability_component.gd` | 136 | 掛在 PieceState 的能力容器：**沉默**（silence_tag/clear_silence）、**附魔**（grant）、依觸發點分派。`source` 用 WeakRef 防循環洩漏。 |
| `ability_registry.gd` | 49 | 工廠：card_id → AbilityComponent；彙整各色 `registrations()`。 |

### `ability/cards/` — 10 色卡牌能力組裝

| 檔案 | 行數 | 派別主題 |
|---|---|---|
| `white.gd` | 80 | 教學基準色：多數純數值；APW 麻痺、APTW 護盾、SPW 額外計分。特殊卡行為在 game_core。 |
| `red.gd` | 213 | 攻擊力滾雪球；所有紅色增益鏡射到我方每張 SPR；HFR 怒氣不死身。 |
| `blue.gd` | 179 | token：斬殺換抽、麻痺產球、球數轉傷、佈署爆發。 |
| `green.gd` | 273 | 運氣：`lucky_effects()` 好/壞運事件表（ap/ap_target/tank 旗標變體）；LUCKYBLOCK。 |
| `orange.gd` | 232 | 機動：攻擊後移動、移動後連鎖、MOVEO 發放、SPO 移動打擊。 |
| `dark_green.gd` | 204 | 圖騰：刻印 → `extra_damage = 圖騰 // divisor`；SPDKG 倍率。 |
| `cyan.gd` | 274 | 金幣＋升級：行動得金、升級生成付費（SPC 折扣）、TANKC 格擋、HFC 怒氣復活。 |
| `fuchsia.gd` | 365 | **最複雜**。鏡像 Shadow：對稱位生成、本體代打、移動同步、APTF 傷害攔截、ASSF 越殺越多。 |
| `brown.gd` | 265 | 高數值高代價巨人；SPBR 攻擊後沉默我方其他 Brown（內建沉默第一個使用者）。 |
| `purple.gd` | 114 | 控制，僅 4 張：驅散、反移動、加攻擊次數、擊殺爆抽。 |

## 5. `script/ai/` — CPU AI（Phase 10，翻譯自 Python `campaign/`）

四層由下而上，全部 RefCounted 純邏輯、常數走 `Balance.ai(...)`（campaign_setting.json）：

| 檔案 | 行數 | 職責 |
|---|---|---|
| `ai_query.gd` | 299 | 幾何/盤面查詢層（空格、安全度、攻擊目標/覆蓋、威脅投影…）。**零 rng**：攻擊模式查詢取「同距全部候選」不呼叫 detection，不污染對局決定性。 |
| `ai_evaluator.gd` | 362 | 評分層：佈署/移動/攻擊的分數公式（income/denial/priority/followup/defensive/threat/lethal…）。 |
| `ai_strategy.gd` | 506 | 策略層：基底 best_placement/best_attack＋White/Red/Blue/Green/Orange/Boss 六策略（inner class）＋`create(stage)` 工廠套 faction_overrides。 |
| `ai_controller.gd` | 470 | 控制器：`tick(core, now_ms, renderer_busy, paused)` 每次回 0/1 個 GameAction；嚴格決策優先序（升級二段→移動→MOVEO→斬殺→治療→佈署→攻擊(panic 門檻)→end_turn）；節奏延遲、關卡 buff（green 運氣/boss 補牌補血/orange 週期移動）、AI 黃圈 `focus_position`。 |

接線：`battle.gd` 的 `_process` 在「AI 回合＋非忙碌＋未結束」時 tick，回傳 action 走既有 `_do`。

## 6. `script/data/`

| 檔案 | 行數 | 職責 |
|---|---|---|
| `balance_db.gd` | 194 | autoload `Balance`。載入 `data/balance/*.json`＋`card_text.json`，schema 驗證；查詢 API：`stats/param/attack_types/color_rgb/ai/text/all_card_ids/job_of/color_code_of/data_version`。 |
| `settings_store.gd` | 48 | `user://settings.json`：提示/動畫開關＋回合/選秀計時器 4 鍵（P11-1）。`save_settings` 收 Dictionary 全量寫回、缺鍵回預設。 |

## 7. `script/view/`（表現層協定；不含場景）

| 檔案 | 行數 | 職責 |
|---|---|---|
| `combat_scheduler.gd` | 182 | 吃事件依 `delay` 排時間軸播動畫；`is_busy()` 鎖輸入（人類/AI/回放共用閘）；瞬時模式直接套最終狀態；`on_kill` 回呼（DEATH 起始觸發鏡頭震動，瞬時不觸發）；`_advance(dt)` 供 headless 精確測時間軸。 |
| `piece_animation_set.gd` | 44 | 每卡可覆寫的動作插槽 Resource（idle/attack/hurt/death/cast＋投射物/命中特效場景＋`fx_color` 派別色）。 |
| `piece_animation_library.gd` | 51 | **P9-3 職業演出指派**：依 `Balance.attack_types` 判遠程（含 large_cross/large_x/farthest → 投射物）或近戰（撲擊），並染派別色。battle/anim_demo 建視圖時呼叫。 |
| `countdown_timer.gd` | 54 | P11-1 純邏輯倒數器：`advance(delta)` 回傳是否逾時（恰一次）；關閉或秒數≤0 時不啟動。BP 與回合計時共用。 |
| `piece_shapes.gd` | 73 | 職業/特殊卡的正規化頂點表（佔位幾何），純資料。 |

## 8. `script/net/` — 連線層（Phase 12，規格 `10`）

> 決策 D17/D18/D19：**專用伺服器＋房間制、伺服器權威、手牌公開／seed 不下發**。
> 鐵則：`script/core` 零改動（core 只被 dispatch/drain_events/快照讀取）；**傳輸集中單一工廠**
> （`NetTransport`，Steam 之路，`10` §9.5）；**任何訊息/快照不得含 seed 或牌庫序**（D19）。
> 分三層——傳輸/協定（peer_base/transport/message）、伺服器邏輯（server/room/session/snapshot/codec）、
> 用戶端（client）＋入口（server_main）。測試以「同程序匯流排」loopback（覆寫 `_transmit` 直投遞），
> `@rpc`-over-ENet 的運行樹整合與跨機驗證屬【人工】（見各 `test_net_*` 檔頭註）。

### 8.1 傳輸與協定（底層，零遊戲邏輯）

| 檔案 | 行數 | 職責 |
|---|---|---|
| `net_transport.gd` | 37 | **唯一 ENet 工廠**（`class_name NetTransport`）：`create_server/create_client`＋`endpoint()` 位址抽象；`DEFAULT_PORT 24242`。全案僅此處知道用 `ENetMultiplayerPeer`——換 Steam 只改此檔（D18/§9.5）。 |
| `net_message.gd` | 127 | 訊息信封（`{v,t,p}` JSON）＋全型別/意圖/拒絕原因常數（握手/大廳/選秀/對戰/**重連 T_SEAT_TOKEN**）；`GAME_VERSION`/`PROTOCOL_VERSION`；`decode()` 對不可信輸入四道驗證（用 JSON 實例 parse 不噴 log）。純資料。 |
| `net_peer_base.gd` | 123 | server/client 共同基底（`extends Node`）：單一 `@rpc` 通道 `send_to/_transmit/_rpc_recv/_ingest` 分層、連線/斷線信號轉發、ping-pong RTT。**RPC 路徑一致**＝兩端節點皆名 `NetPeer`（`PEER_NODE_NAME`）。 |

### 8.2 伺服器邏輯（權威，多為純 RefCounted）

| 檔案 | 行數 | 職責 |
|---|---|---|
| `net_server.gd` | 95 | 握手層（`extends NetPeerBase`）：開埠、版本閘（遊戲版本＋`Balance.data_version()`）＋意圖檢查→welcome/rejected（拒＝flush 後斷線）、追蹤已認證客端。 |
| `net_game_server.gd` | 467 | **大廳/對戰樞紐**（`extends NetServer`）：把已認證客端訊息翻成 `RoomManager` 呼叫並廣播；建/管每房 `NetGameSession`（對戰）與 `NetDraftSession`（BP）；`_send_catchup`（旁觀/重連補送，P12-9）；**`_handle_hello` override＝帶 token 走重連**（逐出舊連線→補送續播，P12-10）；`tick_sessions`＝回合/選秀計時＋`tick_holds` 逾時判定（判勝/解散）；終局存 ReplayLog（P12-11）。輸入全當不可信（席位由 server 認定、旁觀者拒、回合閘）。 |
| `room_manager.gd` | 474 | 純邏輯房間狀態機（零 Node）：建房/加入/離開/座位就緒/大廳列表/生命週期（waiting→drafting→battling→ended→重開/解散）；房碼 4 碼去混淆；上鎖＋密碼（server-only）；觀戰開關/上限；**P12-10 席位 token／held／hold_remaining＋`handle_disconnect`/`reconnect`/`tick_holds`/`vacate_held_seat`**。密碼/token 不進任何對外視圖。 |
| `net_game_session.gd` | 113 | 每房權威 `GameCore` 薄包裝（零 Node）：`start`/`apply_action`（server 權威回合計時逾時 end_turn）；迴圈嚴格對齊本機 `battle.gd`（drain_events＋logic_step）；seed 只存此；**P12-11 起錄 `ReplayLog`**（成功 action 流）。 |
| `net_draft_session.gd` | 113 | 每房權威 `DraftState`＋`DraftDispatcher` 薄包裝（零 Node，P12-8）：一位席位玩家的選秀行動、server 權威選秀計時逾時 `auto_fill_and_advance`；公開 `view()`（不含 seed；**P12-13 追加 `remaining` 剩餘秒**供客端倒數顯示）。 |
| `game_snapshot.gd` | 118 | **公開快照序列化**（`class_name GameSnapshot`，零 Node 純靜態，P12-4）：`GameCore→Dictionary`（盤面全公開欄位＋**雙方手牌/棄牌**＋資源/分數/回合/統計；牌庫**只給張數**）。**鐵則：永不含 seed/draw_pile/revealed_deck**（測試斷言）。battle 端可從快照重建盤面。 |
| `net_codec.gd` | 115 | `GameAction`（client→server）與 `GameEvent`（server→client）的 JSON ↔ 安全字典（P12-6）；**白名單＋不可信拒收**（`ALLOWED_ACTIONS`/`ALLOWED_DRAFT_ACTIONS`，player 由 server 指派）；`Vector2i↔[x,y]`。 |

### 8.3 用戶端與入口

| 檔案 | 行數 | 職責 |
|---|---|---|
| `net_client.gd` | 195 | 用戶端端點（`extends NetPeerBase`）：連上→送握手（版本＋意圖／暱稱／**可選重連 token**）→收 welcome/rejected；大廳/選秀/對戰/**T_SEAT_TOKEN（存 `_token`＋`seat_token()`）** 全套信號；RTT 心跳。表現層（`online_lobby`、battle 客端）訂閱其信號。 |
| `net_mirror.gd` | 130 | **顯示鏡像**（`class_name NetMirror`，零 Node 純靜態，P12-12）：公開快照 → 唯讀 `GameCore`，供 battle net 模式的 HUD／記分板／攻擊範圍預覽／手牌列**原樣重用既有讀取碼**（§11.2-4）。鐵則：鏡像**永不 dispatch**（唯一寫入＝快照重建）、`rng=null`、牌庫只還原張數。`GameSnapshot.encode` 的逆運算（僅還原公開欄位）。 |
| `server_main.gd` | 114 | **headless 專用伺服器入口**（`extends SceneTree`）：`-s script/net/server_main.gd -- key=value`；載 `server_config.json`＋命令列覆蓋（`parse_config` 純函式可測）；動態 `load().new()` NetGameServer（避開 `-s` 下 autoload 編譯期問題）、`_boot.call_deferred()` 入樹後掛 `SceneMultiplayer` 開埠、常駐。部署見 `deploy/`（P12-11）。 |

> 對應場景：`scenes/online/online_lobby.tscn/.gd`（P12-7 大廳/建房/房內四面板，持一顆 `NetClient`）——見 §9。
> 測試：`test_net_transport`（傳輸/協定）、`test_net_battle`（對戰核心）、`test_net_draft`（連線 BP）、
> `test_net_spectator`（旁觀補送）、`test_net_reconnect`（斷線重連）、`test_net_soak`（連續多局＋churn＋
> server ReplayLog 決定性）、`test_room_manager`（房間狀態機＋大廳訊息＋`server_main.parse_config`）。

## 9. `scenes/` — 場景（Node 世界）

> 準則（08 §2）：靜態 UI 骨架宣告在 `.tscn`（編輯器可視可編輯），腳本 `%唯一名稱` 綁定；
> 動態集合生成到宣告好的容器。新場景（百科、Phase 12 大廳）照此模式。

| 檔案 | 行數 | 職責 |
|---|---|---|
| `menu/main_menu.tscn/.gd` | 281 | 入口。六鈕：本機對戰→draft、**單人對戰**（AIPanel 選 白/紅/藍/綠/橙/Boss）、**百科圖鑑**、**回放紀錄**（ReplayPanel 列 `user://replays/`）、設定（提示/動畫＋回合/選秀計時循環鈕）、離開；爬塔灰鈕佔位。 |
| `draft/draft.tscn/.gd` | 380 | 選秀 BP。色頁展示館＋魔法列＋雙方牌組面板＋**卡片說明區**（懸停顯名稱與完整描述，關鍵字高亮）；選秀倒數（逾時 `auto_fill_and_advance`）；完成帶牌組進 battle。**P12-13 連線模式 `boot_net`**：`_state` 為公開 view 重建的唯讀鏡像、輸入只 encode 送 server（gating 單點化、非我回合零送信），draft→battle 轉場由 lobby 主導。 |
| `battle/battle.tscn/.gd` | 976 | **最大場景**。輸入流全組 GameAction 進 `dispatch`；HUD（記分板/資源列/提示浮窗/勝負面板）；攻擊範圍預覽（含鏡像、不耗 rng）；**45°/俯視雙視角**（V 鍵，BoardView 換算）；**AI 接線**（`boot(ai_stage)`）；**回放模式**（`boot_replay`：鎖輸入、空白/→/S 控制）；回合倒數；擊殺鏡頭震動；終局轉場與錄影存檔。 |
| `battle/board_view.gd` | 79 | **P9-1 座標系**：cell↔pixel 雙模式（ORTHO 方格/ISO 菱形，預設 ISO）；`cell_polygon/pixel_to_grid/depth`。格線/高亮/預覽/投射物/飄字/點擊反算全走它。RefCounted 純數學、headless 可測。 |
| `battle/scoreboard.tscn/.gd` | 194 | P8-5 記分板元件：回合列、分差讀數、diverging 記分條（中央 0、±門檻）、近 8 回合趨勢。`update_board(...)` 收基本型別，零 core 依賴。 |
| `battle/piece_view.tscn/.gd` | 437 | 棋子視圖：佔位多邊形＋外框＋數值標籤＋狀態圖示＋SpriteSlot 美術插槽；動畫（撲擊/拉弓投射物/受擊頓幀＋火花粒子/死亡碎裂＋殘影＋碎片/施法能量環）；特效掛 `fx_layer`（視圖釋放後特效仍播完）；`instant` 模式零特效。 |
| `battle/projectile.tscn/.gd`、`impact_flash.tscn/.gd` | 45/35 | 佔位投射物與命中閃光，支援派別色 `set_color()`。 |
| `battle/draw_layer.gd` | 11 | `_draw` 回呼層（高亮/預覽），battle 綁定時設 `cb`。 |
| `battle/piece_gallery.tscn/.gd` | 109 | 展示館（10 色×8 職業＋特殊卡）。百科的前身，保留作快速視覺驗收。 |
| `battle/anim_demo.tscn/.gd` | 152+ | 演出示範：真實 GameCore 讓 ADC 打三目標（空白重播、I 瞬時）。P9-2/9-3 的人工對照場景。 |
| `encyclopedia/encyclopedia.tscn/.gd` | 322 | **P8-4 百科圖鑑**：色系分頁＋職業篩選→卡片清單；詳情頁（數值、攻擊模式圖示 `img/UI/attack_pattern/`、能力描述關鍵字高亮、PieceView 預覽、衍生物連結 SHADOW/LUCKYBLOCK）。資料全走 Balance，零硬編碼。 |
| `end_game/end_game.tscn/.gd` | 319 | 終局統計：折線（z-order 已修，P8-6）＋摘要長條＋**per 玩家 per 卡統計表格**＋圖/表切換＋「回放本局」。資料源 `Statistics.export_for_charts()`。 |
| `online/online_lobby.tscn/.gd` | 580+ | **P12-7 線上大廳**（表現層薄殼，持一顆 `NetClient`，ENet 仍集中 `NetTransport`）：連線設定/大廳列表/建房/房內四面板單場景狀態機；接 `NetClient` 全套信號（房態/延遲/拒絕原因/選秀/快照）。**P12-12/13：draft/battle 客端場景以子場景嵌入**（首份選秀 view→draft 子場景；開局快照→battle 子場景；BP→對戰轉場釋放 draft 嵌 battle）；「開始」＝`start_draft`（正式流程）。**P12-14：旁觀者依 catchup 進行中畫面**（`_my_seat()==""`→spectator 唯讀）＋觀戰人數轉入子場景（`set_spectator_count`）。 |
| `ui/keyword_db.gd` | 131 | P8-3 關鍵字引擎（純靜態）：讀 `data/keywords.json`，`markup()` 把描述中的機制詞包 BBCode 高亮＋`[url]` meta（保留既有 BBCode/圖片不動）；`explain()` 供浮窗。 |
| `ui/keyword_label.gd` | 82 | `KeywordLabel`（RichTextLabel）：`set_source()` 灌文字、懸停機制詞彈出解釋浮窗（隨滑鼠定位、翻邊防出界）。battle 提示/draft 說明區/百科共用。 |

## 10. `data/`、`theme/` 與其他資源

| 路徑 | 內容 |
|---|---|
| `data/balance/*.json` | 平衡數值，**由 Python repo 單向同步**（`tools/sync_balance.ps1`），`_meta.json` 記版本＋hash。不要手改。`campaign_setting.json` 的 `ai/*` 常數由 script/ai 使用。 |
| `data/card_text.json` | 全卡顯示名/描述/提示（82 卡），`tools/gen_card_text.gd` 產生，可手動微調。 |
| `data/keywords.json` | P8-3 關鍵字表：機制詞 →（顯示色、繁中解釋、別名）。新機制詞加這裡即全 UI 生效。 |
| `theme/main_theme.tres`＋`色盤說明.md` | 全域 Theme（Button 五態/Panel/Label 樣式與字級），`project.godot` `[gui] theme/custom` 掛為專案預設；換字型只改 theme。派別色仍走 `Balance.color_rgb`。 |
| `setting/description.json` | 舊版遺留，僅 gen_card_text 來源素材。 |
| `img/UI/buff/` | 狀態圖示（stun/rage/move 使用中，其餘備料）。 |
| `img/UI/attack_pattern/` | 攻擊模式圖示——**百科詳情頁使用中**（P8-4）。 |
| `img/board/`、`img/piece/`、`shader/` | 舊版美術/素材備料，未使用。 |
| `user://`（執行期） | `settings.json`（設定，含 P12-7 `net_host`/`net_port`/`net_nickname`）、`replays/*.jsonl`（對戰紀錄；連線版由 **server 端**存，P12-11）、`server_config.json`（專用伺服器設定，見 `deploy/`）。 |
| `deploy/`（部署包，P12-11） | `run_server.sh`（bash 啟動）＋`afternoon-brainstorm-server.service`（systemd 範本）＋`server_config.json`＋`README.md`（Ubuntu 架設步驟：ufw/systemd/用戶端位址）。 |

## 11. `tests/` — headless 測試（自製 runner）

`run_tests.gd`（81 行）：自製 runner——掃 `tests/test_*.gd` 逐檔實例化呼叫 `run(t)`，
`t` 為斷言工具（eq/ok/fail）；任一失敗 exit 1。零外部依賴。

| 測試檔 | 守什麼 |
|---|---|
| `test_smoke` | runner 自證。 |
| `test_balance_db` / `test_card_text` / `test_keywords` | 平衡查詢、顯示文字、關鍵字表完整性與 BBCode 轉換。 |
| `test_core_turns` / `test_core_actions` | 回合引擎、行動分派合法性。 |
| `test_combat_pipeline` / `test_ability_system` | 傷害十步管線、沉默/附魔骨架。 |
| `test_card_<color>` ×10 | 各色全卡行為（多翻譯自 Python 同名測試）。 |
| `test_integration` | 整局腳本回歸＋固定 seed 煙霧＋**core 零 Node 依賴靜態檢查**。 |
| `test_board_view` | 兩座標模式恆等/菱形/深度（P9-1）。 |
| `test_piece_view` / `test_combat_scheduler` / `test_piece_animation_library` | 視圖組裝、時間軸/on_kill、**瞬時模式零特效不變性**、職業演出指派。 |
| `test_scoreboard` / `test_encyclopedia` | 記分板數值同步、百科資料完整性（76 實卡）。 |
| `test_ai_query` / `test_ai_evaluator` / `test_ai_strategies` / `test_ai_controller` | AI 四層（翻譯自 Python test_ai_*.py＋test_campaign_strategies.py）。 |
| `test_ai_battle_integration` | **White AI 打完整局零非法操作**（整合驗收）。 |
| `test_countdown_timer` / `test_draft` / `test_battle_scene` / `test_menu_flow` | 倒數/逾時補牌、BP 三階段、對戰場景全行動＋視角切換、選單/設定/面板/轉場。 |
| `test_replay` | **錄一局→simulate 重播→分數/統計全等**（決定性）；JSONL round-trip。 |
| `test_net_transport` | 連線傳輸/協定：真 ENet loopback（連上/RTT/斷線）＋協定邏輯（握手/版本閘/意圖閘）。 |
| `test_net_battle` / `test_net_draft` | 連線對戰核心（codec/session/兩 client 走完整局逐位一致）／連線 BP（三階段/計時逾時 auto_fill）。 |
| `test_net_spectator` / `test_net_reconnect` | 旁觀中途加入補送＋唯讀保證／斷線 held＋帶 token 重連收復席位＋逾時判勝。 |
| `test_net_soak` | **連續多局＋旁觀 churn＋重連 churn**（穩定性）＋**server ReplayLog 用 simulate 重播＝權威核心**。 |
| `test_net_battle_scene` | **連線對戰畫面接線（P12-12）**：兩個 battle 場景以 `boot_net` 接兩 client、經 server 打完整局＝兩端最終快照＝server 且彼此一致；非我回合輸入零送信；被拒 action 顯示訊息。 |
| `test_net_draft_scene` | **連線選秀畫面接線（P12-13）**：兩個 draft 場景以 `boot_net` 接兩 client、經 server 走完整三階段 BP＝兩端 view 一致＝server 牌組；非編輯方輸入零送信；同名超上限被拒顯示訊息；完成後建對戰 session／兩端收開局快照（battle 轉場）。 |
| `test_net_spectator_scene` | **旁觀者唯讀畫面（P12-14）**：第三 client 中途旁觀加入→依 catchup 建旁觀 battle/draft 場景＝server；行動控制隱藏、雙方手牌/展示卡唯讀、任何點擊零送信、觀戰人數＋徽章顯示、後續同步。 |
| `test_room_manager` | 房間狀態機（建/加/離/就緒/生命週期/房碼）＋大廳訊息匯流排＋`server_main.parse_config`。 |

**跑法注意**：新增/改名 `class_name` 後先 `--headless --import` 重建 class cache；
`-s` SceneTree harness 內 `add_child` 不觸發 `_ready`（場景測試顯式 `boot()`/`_bind_nodes()`）、
in-tree 煙霧需 `await process_frame`——harness 限制，非產品行為。
**洩漏基準**：**0／0**（P12-12 修正 P8-5 scoreboard `_rebuild_meter` 於 score==0 建 `ColorRect.new()` 卻不掛樹的孤兒洩漏——正是歷史「39 CanvasItem／78 ObjectDB」基準的唯一來源；修復後全套件零洩漏）。新任務要求「零新洩漏」＝維持 0／0。

## 12. 建議讀碼順序（review 路線）

1. **資料模型**：`game_config` → `piece_state` → `board_state`/`player_state` →（`statistics`、`rng_service`）
2. **核心流程**：`game_core`（`dispatch` 分派表）→ `turn_engine` → `combat`（十步管線，對照 01 §6）
3. **能力系統**：`trigger` → `ability_component`（沉默/附魔）→ `cards/white.gd`（最簡）→ 挑一色深讀（推薦 `fuchsia.gd`）
4. **事件→動畫**：`game_event` → `combat_scheduler` → `piece_view` → `board_view`（座標系）
5. **AI**：`ai_query` → `ai_evaluator` → `ai_strategy` → `ai_controller`（對照 03）
6. **場景薄殼**：`battle.gd`（輸入→dispatch→重建；AI/回放/計時接線都在這）→ `draft.gd` → `main_menu`/`end_game` → `encyclopedia`
7. **回放**：`replay_log`（`simulate()` 是理解決定性的最佳入口）
8. **連線層**（規格 `10`）：`net_transport`（唯一 ENet 工廠）→ `net_message`/`net_peer_base`（協定/RPC 通道）
   → `game_snapshot`（D19 公開快照）/`net_codec` → `room_manager`（房間狀態機）→ `net_game_server`
   （握手/大廳/重連/計時樞紐）→ `net_game_session`/`net_draft_session` → `net_client` → `server_main`（入口）
9. **測試**：`run_tests.gd` → `test_integration.gd` → `test_replay.gd` → `test_net_soak.gd`（連線全貌）

## 13. Review 時可留意的慣例與陷阱

- **記憶體**：core 全 RefCounted，Godot 無循環 GC——自我/循環參考處用 WeakRef
  （`ability_component.source`、shadow `linker`）或事後 `hit_cards.clear()`（自傷情境）。
  測試 runner 報 orphan；洩漏基準 39/78，新增結構要比照辦理。
- **決定性**：所有規則隨機走 `RngService`；UI 範圍預覽與 AI 查詢**刻意不呼叫 detection**
  （取同距候選集合）以免消耗 rng；鏡頭震動等純演出用全域 randf（不進 RngService）。
- **瞬時模式等價**：任何新演出必須在 instant 下「零特效節點、同最終狀態」，並補不變性測試。
- **statuses/counters**：棋子布林狀態統一走 `statuses` 字典＋API，不加散裝 bool。
- **常數雙源**：`game_config.gd` 鏡像 Python 硬編碼值（改值兩邊同步）；其餘讀 `Balance`。
  AI 常數在 `campaign_setting.json`（`Balance.ai()`）。
- **`%` 綁定模式**：場景腳本用 `_bind_nodes()`＋`%Name`（headless 下 `_ready` 不觸發）；新場景照此。
- **fx_layer 生命週期**：粒子/殘影掛外部 fx 層，發射者 `queue_free` 後特效播完自清；
  fx_layer 為 null 時退回自身（headless/獨立測試安全）。
- **action 來源可插拔**：人類輸入、AIController、ReplayLog、（Phase 12）遠端玩家都只是
  「組 GameAction 餵 `_do`」——新增來源時不要繞過 dispatch。
- **註解慣例**：每檔頂部說明翻譯來源與規格出處章節，繁體中文。
