# 06 任務清單（主執行文件）

> 執行規則：**由上往下、一次一個任務**。每個任務格式：目標 → 步驟 → 驗收。
> 完成後把 `[ ]` 改 `[x]` 並在行尾加一句備註（日期＋特殊處理）。
> 標【人工】的項目由使用者執行，自動代理**跳過它、不得代勾**。
> 規格出處縮寫：R=01規則、C=02卡牌、A=03AI、D=04架構、J=05JSON、T=07爬塔、**N=10連線**。
> 驗收指令統一為（本機 Godot 路徑已確認）：
> `& "d:\Code\Games\Godot_v4.7-stable_win64.exe" --headless --path "D:\Code\Games\AfternoonBrainStorm\AfternoonBrainstorming_godot" -s tests/run_tests.gd`
> （Godot 4.7 stable；PowerShell 用 `&` 呼叫。新增/改名 `class_name` 後先跑一次 `--headless --import` 重建 class cache。換機器時更新此路徑。）

---

## 方向宣告（使用者決策）

- **D16（2026-07-11）美術與 UI 不再對齊 Python（pygame）版**：畫面、佈局、操作、演出以 Godot 版
  自身的易用性與觀感優先，自由設計。
- **平衡數值與規則行為仍以 Python 版為唯一基準**：`data/balance/` JSON 單向同步機制不變（J），
  規則核心（`script/core`）的行為測試仍以 Python 為準；**任何表現層改動不得改變對局結果**（全套件必須綠）。
- **D18（2026-07-16）連線版＝專用伺服器＋房間制**：使用者提供一台主機跑 dedicated server；
  玩家開房間邀請人加入，房間可公開或上鎖；支援旁觀。規格見 `10_連線版本.md`（N）。
- **D19（2026-07-16）手牌為公開資訊**：雙方手牌對所有參與者（雙方玩家＋旁觀者）可見，
  **本機版也一樣**；唯一隱藏資訊＝牌庫抽序與未來隨機（seed 只存在權威端）。

## 已完成階段摘要（完整執行備註見 `歸檔_已完成任務.md`；逐次紀錄見 `進度日誌.md`）

| Phase | 內容 | 完成日 | 驗收 |
|---|---|---|---|
| 0 地基 | 測試 runner、sync_balance、BalanceDB、card_text | 2026-07-10 | 169 passed |
| 1 規則核心 | GameCore／傷害管線／能力系統／10 色全卡／各派別引擎 | 2026-07-10 | 529 passed |
| 2 表現層 | PieceView／動畫排程／對戰／BP／選單＋終局，本機雙人閉環 | 2026-07-11 | 763 passed＋人工 |
| 6 收尾 | 刪除舊碼、入口收編、README 重寫 | 2026-07-11 | 763 passed |
| 7 編輯器化＋去 v2 | UI 骨架進 .tscn（`%` 綁定）、v2 字眼全移除 | 2026-07-11 | 846 passed |
| 8 UI/UX 基座 | 多分辨率、全域 Theme、關鍵字高亮＋懸停備註、百科圖鑑、記分板、終局統計強化 | 2026-07-13 | 1275 passed |
| 9 視角與演出 | 斜 45°/俯視雙視角棋盤、命中頓幀/粒子/死亡碎裂/鏡頭震動、職業攻擊差異化＋派別色特效＋資源飄字（P9-4 持續型未收） | 2026-07-13 | 1512 passed |
| 10 CPU AI | ai_query/evaluator/strategy/controller 全移植＋主選單「單人對戰」六關卡 | 2026-07-14 | 1749 passed |
| 11 對局功能 | 計時器（BP＋回合，設定可調）、JSONL 對戰紀錄＋決定性回放（單步/倍速） | 2026-07-15 | 1817 passed |
| 12 連線版本 | 專用伺服器＋房間制＋公開快照＋連線對戰/BP＋旁觀＋斷線重連＋Ubuntu 部署包（P12-2…11 headless 全綠；**客端場景接線＋實機跨機驗證屬【人工】**） | 2026-07-18 | 2426 passed |

對照舊編號：Phase 3 →10（已完成）；Phase 4 移除（D10）；Phase 5 →13（擱置）；原 P6-3 →P11-2（已完成）。

## 人工協作待辦（不擋自動任務；使用者有空時做）

- [ ] 【人工】**視覺/體感總驗收**（Phase 8–11 累積的「待人工過目」一次列清）：
  - 四場景通感：多分辨率縮放（拉視窗/全螢幕）、Theme 一致性、主選單新六鈕佈局。
  - battle：記分板可讀性、關鍵字懸停浮窗、45°/俯視切換（V 鍵）、攻擊/受擊/死亡演出手感
    （或 F6 跑 `anim_demo.tscn`，I 鍵切瞬時應完全無特效）、回合計時（設定開啟後）。
  - draft：卡片說明區、選秀倒數逾時自動補牌。
  - 百科圖鑑：瀏覽動線、詳情頁（數值/攻擊圖示/關鍵字/衍生物連結）。
  - 終局統計：折線已可見（z-order 已修）、圖/表切換。
  - 單人對戰：打 White AI 一局（會出牌/攻擊/移動/治療、回合開始有停頓、黃圈目標提示）。
  - 回放：終局「回放本局」與主選單「回放紀錄」，空白鍵播放/暫停、→ 單步、S 倍速。
  - 有要調整的直接回報，開成新任務；不用回頭改各任務勾選。
- [ ] 【人工】**程式碼 review**：配合 `09_程式碼導覽.md`（已更新至 1817 版）走讀。
- [ ] **P9-4 整體 UI 美術優化（持續型，【使用者帶方向】）**：每輪由使用者點名一個場景與想要的方向
  （例：「主選單改成 X 風格」「BP 卡片化」），代理執行一輪並回到此格。點陣素材由使用者提供、
  經 SpriteSlot/Theme 換入。**自動代理不得自行挑選本任務**。

## 優先序說明

**當前主線＝Phase 12 後段：連線對戰畫面接線**（伺服器端 P12-2～P12-11 已完成並實機部署），
由上往下依 **P12-12 → P12-17** 執行（P12-18 選做）。
Phase 13（爬塔）維持擱置（2026-07-18 使用者再確認）。P9-4 為使用者帶方向的持續型任務，不進自動佇列。

---

## Phase 12 連線版本（重點項目；規格＝`10_連線版本.md` v2 定稿）

> **已拍板的核心決策**（2026-07-16，D18/D19，細節見 N）：
> 專用伺服器（使用者主機、headless 常駐、server 權威跑 GameCore）＋大廳房間制
> （公開/上鎖＋密碼、邀請碼加入、觀戰開關）；旁觀進首版（看雙方手牌、看不到牌庫序）；
> **手牌公開（D19）→ 全房間單一公開快照，無需逐客端遮蔽**；傳輸＝Godot High-Level
> Multiplayer（ENet＋自訂 RPC、RELIABLE），版本/資料版本閘；BP 連線化排在對戰骨幹之後；
> 斷線重連（席位 token）納入首版；**不與 Python 版互通**（D17）。
> **部署已定**（2026-07-16，N §9）：Ubuntu 常駐、固定 IP 直連公網、預設埠 **24242/UDP**
> （config 可改）、用戶端內建預設位址＋可手動改；systemd 常駐。
> **Steam 展望**（N §9.5）：未來換 GodotSteam `SteamMultiplayerPeer`，RPC 不動——
> **peer 建立集中單一工廠**為 P12-3 的設計紀律。

- [x] **P12-1 連線規格文件** — 2026-07-15 草案（模型比較/傳輸選型/待決 7 項）；
  **2026-07-16 使用者拍板並追加需求**（專用伺服器＋房間、旁觀首版、手牌公開）→ 規格定稿
  `10_連線版本.md` v2（D18/D19），實作拆解 P12-2…P12-11 如下。草案原文見歸檔。
- [x] **P12-2 手牌公開化（本機＋單人先行，D19）** — 2026-07-17：battle.tscn 加 `OpponentHandTitle`＋
  `OpponentHandBox`（HFlowContainer，右上）；`_rebuild_hand` 一般化為 `_rebuild_hand_into(容器, 玩家, interactive)`，
  己方＝可點列、對手＝唯讀公開列（disabled 鈕＝點擊無作用、派別色 font、名稱＋提示 tooltip）；
  hot-seat/單人/回放皆由每次 `_refresh_hud` 以 cur/對手重建兩列自然互換。驗收：test_battle_scene 複用既有
  battle 斷言兩列與 core 雙方 hand 同步（開局＋換手抽牌後）、對手列 disabled；1826 passed／洩漏 39/78 零新增。
  **版面（右上唯讀列位置/寬度、換手互換觀感）待【人工】過目。**
  - 目標：battle 場景常駐顯示**雙方**手牌：己方（當前操作方）為既有可點手牌列，
    對方為唯讀展示列（名稱＋派別色，懸停關鍵字提示照舊）；hot-seat 換手時兩列互換；
    單人對戰顯示 AI 手牌；回放模式同時顯示雙方。
  - 步驟：battle.tscn 加 `OpponentHandBox`（唯讀，樣式區別）；`_rebuild_hand` 一般化為
    「渲染指定玩家手牌到指定容器（interactive: bool）」；佈局配合多分辨率錨點。
  - 驗收：headless 斷言兩列內容與 core 雙方 hand 同步（出牌/抽牌/換手後）；唯讀列點擊無作用；
    【人工】版面過目。
- [x] **P12-3 net 轉接層骨架（`script/net/`）** — 2026-07-17：新增 `script/net/`＝
  `net_transport.gd`（唯一 ENet 工廠 create_server/create_client＋endpoint 抽象，DEFAULT_PORT 24242，
  守 §9.5 Steam 之路）、`net_message.gd`（GAME_VERSION/PROTOCOL_VERSION＋型別/意圖/拒絕原因常數，
  encode/decode 用 JSON 實例 parse 不噴 log、不可信輸入四道驗證）、`net_peer_base.gd`（單一 @rpc 通道
  send_to/_transmit/_rpc_recv/_ingest 分層、連線/斷線信號轉發、app 層 ping-pong RTT）、
  `net_server.gd`（開埠＋握手版本閘：遊戲版本＋Balance.data_version()＋意圖，追蹤已認證客端）、
  `net_client.gd`（連上送握手、收 welcome/rejected、轉發斷線）。驗收 test_net_transport：真 ENet
  loopback（連上/底層 RTT/斷線）＋協定邏輯同程序匯流排（握手/遊戲版本閘/資料版本閘/意圖閘/RTT）；
  **1861 passed／洩漏 39/78 零新增**。備註：run_tests 於 _initialize 無幀迴圈、root 未 in-tree，
  @rpc-over-ENet 的樹內分派無法在此 harness 驗（拆兩層各自忠實，見測試檔頭註）；整合待場景於運行樹跑（P12-6+）。
  - 目標：`NetMessage`（訊息型別常數＋payload Dictionary＋JSON 序列化/驗證）、
    `NetPeerBase`/`NetServer`/`NetClient`（ENetMultiplayerPeer 封裝：開埠/連線/斷線信號轉發、
    **握手：遊戲版本＋`Balance.data_version()` 閘**、心跳與 RTT 量測）。不含 UI、不含遊戲邏輯。
    **設計紀律（Steam 之路，N §9.5）**：peer 建立集中於單一工廠（`NetTransport.create_server/client`），
    全案僅此一處知道用 ENet；位址抽象為「端點」——未來換 `SteamMultiplayerPeer` 只改一檔。
  - 驗收：headless loopback（同程序 server+client peer）：連上、握手成功；版本不符被拒且收到
    原因；心跳 RTT 有值；斷線信號觸發。（N §3）
- [x] **P12-4 公開快照序列化** — 2026-07-17：新增 `script/net/game_snapshot.gd`（`class_name GameSnapshot`，
  零 Node 純靜態）＝`encode(core)→Dictionary`（回合/當前玩家/分數/勝負、盤面棋子全公開欄位、
  **雙方手牌明細＋棄牌堆（D19）**、資源四類、各類次數、統計 export；**牌庫只給張數 deck_counts**）
  ＋`encode_piece(p)`（盤面重建與逐欄位驗證共用，含 Fuchsia 鏡像位置/本體職業；刻意排除
  hit_cards/_linker_ref/abilities/pending_death）。battle 端把 `_rebuild_board` 資料源一般化為
  `_board_pieces()`（快照非空取快照 pieces，否則即時 `encode_piece`），新增 `apply_snapshot(snap)`
  （空 Dictionary 還原 core 資料源）。**D19 鐵則**：快照永不含 seed/draw_pile/revealed_deck
  （模組刻意不讀，測試斷言 JSON 不含這三詞）。驗收 test_game_snapshot（純 GameCore 零 Node 零洩漏）：
  逐欄位一致（頂層＋每棋子 card_id/座標/HP/ATK/armor/extra/attack_types/numbness/鏡像）、不含隱藏欄位、
  JSON round-trip 正規化後冪等；test_battle_scene 複用既有場景驗「經 JSON 往返的快照重建盤面（含鏡像）
  視圖數＝棋子數、還原 core 一致」（不新增場景實例以守洩漏基準）。**1925 passed／洩漏 39/78 零新增**。
  - 目標：`GameCore → Dictionary` 公開快照（盤面棋子全欄位、**雙方手牌明細（D19）**、資源/
    分數/回合/攻擊次數、棄牌堆、統計 export；**不含 seed、不含牌庫序**——牌庫只給張數）＋
    `battle` 端「從快照重建盤面」（把 `_rebuild_board` 的資料源一般化為「core 或快照」）。
  - 驗收：headless：對局中途快照→重建，公開狀態逐欄位一致；斷言快照序列化後**不含**
    seed/牌庫序欄位；快照 JSON round-trip 穩定。（N §4）
- [x] **P12-5 伺服器程序與房間管理** — 2026-07-17：新增 `script/net/room_manager.gd`（`RoomManager`，
  零 Node 純邏輯房間狀態機：建房/加入/離開/座位就緒/大廳列表/生命週期 waiting→drafting→battling→ended→
  重開；房碼 4 碼去混淆字母表＋碰撞重試；上鎖必附密碼否則視為公開；滿座第三人依觀戰開關退旁觀或拒；
  無玩家即解散；**密碼為 server-only，列表/成員視圖皆不外送**）＋`net_game_server.gd`（`NetGameServer`
  extends NetServer：認證後把 create/join/leave/set_ready/list_rooms 訊息翻成 RoomManager 呼叫並廣播
  room_state／room_list／room_closed，非法請求回 lobby_error 不斷線；斷線自動離房）＋`server_main.gd`
  （headless SceneTree 入口：載 server_config.json＋命令列 `key=value` 覆蓋、開 NetGameServer、常駐不 quit）。
  net_message 增大廳訊息型別＋原因常數；net_client 增大廳送訊 API＋信號。驗收 test_room_manager：
  (A) RoomManager 純邏輯走完整驗收流程（建房→列表上鎖標記→密碼錯拒→兩人入座就緒→滿座第三人退旁觀/關觀戰則拒→
  離開解散）＋觀戰上限/房間上限/生命週期轉換/房碼格式；(B) NetGameServer 同程序 _Bus 多 peer 大廳流程
  ＋斷線自動離房；(C) server_main.parse_config 預設/JSON/命令列覆蓋。**2117 passed／洩漏 39/78 零新增**。
  備註：廣播經 JSON 後 peer id 為浮點，用戶端顯示層成員判定需 int 轉換（測試已示範）；每房權威 GameCore
  於「對戰起」建立留待 P12-6/P12-8。
  - 目標：headless 常駐入口（`script/net/server_main.gd`，`--headless -s` 可跑，
    預設埠 **24242/UDP**、`server_config.json`/命令列可覆蓋）＋
    `RoomManager`：大廳（房間列表查詢）、建房（名稱、公開/上鎖＋密碼、觀戰開關、房碼）、
    加入（房碼或列表；上鎖驗密碼）、離開/解散、座位（P1/P2）與就緒、多房間並行；
    房間生命週期 等待→BP→對戰→終局→重開或解散。每房一顆權威 GameCore（對戰起）。
  - 驗收：headless 多 peer：建房→列表可見（上鎖房標記）→密碼錯誤被拒→兩人入座就緒→
    滿房第三人轉旁觀或被拒（依觀戰開關）→離開解散。（N §5）
- [x] **P12-6 連線對戰核心（server 權威）** — 2026-07-17：新增 `script/net/`＝`net_codec.gd`
  （GameAction 白名單／不可信輸入拒收；GameEvent 經 JSON 往返、Vector2i↔[x,y]；core 不動）、
  `net_game_session.gd`（`NetGameSession`＝每房權威 GameCore 薄包裝，零 Node：start／apply_action
  ＝dispatch→drain_events→logic_step 迴圈嚴格對齊本機 battle.gd／server 權威回合計時 tick 逾時
  end_turn；seed 只存此、開發旗標預設牌組跳過 BP 先驗鏈路）。net_game_server 接對戰訊息：
  T_START_BATTLE（限房內玩家＋兩席就緒，waiting→drafting→battling 建 session、廣播開局快照＋事件）、
  T_GAME_ACTION（席位由 server 認定不採 client 值→回合閘＋旁觀者拒→apply→廣播事件流＋回合交接
  校正快照；終局→game_over＋end_battle）；`_process`／`tick_sessions` 每幀推進計時廣播；斷線/解散
  清 session。net_client 加 start_battle/send_action＋battle_events/snapshot_received/game_over/
  action_rejected 信號。room_manager 加 player_seat/state_of。驗收 test_net_battle：(A) codec 往返
  ＋不可信拒收；(B) session 開局/換手/逾時；(C) 同程序 _Bus 兩 client AI 驅動走完整局（player1 勝、
  31 回合）——**server 權威核心與參考 session 逐位一致（分數/回合/統計 export/公開快照除 instance_id）、
  兩 client 快照＝server 且彼此一致（D19 單一公開快照）**；非當前玩家與旁觀者行動被拒；server tick
  逾時廣播換手。**2165 passed／洩漏 39/78 零新增**。備註：instance_id 為行程內物件識別碼、跨獨立
  core 本就不同（非對局狀態），逐位比對時排除；@rpc-over-ENet 整合待運行樹（P12-7/11）。
  **對戰計時的表現層節奏（客端動畫忙碌暫停）本任務只到 server 權威骨幹，UI 手感待 P12-7＋【人工】。**
  - 目標：client 組 `GameAction` → server `dispatch`（合法性/回合閘）→ 廣播 `GameEvent` 流
    ＋回合交接/關鍵點廣播公開快照（單一份，全房間同一視角，D19）；client 照既有
    事件→`CombatScheduler` 播動畫、非本回合鎖輸入；回合計時 server 權威（逾時 server 端 end_turn）。
  - 驗收：headless loopback 兩 client 打完整局：**兩端最終分數/回合/統計 export 與 server
    完全一致**；非當前玩家的 action 被 server 拒絕；計時逾時由 server 定奪。（N §6）
- [x] **P12-7 大廳與房間 UI** — 2026-07-17：新增 `scenes/online/online_lobby.tscn`＋`.gd`（表現層薄殼、
  持一顆 NetClient，ENet 建立仍集中 NetTransport 工廠）四面板單場景狀態機＝連線設定（暱稱/位址/埠，
  帶入上次設定）／大廳（公開列表可點列＝房碼·名稱·玩家n/2·觀m/limit·🔒·狀態、重新整理、房碼＋密碼＋
  旁觀加入、中斷）／建房（名稱/上鎖＋密碼/允許旁觀，驗證上鎖必填密碼）／房內（房碼供邀請、P1/P2 席位
  ＋就緒、旁觀數、延遲 ms、就緒鈕、房主開始鈕、離開）；NetClient 信號全接（welcomed→列房、rejected/
  失敗→退回並顯示明確訊息＝reason_text 對映版本閘等、room_updated→房內、rtt→延遲、snapshot/over→
  房內狀態提示）；週期 ping 量 RTT。主選單加「線上對戰」鈕（八鈕改 pitch 60 重排、1024×768 容得下）；
  SettingsStore 加 net_nickname/net_host(127.0.0.1)/net_port(24242) 並全量寫回不影響其他設定。
  驗收 test_online_lobby（純表現層方法、不需連線）：節點樹／面板切換／房列表填充（空＋兩房）／房態套用
  （房主：對手空位開始鈕禁用、雙方就緒啟用、就緒鈕文字切換、P2 標對手；旁觀：無就緒/開始鈕、旁觀中）／
  房碼＋建房欄位驗證／拒絕原因文字／連線設定 round-trip。**2256 passed／洩漏 39/78 零新增**。
  備註：**實際連線流程屬【人工】跨機測**（@rpc-over-ENet 需運行樹＋遠端 server，不在 -s harness）；
  **連線對戰畫面**（收快照/事件播動畫、唯讀旁觀 battle）未接——房內「開始」發 start_battle（開發旗標
  跳過 BP），開局快照僅狀態提示，正式 battle 接線待 P12-8/P12-9。**四面板版面＋主選單新排版待【人工】過目。**
  - 目標：主選單「線上對戰」→ 伺服器位址/埠設定（存 settings，預設記住上次）→ 大廳
    （公開房列表＋鎖圖示＋人數/觀戰數、重新整理、輸入房碼加入）、建房面板（名稱/公開/密碼/
    觀戰開關）、房內畫面（雙方座位與就緒、延遲顯示、房碼供邀請、開始）。版本閘拒絕給明確訊息。
  - 驗收：headless node tree＋面板開關/填充/欄位驗證；連線流程【人工】跨機測。
- [x] **P12-8 連線選秀（BP）** — 2026-07-17：新增 `script/net/net_draft_session.gd`（`NetDraftSession`，
  RefCounted 零 Node＝每房權威 DraftState＋DraftDispatcher 薄包裝：apply 一位席位玩家的選秀行動、
  server 權威選秀計時逾時 `auto_fill_and_advance`、公開 view／decks／seed 只存此）；`net_codec.gd` 加
  `encode/decode_draft_action`（白名單 add/remove/remove_last/advance/confirm；toggle 類不上網；player
  由 server 指派）；`net_game_server.gd` 接 `T_START_DRAFT`（房內玩家＋兩席就緒→waiting→drafting、建
  session、廣播房態＋開局選秀狀態）、`T_DRAFT_ACTION`（席位由 server 認定、旁觀者拒、回合閘由 dispatcher
  →apply→廣播公開 view；完成→`_complete_draft`＝建 GameCore(seed 沿用選秀階段) drafting→battling→
  `_launch_battle` 發首份快照）＋`tick_sessions` 推進選秀計時逾時廣播；`net_client.gd` 加 `start_draft`／
  `send_draft_action`＋信號 `draft_updated`／`draft_rejected`；online_lobby 接 draft 信號（房內狀態列即時
  顯示階段/當前選手/雙方張數，reason_text 增選秀原因）。**start_battle 開發旗標保留**（跳過 BP）。
  驗收 test_net_draft（同程序 _Bus，@rpc 整合待運行樹）：(A) codec 往返＋不可信拒收；(B) NetDraftSession
  三階段/回合閘/同名上限/完成＋view 不含 seed；(B2) server 權威計時逾時 auto_fill 連續走完 BP；
  (C) 兩 client 走完三階段——**雙方牌組兩端一致（單一公開 view）＝預期、完成後 server 移除選秀 session／建
  對戰 session／房間 battling／兩端收開局快照、對戰 core 雙方 deck＝選秀結果**；非編輯方與旁觀者選秀行動被拒。
  **2312 passed／洩漏 39/78 零新增**。備註：**連線選秀畫面**（draft.tscn 接 NetClient：可點選牌、非編輯方
  鎖定「對方選牌中」、完成進對戰）與 battle 客端渲染同屬待接線；lobby「開始」暫走 start_battle 開發旗標，
  換 start_draft＝一行事，待客端 BP/對戰場景落地。**逾時補牌只由 server 產生（(B2)/(C) 已驗）。**
  - 目標：房間流程串 BP→對戰：`DraftAction` 經 server 權威同步（回合閘/上限規則同本機），
    非編輯方鎖定顯示「對方選牌中」；選秀計時逾時由 **server** 執行 `auto_fill_and_advance` 並廣播；
    完成後 server 建 GameCore 發首份快照進對戰。
  - 驗收：headless 兩 client 走完三階段，雙方牌組兩端一致；逾時補牌只由 server 產生。（N §6）
- [x] **P12-9 旁觀系統** — 2026-07-18：核心＝**中途加入補送**（server 端）。既有基座（RoomManager
  旁觀入房/上限/觀戰開關/密碼、`_broadcast_to_room` 依成員清單廣播玩家＋旁觀者同一份、旁觀者
  action 已由 server 拒）之上，net_game_server 於 `_do_join` 成功後新增 `_send_catchup(sender, room)`：
  依房態只補送給新加入者——drafting→當前公開選秀 view、battling→當前公開快照、ended→終局快照＋勝方；
  waiting 無對局狀態不補送。此後事件/校正快照自然續播（新加入者已入成員清單）。全為 D19 公開資訊
  （含雙方手牌、不含 seed/牌庫序）。驗收 test_net_spectator（同程序 _Bus）：(A) 對戰中途加入→補送快照
  與 server 逐位一致（同顆 core，直接比對）、含雙方手牌、不含 seed/draw_pile、後續換手校正快照同步、
  旁觀者 action 被拒且權威核心未受影響；(B) 選秀中途加入→補送 view＝server（反映已選張數）、後續選秀
  同步、旁觀者 draft_action 被拒；(C) 終局後加入→收 game_over（含快照＋勝方＝可看統計）；(D) 上鎖房
  旁觀驗密碼（錯拒/對放行且為無席位旁觀）、關觀戰房拒旁觀。**2338 passed／洩漏 39/78 零新增**。
  備註：**旁觀者唯讀 battle/draft 場景**（無行動列、手牌不可點、「旁觀中」徽章＋觀戰人數）＝客端渲染，
  與 P12-7/P12-8 的 battle/draft 客端接線同屬待接線（大廳層旁觀顯示已於 P12-7 落地）；
  **@rpc-over-ENet 中途加入的實機跨機驗證屬【人工】**（-s harness 無運行樹，見測試檔頭註）。
  - 目標：以旁觀者身分加入（上鎖房同樣驗密碼；受觀戰開關控制）；收**同一份公開快照＋事件流**
    （＝看得到雙方手牌、看不到牌庫序，D19）；中途加入＝快照＋續播；旁觀者 UI 全唯讀
    （無行動列/手牌不可點）、顯示「旁觀中」與觀戰人數；終局可看統計。
  - 驗收：headless：對局中途第三 peer 以旁觀加入→重建狀態與 server 一致→後續事件同步；
    旁觀者送 action 被拒。（N §7）
- [x] **P12-10 斷線重連** — 2026-07-18：席位 token 存 RoomManager（server-only、單局有效、不落盤），
  入座即私下 `T_SEAT_TOKEN` 下發該玩家。房間新增 `tokens/held/hold_remaining` 三席位欄位；`_seat_taken`
  （live peer 或 held）一般化 `_free_seat/_player_count`（held 不放給新人、不解散）。`handle_disconnect`
  （對局中玩家→held＋倒數／旁觀者與非對局→leave）、`reconnect`（依 token 找席位、逐出殘留舊連線、收復）、
  `tick_holds`／`vacate_held_seat`／`has_held_seat`／`seat_peer`／`force_waiting`。握手層 `_handle_hello`
  override：父類版本閘＋認證通過後若帶 token→`_handle_reconnect`（`_evict_peer` 逐出舊連線→廣播房態→
  重發 token→`_send_catchup` 續播，複用 P12-9）。`_on_peer_left` 改走 `handle_disconnect`（held→廣播房態、
  計時暫停）；`tick_sessions` 先 `tick_holds`→`_on_hold_expired`（**對戰中對手判勝**＝game_over winner=對手/
  reason=opponent_forfeit/房 ended；選秀中或無 session→回 waiting；無玩家→解散 room_closed），
  `has_held_seat` 房**跳過回合/選秀計時**（§8 不懲罰掉線方）。`member_view` 增 `held`（不含 token/password）。
  net_client 收 T_SEAT_TOKEN→存 `_token`＋`seat_token()`／信號（既有 `start(...,token)` 已帶進 hello）。
  server_main 灌 `seat_hold_seconds` config。驗收 test_net_reconnect（同程序 _Bus）：(A) 入座即發 16 碼
  token（雙席不同、不進廣播房態、client 存下）；(B) 對戰中斷線→held（不解散/對手見 held/計時暫停）、帶 token
  重連→收復席位（新 peer id）/held 清除/補送快照與 server 逐位一致（含雙方手牌、不含 seed/draw_pile）/計時
  恢復/可續玩；(C) 錯 token→bad_token、held 不受影響；(D) 逾時→對手判勝（房 ended）；(E) 旁觀者斷線直接移除。
  **2373 passed／洩漏 39/78 零新增**。備註：**客端重連 UI＋@rpc-over-ENet 實機跨機測屬待接線／【人工】**
  （同 P12-7/8/9 的 battle/draft 客端接線）；**逾時判定採 server 自動**（§8「對手可選判勝/再等」的按鈕列 UI 精修）。
  - 目標：席位 token（入座時發、client 存）；玩家斷線→席位保留 N 秒（可設）、對手畫面顯示
    等待；帶 token 重連→逐出舊連線、收復席位、補送快照＋續播；逾時未回→對手可判勝或解散。
    旁觀者斷線直接移除。
  - 驗收：headless：client 斷線→帶 token 重連→狀態一致可續玩；錯 token 被拒；逾時判定。（N §8）
- [x] **P12-11 部署與穩定性（收官；部署目標＝Ubuntu，N §9）** — 2026-07-18：server 端 ReplayLog
  （NetGameSession 起錄 seed＋牌組＋成功 action 流；NetGameServer.save_replays 於終局／判勝存
  `user://replays/*.jsonl`，P11-2 格式，測試預設關不寫檔）；`deploy/`＝`run_server.sh`（bash）＋
  `afternoon-brainstorm-server.service`（systemd：Restart=always/開機自啟/journald/SIGTERM）＋
  `server_config.json` 範本＋`deploy/README.md`（ufw allow 24242/udp、user:// 設定路徑、systemd 安裝、
  用戶端預設位址、安全＋Steam 展望）；主 README 增「架伺服器」節；用戶端預設位址已於 P12-7 內建
  （net_host 127.0.0.1／net_port 24242，設定可改）。**另修正 server_main 實機無法啟動的 P12-5 遺留 bug**：
  以 `-s` 進入點時 autoload `Balance` 不進主腳本＋依賴鏈編譯期→改動態 `load().new()` NetGameServer
  ＋`_data_version()` 執行期查 `/root/Balance`；`-s` 主迴圈不自動建 MultiplayerAPI 且 `_initialize` 內
  新節點未完整入樹→開伺服器延到 `_boot.call_deferred()`＋`set_multiplayer(SceneMultiplayer.new(), 節點路徑)`。
  驗收：新增 `test_net_soak.gd`（同程序 _Bus）——(A) 連續 3 局＋每局中途掉線帶 token 重連續打到底/房間
  存續/兩端快照一致；(B) 旁觀者反覆進出（補送快照＝server/行動被拒/核心不受影響）；(C) server ReplayLog
  用 ReplayLog.simulate 重播＝權威核心逐位一致。**實機 server 啟動煙霧**：`-s server_main.gd -- port=39321`
  成功開埠並印版本（godot-0.1.0／bal 4.3.0.0）常駐。**2426 passed／洩漏 39/78 零新增**。備註：**實機部署
  ＋兩台以上跨機對戰/旁觀＋@rpc 跨機路徑（客端 NetClient 掛對應路徑）驗證屬【人工】**（deploy/README.md
  步驟備妥；客端 battle/draft 渲染接線同 P12-7/8/9 待接線）；用戶端預設 net_host 仍 127.0.0.1（部署改公網 IP）。
  - 目標：Ubuntu 部署包——bash 啟動腳本＋**systemd unit 範本**（自動重啟/開機自啟/journald）
    ＋`ufw allow 24242/udp` 說明＋`server_config.json`（埠/最大房間/觀戰上限/席位保留秒數）；
    README 新增「架伺服器」一節（Linux Godot 4.7 `--headless` 跑 `server_main.gd`）；
    長跑 soak 測試（headless 連續多局、旁觀加入退出、重連，斷言零洩漏）；
    對局結束 server 端存 ReplayLog（沿用 P11-2 格式）。用戶端內建預設伺服器位址（可手動改）。
  - 驗收：soak headless 全綠；【人工】實機部署到使用者 Ubuntu 主機、兩台以上跨機對戰＋旁觀驗證。

### Phase 12 後段：連線對戰畫面接線（**當前主線**；規劃見 N §11）

> **現況（2026-07-18，使用者實機驗證）**：伺服器端 P12-2～P12-11 全落地並部署成功——
> 連線/建房/就緒/「開始」後 server 權威對局真的在跑（loopback 測試護體）；
> **但用戶端還沒有連線對戰畫面**：玩家停在房內面板、只看得到狀態提示文字。
> 後段＝把 battle/draft 場景接上 NetClient，補完整條線上流程
> （BP→對戰→終局→再戰），含旁觀畫面與重連 UX。伺服器端無需新協定即可支撐全部後段任務
> （再戰=既有 `force_waiting`；如需補小欄位如 remaining 秒數，屬相容性追加）。
> **架構鐵則（實機 bug f329077 的教訓，N §11）**：@rpc 兩端節點路徑必須一致——
> NetClient 常駐 `online_lobby` 之下**不得搬移**；連線對戰/選秀以**子場景嵌入 lobby**
> （隱藏大廳 UI、結束後釋放子場景回房內面板），**不做 change_scene 整場景切換**。

- [x] **P12-12 連線對戰場景接線（battle net 模式骨幹）** — 2026-07-18：新增 `script/net/net_mirror.gd`
  （`NetMirror`：公開快照→唯讀「顯示鏡像」GameCore，讓 battle 既有讀取碼原樣重用；鏡像永不 dispatch、
  rng=null、牌庫只還原張數，§11.2-4）。battle.gd 加第四模式 `boot_net(client, my_seat, snapshot, spectator=false)`：
  連 NetClient 信號（battle_events→CombatScheduler 播動畫、snapshot→resync 鏡像+盤面、game_over→勝負面板、
  action_rejected→狀態列）；`_do` net 分支**只 encode 送 server、絕不本地 dispatch**（§11.2-3）；輸入 gating 單點化
  `_net_input_allowed()`（我的席位＝當前玩家＋非忙碌＋非旁觀＋未結束，§11.2-5）；事件佇列＋忙碌時暫存校正快照；
  net 模式 `_new_game`/win 面板 change_scene/`_open_end_game` 皆守（回房/再戰閉環＝P12-15）。online_lobby.gd：
  首份對戰快照→**instantiate battle 子場景嵌入大廳、隱藏大廳 UI**（不 change_scene，NetClient 常駐存活，§11.2-2）、
  離開/斷線/房解散釋放子場景。**另修 P8-5 scoreboard 既有洩漏**：`_rebuild_meter` 於 score==0 時仍
  `ColorRect.new()` 卻不掛樹＝孤兒（正是歷史 39/78 洩漏基準的來源）→改為僅有領先時建立掛入；
  併修 `play_attack` 瞬時模式提前返回（零特效不變性，避免 headless 投射物堆積）。驗收 test_net_battle_scene
  （同程序 _Bus＋瞬時模式）：兩 battle 場景經 server 打完整局——開局盤面＝快照棋子數、非我回合輸入**零送信**、
  被拒 action 顯示訊息、**兩端最終快照＝server 且彼此一致**（D19 單一公開快照）。**2439 passed／洩漏 0/0**
  （scoreboard 修復後全套件零洩漏，優於原 39/78 基準）。**實機跨機打一局體感待【人工】；HUD 數字/手牌於己方回合
  中途因 server 僅回合交接下發校正快照而略滯（盤面經事件即時演出），屬 §4 協定設計、非缺陷。**
  - 目標：battle 第四種模式 `boot_net(client, my_seat, snapshot)`（既有 本機/AI/回放 之外）：
    開局快照重建盤面；`battle_events` → 既有 CombatScheduler 播動畫；**行動不本地 dispatch、
    一律 encode 後 `send_action`**；只有「我的席位＝當前玩家 且 非動畫忙碌」開放輸入，
    否則鎖定並顯示「對方回合」；`action_rejected` 顯示於訊息列；換手校正快照 → resync；
    回合倒數顯示（server 於換手/快照附 remaining，客端做顯示性倒數，權威仍在 server）；
    `game_over` → 勝負面板。lobby「開始」→ 嵌入 battle 子場景（先走開發旗標跳過 BP，
    正式流程待 P12-13）。
  - 步驟：HUD/攻擊範圍預覽等讀取面建議走「快照 → display 鏡像盤面」重用既有讀取碼
    （**嚴禁對鏡像 dispatch**——net 模式的 `_do` 只送網路）；輸入 gating 集中一處。
  - 驗收：headless（同程序 _Bus＋瞬時模式）：兩個 battle 場景驅動兩 client 打完整局，
    兩端畫面狀態與 server 快照一致；非我回合的輸入**不產生任何送信**；被拒 action 顯示訊息；
    洩漏 39/78 零新增。【人工】跨機打一局體感。
- [x] **P12-13 連線選秀場景接線＋正式流程切換** — 2026-07-18：draft.gd 加第五種來源 `boot_net(client,
  my_seat, opening_view, spectator=false)`（連 NetClient draft_updated→重建顯示鏡像 `_state`／
  draft_rejected→訊息列；**net 分支只 encode 送 server、絕不本地 dispatch**，§11.2-3）；`_state` 於 net
  模式為由公開 view 重建的**唯讀鏡像**（phase＋雙方牌組，current_editor/can_advance 為衍生，嚴禁 dispatch）；
  輸入 gating 單點化 `_net_draft_input_allowed()`（我的席位＝當前編輯方＋非旁觀＋未 done，§11.2-5）→
  展示卡/牌組/進階/移除鈕依此 disable、非我回合＝零送信；階段標籤顯示「輪到你選牌／對方選牌中…／
  你＝先手 P1」＋server 權威剩餘秒；本機專用 toggle_timer/toggle_file 不上網（隱藏）。net_draft_session
  `view()` 追加 `remaining`（server 權威倒數；<0＝不顯示，相容性小追加 §11.2-6）。online_lobby：首份選秀
  view→**instantiate draft 子場景嵌入大廳、隱藏大廳 UI**（不 change_scene，NetClient 常駐存活）；開局對戰
  快照→**釋放 draft 子場景、嵌入 battle 子場景**（BP→對戰轉場）；離開/斷線/房解散一併釋放；房態廣播不再
  蓋掉子場景（guard 併查 `_draft_scene`）；**「開始」鈕由開發旗標 `start_battle` 改為 `start_draft`**（正式
  流程 BP→對戰；開發旗標保留於 NetClient 供除錯、不外露）。驗收 test_net_draft_scene（同程序 _Bus）：兩
  draft 場景經 server 走完整 BP——開局鏡像＝公開 view、非編輯方輸入**零送信**、同名超上限被拒顯示訊息、
  **兩端最終 view 一致＝server 牌組、完成後移除選秀 session／建對戰 session／房 battling／兩端收開局快照**、
  對戰 core 雙方 deck＝選秀結果、done 後零送信。**2460 passed／零洩漏（0/0）**。
  **實機跨機 BP 一輪體感待【人工】**；旁觀者唯讀變體（「旁觀中」徽章）＝P12-14。
  - 目標：draft 加 net 模式：公開 view 渲染、**輪到我才可點**、非編輯方鎖定顯示
    「對方選牌中…」、server 權威選秀倒數顯示、完成後自動進 battle net 子場景；
    lobby「開始」由開發旗標 `start_battle` 改為 **`start_draft`（正式流程 BP→對戰）**，
    開發旗標降級為 debug 保留不外露。
  - 驗收：headless 兩 client 經 draft 場景走完三階段→battle 轉場，雙方牌組與 server 一致；
    非編輯方點擊不送信。【人工】跨機 BP 一輪。
- [x] **P12-14 旁觀者唯讀畫面** — 2026-07-18：battle/draft net 模式的 spectator 變體（席位=""＝
  boot_net 判定旁觀）。**基座已於 P12-9/12/13 備妥**（server catchup 送快照/view、旁觀 action 恆被
  server 拒、gating 恆拒旁觀輸入），本任務補**客端唯讀 UI**：battle `_apply_net_spectator_controls()`
  隱藏行動控制（模式工具列/升級鈕/結束回合鈕，`_end_turn_btn` 提為成員）、`_rebuild_hand` 依
  `_net_spectator` 令**雙方手牌皆唯讀**（己方列亦 disabled）、`_net_status_text` 加「👁 旁觀中／觀戰 N 人」；
  draft `_refresh_body` 既有旁觀分支隱藏進階/移除鈕＋展示卡 disabled，`_net_phase_text` 加「👁 旁觀中／
  觀戰 N 人」。**觀戰人數**＝lobby 於 `_on_room_updated`／進場時把房態 `spectators.size()` 轉入子場景
  （`set_spectator_count`，battle/draft 皆有）。中途加入＝lobby 依 catchup（drafting→draft 子場景、
  battling→battle 子場景）直接開進行中畫面（承 P12-12/13 進場路徑，spectator 旗標由 `_my_seat()==""` 帶入）。
  驗收 test_net_spectator_scene（同程序 _Bus）：(A) 對戰中途旁觀加入→旁觀 battle 場景重建＝server 快照、
  行動控制隱藏、雙方手牌唯讀、任何點擊（盤面/手牌/結束回合）**零送信**、觀戰人數＋徽章顯示、後續換手
  校正快照同步；(B) 選秀中途旁觀加入→旁觀 draft 場景重建＝server view、進階/移除鈕隱藏、展示卡 disabled、
  點擊零送信、後續選秀同步。**2483 passed／洩漏 0/0**。**實機跨機旁觀＋終局統計檢視待【人工】**
  （終局→統計畫面閉環＝P12-15）。
  - 目標：battle/draft net 模式的旁觀變體（席位=spectator）：無行動列、手牌恆不可點、
    「旁觀中」徽章＋觀戰人數（room 狀態）；中途加入依 catchup（快照/view）直接開進行中畫面；
    終局可看統計。
  - 驗收：headless：第三 client 於對戰中途旁觀加入→場景重建與 server 快照一致→後續事件同步；
    旁觀畫面任何點擊**零送信**。【人工】跨機旁觀。
- [x] **P12-15 終局與再戰流程閉環** — 2026-07-18：連線終局→終局統計→回房→再戰閉環。
  server 端 net_message 加 `T_REMATCH`；net_game_server `_do_rematch`＝終局房（ended）玩家請求
  再來一局→首位請求者 `rooms.reopen`（ended→waiting 清就緒）＋丟棄上一局權威 session、標記本席就緒
  （雙方皆按＝兩席就緒→回一般 waiting 流程，房主按開始→BP→對戰，新 seed）；非玩家/旁觀者/非
  ended-或-waiting 皆回 lobby_error。GameSnapshot.encode 追加 `score_history`（公開 int 陣列，供終局
  折線；D19 無涉隱藏資訊）。end_game.gd 加 `boot_net`（net 模式：AgainBtn＝「再來一局」/MenuBtn＝
  「回房間」改 emit `net_rematch`/`net_back_to_room` 不 change_scene，§11.2-2；旁觀者無再來一局；
  reason=opponent_forfeit 標題加註；ReplayBtn 隱藏＝P12-18 選做）。battle.gd 加信號
  `net_game_finished`：net 終局不再用 battle 內建勝負面板（`_apply_net_snapshot` 於 net 恆 `_hide_win`），
  `_finish_net_game`（動畫播完後）emit 勝方/分數/門檻/score_history/stats/reason 交 lobby。online_lobby.gd：
  `_enter_battle` 連 `net_game_finished`→`_on_net_game_finished`→釋放 battle、嵌入 end_game 子場景
  （§11.2-2 不 change_scene）；`net_rematch`→送 rematch 回房內面板、`net_back_to_room`→回房；旁觀者
  終局後才加入（catchup 直送 game_over 無 battle 場景）→`_on_game_over` 直接開終局統計（P12-9(C)）；
  房內面板 ended 態 ReadyBtn＝「再來一局」（送 rematch）、`_on_room_updated` 於 end_scene 在場時不蓋畫面。
  驗收 test_net_endgame_scene（同程序 _Bus）：(A) 兩 battle 場景經 server 打到終局→各 emit
  net_game_finished，勝方/統計 export＝server 終局快照（正規化逐位一致）、以該資料 boot_net 建 end_game
  （net 版按鈕文字、旁觀者無再來一局、勝方＝server）；(B) 兩 client rematch→房 ended→waiting＋雙方就緒
  ＋上一局 session 已丟棄→房主開新局（新 seed）→兩端收全新開局快照（≠上局終局）、房 battling、新 session；
  (C) game_over(reason=opponent_forfeit)→net_game_finished 帶 forfeit＋狀態列訊息、end_game 標題加註。
  **2510 passed／洩漏 0/0**。備註：**實機跨機再戰一輪體感待【人工】**；斷線重連 UX（客端遮罩）＝P12-16。
  - 目標：連線終局 → 終局統計畫面（沿用 end_game，資料源＝終局快照的統計 export）→ 回房間
    （房態 ended：「再來一局」＝雙方重新就緒 → server `force_waiting` 重開流程；或離開回大廳）；
    對手逾時判勝（opponent_forfeit）顯示原因。
  - 驗收：headless：整局結束→終局統計→回房→再戰第二局成功（同成員、新 seed）；
    forfeit 路徑顯示原因文字。【人工】跨機再戰一次。
- [x] **P12-16 斷線重連 UX（客端）** — 2026-07-18：斷線偵測＋自動重連遮罩＋對手 held 顯示。
  online_lobby.gd 加 `UI_RECONNECT` 遮罩面板（online_lobby.tscn 新增 ReconnectPanel＝狀態文字＋
  「放棄，回主選單」鈕）；`_on_server_disconnected`＝持有席位 token（只有玩家有，旁觀者斷線直接移除
  §7）且原在房→捕捉 token／釋放舊 client（於自身信號回呼中 queue_free）／`_begin_reconnect`（純狀態、
  釋放子場景、顯示遮罩）；`_process` 退避倒數（RECONNECT_INTERVAL）到→`_reconnect_attempt`（建新 client
  掛 /root、`start(...token)`；@rpc 路徑鐵則不變）；上限 MAX_RECONNECT_ATTEMPTS 逾則 `_fail_reconnect` 回
  連線設定；`_on_welcomed`/`_on_room_updated` 於重連中不跳大廳、收房態＝恢復信號→清旗標（catchup 快照/
  view 隨後由既有 `_on_snapshot_received`/`_on_draft_updated` 重建子場景，承 P12-9 續播）；重連遭 rejected
  （版本閘）或 lobby_error(bad_token 席位逾時)→放棄回連線設定。連線參數（暱稱/位址/埠）於 `_on_connect`
  記住供重連沿用。**對手 held**：room_manager.member_view 廣播加 `hold_remaining`（§11.2-6 客端顯示性、
  權威仍在 server tick_holds）；lobby `_forward_opponent_held` 把「非我席位 held＋剩餘秒」轉入活躍
  battle/draft 子場景（`set_opponent_held`），battle `_net_status_text`／draft `_net_phase_text` 顯示
  「⏳ 對方斷線，等待重連（剩餘 n 秒）」；房內面板 held 席位文字＝「（斷線，等待重連…）」。逾時判勝
  （opponent_forfeit）承 P12-10 server 端＋P12-15 終局畫面。驗收 test_net_reconnect_scene：(A) battle/draft
  net 場景 `set_opponent_held` 顯示/清除等待提示（含剩餘秒）；(B) lobby 重連狀態機純方法——`_begin_reconnect`
  進遮罩存 token、`_on_room_updated` 恢復清旗標回房、`_fail_reconnect` 回連線設定顯因、held 席位文字、
  `_forward_opponent_held` 轉入子場景；(C) member_view 廣播 hold_remaining。**2529 passed／洩漏 0/0**。
  備註：**@rpc-over-ENet 實機拔線重連＋跨機續玩屬【人工】**（-s harness 無運行樹、不建實際連線，
  故不涉 @rpc 節點；`_reconnect_attempt` 建 client 走 get_tree().root 於運行樹才有意義，同 P12-7 註）。
  - 目標：斷線偵測（server_disconnected/心跳逾時）→ 重連遮罩：自動帶席位 token 退避重試、
    可手動放棄回主選單；重連成功依 catchup 房態**自動回到對戰/選秀子場景**續玩；
    對手 held → 對戰畫面顯示「對方斷線，等待重連（剩餘 n 秒）」、恢復或判勝收尾。
  - 驗收：headless：對戰中 client 斷開→帶 token 重連→場景恢復續玩且與 server 一致；
    對手側 held 顯示→恢復。【人工】跨機拔線實測。
- [ ] **P12-17 連線收官：品質顯示＋場景級 soak＋【人工】總驗收**
  - 目標：對戰 HUD 顯示 RTT/連線品質；斷線/重連/被拒訊息文案統一；**房內與對戰顯示對手暱稱**
    （2026-07-18 實機截圖：P2 顯示 `對手 #948868441`——握手層 `net_server.peers` 已存 nickname，
    補進 `member_view` 廣播即可，相容性小追加；空暱稱才退回 #peer-id）；場景級 soak
    （兩 client 場景連續多局＋旁觀進出＋中途重連，零洩漏）；deploy/README 客端章節補
    「預設位址改公網 IP」提醒。
  - 驗收：soak headless 全綠；【人工】Ubuntu 實機總驗收——兩台跨機 BP→對戰→終局→再戰、
    第三機旁觀、中途拔線重連、對手逾時判勝。
- [ ] **P12-18伺服器回放檔下載**
  - 目標：終局後 client 可向 server 索取該局 ReplayLog（JSONL）存本地 `user://replays/`，
    用既有回放模式觀看（對齊 Python `log_transfer` 精神）。
  - 驗收：headless：終局→請求→收檔→`ReplayLog.simulate` 重播與終局統計一致。

## Phase 13 爬塔模式（維持擱置，D11）

> 規格已備於 `07_爬塔模式與新功能.md`（T）。等 Phase 12 交付後再議；
> 屆時承擔教學功能（取代已移除的戰役模式，D10）。

---

## 行為疑義登記表（執行中發現 Python 行為與規格不符時記錄於此）

| 日期 | 發現 | 處理 |
|---|---|---|
| （範例）2026-07-10 | Brown TANK 設定鍵不存在（B1） | 按 01 §12 裁定實作 |
| 2026-07-10 | Godot `_spawn_card`（P1-2）順序為 append→occupy→deploy，與 Python `factory.spawn_card`（deploy→occupy→append）不同；deploy 時本子是否在場/格是否佔用有別，影響 SPB 佈署自我計數（多打 1 次）。 | 修正 Godot 為 deploy→occupy→append 對齊 Python（Python 為準）。既有 White/Red 無 deploy 能力，不受影響。 |
| 2026-07-11（P7-6） | end_game 圖表框 ColorRect（不透明）掛在 `_hud`(CanvasLayer)，渲染於折線所在 `_chart_layer`(Node2D) 之上，遮蔽折線（P2-5 既有 z-order 問題，非 Python 相關）。 | **已於 P8-6（2026-07-13）修正**：ChartFrame 移出 HUD 到基礎場景、樹序置於 ChartLayer 之前，渲染於折線之下。 |
| 2026-07-16（D19） | 手牌可見性：Python 版對戰畫面僅顯示己方手牌；使用者裁定**手牌屬公開資訊**（雙方玩家＋旁觀者皆可見，本機版同步適用）。 | 屬資訊呈現（D16 範疇）非規則變更，對局結果不受影響；以 D19 記錄，P12-2 實作。 |

## 給 Python 端的回報清單（不由本專案修改）

1. `card_setting.json` Green APT 的 `luck_increase` 未被程式使用。
2. 建議把 05 §2.3 表列的硬編碼常數移入 `config/setting.json`，兩引擎即可全量共用。
