# 06 任務清單（主執行文件）

> 執行規則：**由上往下、一次一個任務**。每個任務格式：目標 → 步驟 → 驗收。
> 完成後把 `[ ]` 改 `[x]` 並在行尾加一句備註（日期＋特殊處理）。
> 標【人工】的項目由使用者執行，自動代理**跳過它、不得代勾**。
> 規格出處縮寫：R=01規則、C=02卡牌、A=03AI、D=04架構、J=05JSON、T=07爬塔。
> 驗收指令統一為（本機 Godot 路徑已確認）：
> `& "d:\Code\Games\Godot_v4.7-stable_win64.exe" --headless --path "D:\Code\Games\AfternoonBrainStorm\AfternoonBrainstorming_godot" -s tests/run_tests.gd`
> （Godot 4.7 stable；PowerShell 用 `&` 呼叫。新增/改名 `class_name` 後先跑一次 `--headless --import` 重建 class cache。換機器時更新此路徑。）

---

## 方向宣告（2026-07-11 使用者決策，見 00 決策 D16）

- **美術與 UI 不再對齊 Python（pygame）版**：畫面、佈局、操作、演出以 Godot 版自身的
  易用性與觀感優先，自由設計，做得比原版更人性化。
- **平衡數值與規則行為仍以 Python 版為唯一基準**：`data/balance/` JSON 單向同步機制不變（J），
  規則核心（`script/core`）的行為測試仍以 Python 為準；**任何表現層改動不得改變對局結果**（全套件必須綠）。

## 已完成階段摘要（完整執行備註見 `歸檔_已完成任務.md`；逐次紀錄見 `進度日誌.md`）

| Phase | 內容 | 完成日 | 驗收 |
|---|---|---|---|
| 0 地基 | 測試 runner、sync_balance 同步腳本、BalanceDB、card_text | 2026-07-10 | 169 passed |
| 1 規則核心 | GameCore／傷害管線／能力系統（沉默/附魔）／10 色全卡＋特殊卡／token·luck·totem·coin·shadow 引擎 | 2026-07-10 | 529 passed |
| 2 表現層 | PieceView／動畫排程／對戰／選秀 BP／主選單＋設定＋終局；本機雙人流程閉環 | 2026-07-11 | 763 passed＋人工 |
| 6 收尾 | 刪除舊碼（213 檔）、入口收編 main_menu、README 重寫 | 2026-07-11 | 763 passed |
| 7 編輯器化＋去 v2 | UI 骨架宣告進 .tscn（`%` 唯一名稱綁定）、v2 字眼全移除 | 2026-07-11 | 846 passed |

對照舊編號：原 Phase 3（CPU AI）**解凍為 Phase 10**；Phase 4（戰役）已移除（D10）；
Phase 5（爬塔）維持擱置＝**Phase 13**（D11）；原 P6-3（JSONL 重播）轉正為 **P11-2**。

## 人工待辦（不擋自動任務；使用者有空時做）

- [ ] 【人工】**P7-7 總驗收**：編輯器逐一打開各 `.tscn` 確認可視可編輯；跑一輪全流程
  （選單→BP→對戰→終局→回選單）確認**可玩、無報錯**。
  註：Phase 8 起會刻意改變外觀，「與舊版外觀一致」的對比不再適用；
  end_game 折線被框遮住的既有問題已排入 P8-6 修正，不用另外回報。
- [ ] 【人工】**程式碼 review**：配合 `09_程式碼導覽.md` 走讀；發現要改的東西，
  直接在下方清單插入新任務（或告訴代理補任務）。

## 優先序說明（可調整）

**Phase 8（UI/UX 基座）→ 9（棋盤視角與演出）→ 10（CPU AI）→ 11（對局功能）→ 12（連線）** 為建議順序：
先把 UI 底盤（多分辨率、Theme、資訊呈現）做穩，美術演出與後續功能都蓋在它上面；
連線工程最大、對核心與 UI 穩定度要求最高，放最後。
**Phase 10（CPU AI）與 UI 完全獨立（純邏輯，headless 可驗），想先玩單人可隨時把它搬到最前。**
調整優先序＝直接搬動下方區段順序（自動代理由上往下取任務）。

---

## Phase 8 UI/UX 體驗基座

> 新方向第一步：不改任何規則，把「資訊呈現」與「畫面適應性」做到人性化。
> 本 Phase 起 UI 佈局/配色/文案自由設計（D16），不需比照 Python 版畫面。

- [x] **P8-1 多分辨率支援**（待辦：支援多分辨率）— 2026-07-12：`project.godot` 設 `stretch/mode=canvas_items`＋`aspect=expand`（基準 1024×768；expand 保證可視區兩軸皆 ≥ 基準，1024×768/1152×648/1280×720/1920×1080 皆不裁切互動元素）、`resizable=true`、`default_clear_color` 設深色使擴展邊緣與 UI 底色一致。四場景 Background 改全矩形錨點（填滿擴展視窗）；主選單標題/副標/訊息全寬置中、五鈕水平置中、版本標籤錨底左、設定面板置中；終局標題全寬置中、兩鈕錨底左；battle 勝負面板置中。錨點由引擎自動 reflow，無需 resize 程式碼。**視覺待人工過目**（battle/draft 為左上錨點工作型佈局，寬螢幕右側留深色邊距＝非破版；世界層置中／更充分利用寬螢幕留待 P9-4 視覺迭代）。驗收：846 passed。
  - 目標：任意視窗大小／全螢幕下不破版、不裁切互動元素。
  - 步驟：`project.godot` 設 `display/window/stretch`（建議 `canvas_items`＋aspect `expand`，
    設計基準解析度由現行 1152×648 起評估，必要時改 1280×720）；四場景 HUD 控件由固定 offset
    改 anchors／Container 佈局；Node2D 世界（棋盤、展示館、終局圖表）加自適應 Camera2D 或依
    viewport 尺寸置中縮放（`piece_gallery.gd` 已有自動縮放 Camera2D 可參考）；監聽視窗 resize 重排。
  - 驗收：1920×1080、1280×720、1152×648、1024×768（4:3）各跑完整流程無破版、無元素超出畫面
    或互相重疊（人工過目截圖）；全套件綠。
- [x] **P8-2 全域 Theme 與視覺基調** — 2026-07-12：建 `theme/main_theme.tres`（Button 五態 StyleBoxFlat 圓角＋色態、Panel 圓角框、Label 主文字色＋黑描邊 outline 4、Button/Label 預設字級 15、default_font_size 16）＋`theme/色盤說明.md`（色盤表＋派別色走 Balance 不進 theme＋換字型插槽說明）；`project.godot` 加 `[gui] theme/custom` 掛成 `ThemeDB.project_theme`（四場景 root 為 Node2D 無 theme 欄位，改用專案預設主題＝全 Control 自動繼承，效果更全面）。四 `.tscn` 收斂逐節點 override 共 72 行（menu 13/end 5/draft 24/battle 30：移除全 Label 的 outline 對＋主文字 font_color＋font_size 15；保留強調色與特殊字級；draft/battle 背景統一 0.09,0.1,0.13）。驗收：846 passed（與基準相同）；ThemeDB.project_theme 已註冊、stylebox/色/字級查詢正確。**視覺待人工過目**（按鈕圓角/hover/focus 觀感、四場景一致性）。
  - 目標：全專案統一 Theme 資源（字體、字級、色盤、按鈕/面板樣式），取代 `.tscn` 內散落的
    逐節點 theme override；為後續美術優化打底。
  - 步驟：建 `theme/main_theme.tres`＋色盤說明（背景/面板/主文字/強調色；派別色沿用
    Balance `color_rgb`）；四場景 root 掛 theme、清除可收斂的逐節點 override；
    字體先用內建，留自訂字型檔插槽（之後換字型只改 theme）。
  - 驗收：四場景觀感一致（按鈕/面板/字級統一）；全套件綠；【人工】過目。
- [x] **P8-3 關鍵字系統（描述高亮＋懸停備註浮窗）** — 2026-07-12：建 `data/keywords.json`（20 正規機制詞＋別名，各帶顯示色＋繁中解釋）；`scenes/ui/keyword_db.gd`（`class_name KeywordDB`，純靜態：`markup()` 貪婪長詞優先把機制詞包 `[url=key][color]…[/color][/url]`，保留既有 BBCode 與 `[img]` 路徑不動、既有 `[color]` 內只加 url 不重上色；`explain()` 供浮窗）＋`scenes/ui/keyword_label.gd`（`class_name KeywordLabel` extends RichTextLabel，`set_source()` 灌文字、`meta_hover_started/ended` 彈出/收起解釋 PanelContainer 浮窗，隨滑鼠定位、翻邊防出界）。battle `HintLabel` 由 Label 改 KeywordLabel（提示多行併單行、名稱+hint 走 markup）；draft 新增底部 `CardDetail` 說明區（懸停展示館/牌組卡片顯示名稱+完整描述）。驗收：新增 `tests/test_keywords.gd`（entry 格式、markup 轉換多案例、REQUIRED 覆蓋度、REQUIRED 確實出現於 card_text）；全套件 **946 passed**（846→946）。**視覺待人工過目**（battle/draft 懸停浮窗、高亮配色、CardDetail 版面）；注意：攻擊模式詞（大十字/九宮格…）刻意未納入高亮，留給 P8-4 attack_pattern 圖示。
  - 目標：卡牌描述/提示中的機制詞（麻痺、護盾、怒氣、斬殺、圖騰/刻印、Token、運氣、
    金幣/升級、鏡像、沉默、附魔、方塊、幸運方塊…）自動上色高亮；滑鼠懸停顯示該詞的解釋備註浮窗。
  - 步驟：建 `data/keywords.json`（詞 → {顯示色, 解釋}，繁中，涵蓋 R/C 全機制詞）；
    共用元件 `scenes/ui/keyword_label.gd`（RichTextLabel＋BBCode，描述→自動標 `[url]` meta，
    `meta_hover_started/ended` 顯示/收起浮窗）；battle 棋子提示、draft 卡牌提示改用該元件。
  - 驗收：headless 測「純文字→BBCode 轉換」正確與關鍵字表完整性（掃 card_text 出現的機制詞都有解釋）；
    【人工】battle/draft 懸停浮窗抽查。
- [x] **P8-4 百科字典（圖鑑）** — 2026-07-13：建 `scenes/encyclopedia/encyclopedia.tscn`＋`.gd`（Node2D 骨架宣告進 .tscn，`%` 唯一名稱綁定）。左側 10 色分頁＋職業篩選（全部＋8 職業）→ 卡片清單（GridContainer，色頁實卡＝JOB_ORDER，魅紫用 PURPLE_JOBS 4 張，避開 card_setting 內 4 個無描述的 Purple 佔位條目）；右側詳情：名稱/id、生命·攻擊·費用、攻擊範圍圖示（`img/UI/attack_pattern/`，tag→檔名對照表 `ATTACK_ICON`）、能力描述（沿用 P8-3 KeywordLabel 機制詞高亮＋懸停解釋）、PieceView 佔位圖預覽、衍生物連結（描述含「影子/鏡像」→SHADOW、「幸運方塊」→LUCKYBLOCK，可點切換預覽/說明）。資料全走 Balance（stats/text/attack_types/color_rgb/job_of/color_code_of）。主選單「戰役模式（尚未開放）」灰鈕改為「百科圖鑑」入口（節點更名 CampaignBtn→EncyclopediaBtn，test_menu_flow 節點清單同步）。驗收：新增 `tests/test_encyclopedia.gd`（節點樹、建構＋預設詳情、色系/職業篩選、攻擊圖示對應無缺漏、76 張實卡詳情資料完整、衍生物連結偵測）；全套件 **1228 passed**（946→1228）。**視覺待人工過目**（版面/瀏覽動線、詳情頁排版、攻擊圖示與預覽觀感）。
  - 目標：遊戲內百科：全卡瀏覽（色系/職業篩選）、單卡詳情頁（數值、攻擊模式圖示、
    能力描述含關鍵字高亮、衍生物連結如 SHADOW/LUCKYBLOCK）。
  - 步驟：以 `piece_gallery` 為基礎建 `scenes/encyclopedia/`（沿用 PieceView 佔位圖）；
    攻擊模式圖示用現成的 `img/UI/attack_pattern/`（cross/large_cross/x/near/far…，目前閒置）；
    資料全走 Balance＋card_text＋keywords，零硬編碼；主選單加「百科」入口（取代「戰役（尚未開放）」灰鈕位）。
  - 驗收：headless 建構＋資料完整性（全卡皆有詳情資料、圖示對應 attack_types 無缺漏）；
    【人工】抽查 5 張卡內容正確、瀏覽動線順手。
- [x] **P8-5 記分板改版** — 2026-07-13：新增 `scenes/battle/scoreboard.tscn`＋`.gd`（`class_name Scoreboard`，純表現層、零 core 依賴，資料由 `update_board(score, win_threshold, turn, current, history)` 以基本型別傳入）。呈現＝(1) 回合列（回合數＋當前玩家、以派色標示）、(2) 分差讀數（P1/P2 領先 N／平手，達門檻加「達門檻！」）、(3) diverging 記分條（中央 0、左紅 P1 負分／右藍 P2 正分填色，端點 ±門檻，中線標 0）、(4) 近 8 回合趨勢迷你長條（中線上下發散、下紅 P1／上藍 P2、以門檻為滿格）。分數語義同 core：負＝先手 P1 領先、正＝後手 P2 領先、|score|≥門檻獲勝。battle.tscn 以 instance 嵌入（`%Scoreboard`，取代原 `%ScoreLabel`＋`%TurnLabel`），錨點左上（P8-1）；battle.gd `_refresh_hud` 改呼叫 `_scoreboard.update_board(...)`。動態長條用 `free()`（無信號 ColorRect，即時且 headless 無 frame 也可清）＋尺寸常數（不依賴 headless 未必更新的 Control.size）。驗收：新增 `tests/test_scoreboard.gd`（節點樹／領先方與讀數含正負分／達門檻旗標與標示／回合文字／記分條與趨勢動態子節點數＝含更新不累積與 >8 只取最近 8）＋更新 `test_battle_scene`（節點樹改列 Scoreboard、分數/回合同步斷言）；`--import` 後 **1260 passed**（1228→1260，零回歸，exit 0）。**視覺待人工過目**（記分條/趨勢配色與可讀性、頂部佈局不與資源列重疊）。
  - 目標：對戰分數呈現改為一眼可讀的記分板：雙方目前分數、±勝利門檻進度、回合數、
    最近數回合的分數變化趨勢（迷你長條或箭頭）。
  - 步驟：從 battle HUD 拆出獨立元件 `scenes/battle/scoreboard.tscn`＋`.gd`；
    資料讀 core 的 `score`/`score_history`/回合數；佈局配合 P8-1 錨點規範。
  - 驗收：headless 斷言記分板顯示值與 core 同步（含正負分、達門檻）；【人工】可讀性確認。
- [x] **P8-6 終局統計強化（含折線 z-order 修正）** — 2026-07-13：(1) **z-order 修正**：`end_game.tscn` 的 `ChartFrame` 由 `HUD`(CanvasLayer) 移到基礎場景（`Background` 之後、`ChartLayer` 之前），依樹序渲染於折線之下——解決 P7-6 登記的「折線被不透明框遮住」（CanvasLayer 恆蓋 Node2D）問題（行為疑義登記表對應列標記已修正）。(2) **統計頁升級**：`configure()` 改收 core 完整統計 export（`Statistics.export_for_charts()`，格式 `{stat_name:{owner_cardid:int}}`）為單一資料源；**摘要長條**改由 end_game `_bars_for()` 從 export 派生前 5 名（原 `battle._build_stat_bars` 刪除）；新增 **per 玩家 per 卡統計表格**（GridContainer→`%TableRoot`，欄＝擊殺/造成傷害/得分，依玩家分組、卡 ID 排序）；**圖／表切換鈕**（`%ViewToggle`→`toggle_view()`，切換圖表群[ChartFrame/ChartLayer/BarsRoot/Caption]與表格群可見性）。資料源 `table_data()` 由 export 派生（未列欄位補 0）。備註：治療（HEALING）未被 core 追蹤（只有 per-player 的 HEAL_USE），故不列入 per-卡表格（見進度日誌），如需另開 core 任務補追蹤。驗收：更新 `test_menu_flow`（新 export 格式、ChartFrame 樹序在 ChartLayer 前、`table_data()` 與 Statistics 一致含補 0、圖/表切換可見性、空統計不崩潰、摘要長條前 5 排序經真實 battle stats）→ **1275 passed**（1260→1275，零回歸，exit 0）。**視覺待人工過目**（折線現應可見、表格版面與圖/表切換）。
  - 目標：修正 end_game 折線被 ChartFrame 遮住的既有問題（P7-6 登記）；統計頁升級：
    每回合分數折線、統計表格（擊殺/傷害/治療/得分，per 玩家 per 卡）、圖/表切換。
  - 步驟：ChartFrame 移到折線層之下（或改半透明背景框）；表格用 Tree 或 GridContainer；
    資料源 `Statistics`（key 格式見 09 §3.1）；保留現有長條圖為「摘要」區。
  - 驗收：折線可見；表格數字與 Statistics 一致（headless）；【人工】過目。

## Phase 9 棋盤視角與演出美術

- [x] **P9-1 斜 45 度棋盤（等距視角）** — 2026-07-13：新增 `scenes/battle/board_view.gd`（`class_name BoardView`，RefCounted 零 Node 依賴、純數學）統一 cell↔pixel：`corner(gx,gy)`（格線交點基元，等距 `sx=ox+(gx−gy)·HW、sy=oy+(gx+gy)·HH`；正交沿用 ORIGIN=(40,150)/STRIDE=118）、`cell_center/cell_topleft/cell_polygon（方形↔菱形）/pixel_to_grid（反矩陣）/cell_from_pixel/depth(x+y)`；`Mode.ORTHO|ISO` 可切，**預設 ISO**。battle.gd：移除舊 ORIGIN/STRIDE/INSET/`_cell_rect`，`_cell_topleft/_cell_center/_cell_from_global` 全委派 `_view`；`_layout_grid()` 把 .tscn 預置的 10 條格線（H0..H4/V0..V4）依模式重排為方格/菱形（節點數不變，`_test_node_tree` 10 條斷言仍成立）；棋子/鏡像視圖 `z_index=_view.depth(cell)` 決定等距遮擋（畫面越前越上層）；高亮/攻擊範圍預覽改 `draw_colored_polygon`+`draw_polyline`（走 cell_polygon，兩模式通用）；scheduler 經既有 `_cell_center` callback 自動吃等距（攻擊瞄準/投射物/飄字定位一致）；佔位形狀不變（不做偽 3D）。**兩視角並行可切換**：HUD 新增「視角」鈕（`%ViewToggle`，文字反映當前＝「視角：45度」/「視角：俯視」，沿用提示/動畫鈕慣例）＋ KEY_V 快捷，即時重排不影響對局。驗收：新增 `tests/test_board_view.gd`（兩模式 cell→pixel→cell 恆等含格內取樣、正交像素與 P2 舊常數一致、topleft+半格=中心、等距菱形頂點/depth 遞增、界外 (-1,-1)）＋ `test_battle_scene` 視角切換案例（預設 ISO／鈕文字／切換後模式與格線斜↔平／兩模式反算恆等）＋節點樹加列 ViewToggle；`--import` 後 **1461 passed**（1275→1461，零回歸，exit 0）。**視覺待人工過目**（俯視與 45 度兩視角切換對照、對局無錯位、格線菱形、遮擋順序；棋子 96 佔位在等距下相鄰列略有縱向重疊＝由 z 排序處理，屬預期）。
  - 目標：棋盤呈現改斜 45 度菱形格；**規則核心零改動**，純 view 層換算。
  - 步驟：抽出 `scenes/battle/board_view.gd` 統一 cell↔pixel 換算（正交/等距雙模式可切，
    等距：`px = (x−y)·w/2＋偏移、py = (x+y)·h/2＋偏移`）；格線改菱形；點擊輸入反算格座標；
    棋子依畫面 y 排序決定遮擋（z_index 或 Y-sort）；投射物/飄字/高亮/攻擊範圍預覽全部走同一換算；
    佔位形狀先不做偽 3D（美術替換時再處理立面）。
  - 驗收：headless：cell→pixel→cell 恆等、等距模式下 `test_battle_scene` 輸入路徑全過；
    【人工】等距棋盤完整對局無錯位、遮擋順序正確。
- [x] **P9-2 命中與死亡動畫強化** — 2026-07-13：純表現層強化，`script/core` 零改動、瞬時模式（動畫關）結果完全不變。(1) **受擊**（`piece_view.play_hurt`）：既有白閃＋抖動之外，加**局部命中頓幀**（squash-and-hold：壓扁 `1.18×0.84`→定格 0.045s→`TRANS_BACK` 回彈）＋**受擊火花粒子**（`_spawn_hit_particles`，程序生成 CPUParticles2D，非點陣素材）。(2) **死亡**（`play_death`）：既有淡出縮小之外，加**碎裂旋轉**（rotation→0.6）＋**短暫殘影**（`_spawn_afterimage`：複製本體多邊形於原位放大淡出）＋**碎片粒子**（`_spawn_death_particles`，本體填色向四面爆開）。(3) **擊殺鏡頭震動**：`combat_scheduler` 新增 `on_kill` 回呼（DEATH 動畫起始時呼叫、**瞬時模式不呼叫**）；battle `_camera_shake` 對世界根 Node2D 做衰減隨機位移（HUD 為 CanvasLayer 不受影響、震完歸位、用全域 randf 不動 RngService）。特效容器：PieceView 新增 `fx_layer`（由 battle/anim_demo 建視圖時設定），粒子/殘影掛 fx 層，本視圖 `queue_free` 後仍存活播完；null 時退回自身。anim_demo 亦接 `on_kill`＋fx_layer 供人工對照。驗收：新增 `test_piece_view._test_animation_instant_invariance`（瞬時受擊/死亡不生任何特效、不改 visual_root、死亡立即回呼；fx_layer 預設 null）＋`test_combat_scheduler._test_on_kill_hook`（非瞬時 DEATH 觸發 on_kill 一次、瞬時不觸發）；`--import` 後全套件 **1473 passed**（1465→1473，+8 全為新斷言，零回歸、零新洩漏＝與基準同 39 CanvasItem／78 ObjectDB，exit 0）。**視覺待人工過目**（跑一局或 F6 開 anim_demo.tscn：受擊頓幀手感、死亡碎片/殘影、擊殺鏡頭震動；I 鍵切瞬時應完全無特效）。
  - 現況：P2-2 已有基本 fallback（受擊白閃＋抖動、死亡淡出縮小、飄字）。
  - 目標：升級打擊感：受擊粒子（CPUParticles2D 即可）、命中頓幀（hit-stop）、
    死亡演出（碎裂/消散＋短暫殘影）、擊殺時輕微鏡頭震動。
  - 驗收：anim_demo 展示對照；**瞬時模式（動畫關）結果完全不變**；全套件綠；【人工】手感確認。
- [x] **P9-3 攻擊與能力觸發演出** — 2026-07-13：純表現層（`script/core` 零改動、瞬時模式結果完全不變）。(1) **職業攻擊差異化**：新增 `script/view/piece_animation_library.gd`（`class_name PieceAnimationLibrary`，static、零 Node）依 `Balance.attack_types(job)` 判定——含遠程 tag（`large_cross`/`large_x`/`farthest`）→ 發射投射物（ADC/SP），否則近戰撲擊（AP/TANK/HF/ASS/LF/APT）；battle `_make_piece_view`＋anim_demo 皆改由本庫指派 `PieceAnimationSet`（取代 anim_demo 原寫死的 `adc_ranged()`）。(2) **派別色命中特效**：`PieceAnimationSet` 加 `fx_color`；`Projectile`/`ImpactFlash` 加 `set_color()`；`piece_view` 投射物、近戰命中特效皆染攻擊者派別色（近戰亦於撲到位時在目標點濺命中特效，原僅遠程有）。(3) **能力觸發（CAST）回饋**：`piece_view.play_cast()` 由單純脈動升級為「脈動＋派別色能量環向外擴散」（程序生成，CAST 事件由 core 於能力型攻擊時發出）。(4) **資源事件飄字**：battle 於 `_do` 前快照雙方 luck/token/totem/coin，`_resync` 後以純函式 `resource_deltas()` 取正向變化，於資源面板旁飄「Pn +N 圖騰/藍球/金幣/運氣」（派別色，瞬時模式不演出）。驗收：新增 `tests/test_piece_animation_library.gd`（遠程/近戰判定跨色、派別色、特殊卡預設色）＋`test_piece_view` 擴充瞬時攻擊/施法不變性（遠程攻擊不改 visual_root、施法不生環、遠程有投射物/近戰無）＋`test_battle_scene` 加 ADC 視圖獲遠程演出＋`resource_deltas` 純函式（只報正向、多類多方）；`--import` 後 **1512 passed**（1473→1512，+39 全為新斷言，零回歸；洩漏 39/78＝與基準相同，零新洩漏）。**視覺待人工過目**（跑一局或 F6 開 anim_demo：遠程箭矢 vs 近戰撲擊、派別色命中閃光/能量環、資源獲得飄字）。**備註**：沉默/護盾增減的「專屬圖示飄字」與鏡像生成特效未含（鏡像已於盤面以半透明 shadow 視圖呈現）——需 core 補發 STATUS 事件＋視覺語彙定案，留待 P9-4（整體 UI 美術，持續型）或後續 core 事件化任務；使用者回饋要改另開任務。
  - 目標：職業攻擊差異化（近戰撲擊/遠程投射物已有骨架→補施法系彈道與命中特效）；
    能力/資源事件的視覺回饋：圖騰刻印、token 獲得、金幣、好/壞運、沉默、鏡像生成、
    護盾增減等，各以派別色特效＋圖示飄字呈現。
  - 步驟：CombatScheduler 已接 STATUS/RESOURCE/FLOAT 事件→逐類補演出；
    個別卡的差異化用 `PieceAnimationSet` 覆寫（`data/animation_sets/` 插槽已預留）。
  - 驗收：對局中各類觸發有可辨識回饋（列清單抽查）；瞬時模式等價；全套件綠；【人工】確認。
- [ ] **P9-4 整體 UI 美術優化（持續型任務）**（待辦：優化整體 UI 美術）
  - 目標：在 P8-2 Theme 基礎上逐場景美化：主選單視覺、BP 選卡卡片化、對戰 HUD 佈局、
    按鈕 hover/pressed 動效、場景轉場。點陣美術素材待使用者提供後經 SpriteSlot/Theme 換入；
    程式端先以向量/程序方式打磨。
  - 驗收：【人工】分場景逐一視覺驗收（本任務可多輪迭代，每輪一個場景）。

## Phase 10 CPU AI（原 Phase 3 解凍）

> 規格：`03_CPU_AI規格.md`（A）。驗收多為翻譯 Python `tests/test_ai_*.py`，headless 可自動推進。
> 原戰役模式已移除（D10）：AI 對戰入口改為主選單「**單人對戰**」——玩家 vs 選定色系/難度的 CPU。

- [x] **P10-1 ai_query 移植**（A §2 全函式，純 static、零 Node 依賴）— 2026-07-13：新增 `script/ai/ai_query.gd`（`class_name AIQuery` extends RefCounted，全 static、零 Node、零 rng——攻擊模式查詢刻意取「同距全部候選」而非 detection，不消耗 rng）。翻譯 Python `campaign/ai_query.py` 全函式：`empty_positions/is_corner/is_edge/position_safety/manhattan/enemy_cards/friendly_cards/attack_targets_from_pos(+_at)/nearest_enemy_distance/attack_coverage_cells/is_playable_unit_card/units_with_pending_move/move_destinations_for/attacker_would_hit_position/incoming_damage_at_position/projected_incoming_damage/cells_threatening_card`。GameState→GameCore 對照：`get_side_cards(owner,opp=True)`＝`get_enemies_of`（對手 on_board＋neutral）、`board_config.width/height`＝`BoardState.SIZE`、`number_of_attacks`/`opponent_name`/`get_player` 直接對應；卡 `.moving/.numbness`→`is_moving()/is_numb()`。常數 `ASS_THREAT_DAMAGE` 讀 `core.balance.ai("threat_model/ass_threat_damage")`（不寫死）。驗收：新增 `tests/test_ai_query.gd`（翻譯 test_ai_query.py 全 19 案例＋佈署/移動輔助），全檔共用單一 BalanceDB 並於 run() 結尾 free（零新增 orphan）；`--import` 後 **1564 passed**（1512→1564，零回歸；洩漏 39 CanvasItem／78 ObjectDB＝與基準相同）。
  - 驗收：翻譯 `tests/test_ai_query.py` 全數通過。
- [x] **P10-2 ai_evaluator 移植**（A §3–§5 全公式，常數走 BalanceDB）— 2026-07-14：新增 `script/ai/ai_evaluator.gd`（`class_name AIEvaluator` extends RefCounted，全 static、零 Node，幾何委派 AIQuery）。翻譯 Python `campaign/ai_evaluator.py` 全函式：`parse_card_name/card_base_stats/estimate_score_per_turn/score_income_bonus/attack_denial_bonus/target_priority_bonus/followup_kill_bonus/defensive_placement_bonus/threat_placement_bonus/incoming_damage_penalty/hand_threat_penalty/future_ass_threat_penalty/protection_bonus/reach_bonus/lethal_placement_bonus/evaluate_placement/score_move_destination/evaluate_attack`。可調常數（`scoring/*`、`threat_model/*`）全讀 `campaign_setting.json`（`core.balance.ai(...)`），Python 端硬編碼係數（0.5/1.5/100…、`WASTED_CHIP_PENALTY=18`）以字面值對齊原始碼。GameState→GameCore 對照：`gs.board_config.is_valid_position`＝`core.board.in_bounds`、`gs.board_dict[x,y].occupy`＝`core.board.occupy[Vector2i]`、`card.numbness/moving/anger`＝`is_numb()/is_moving()/is_angry()`、`card.job_and_color`＝`card.card_id`、`card.job`直接對應。`evaluate_attack` 回 `[best_score, best_target]`（GDScript 無 tuple）；`parse_card_name` 依 Godot canonical 回「色碼」（"W"）而非色名（評分只讀職業碼，等價）。驗收：新增 `tests/test_ai_evaluator.gd`（翻譯 test_ai_evaluator.py 全 40 案例；其中 2 個經 WhiteStrategy.best_placement 的案例改以純評分等價 `_white_best_placement` 驗證，避免前置 P10-3 的 Strategy 類別），`_place` 對齊 Python `place_card`（工廠預設暈眩、不強制翻轉）；`--import` 後 **1629 passed**（1564→1629，+65 全為新斷言，零回歸；洩漏 39 CanvasItem／78 ObjectDB＝與基準相同，exit 0）。
  - 驗收：翻譯 `tests/test_ai_evaluator.py` 全數通過。
- [x] **P10-3 Strategy 基底＋5 色與 boss 策略**（A §6 加成表＋faction_overrides 套用）— 2026-07-14：新增 `script/ai/ai_strategy.gd`（`class_name AIStrategy` extends RefCounted，純邏輯零 Node）：基底 `best_placement`（每張可打手牌×每個空格跑 `AIEvaluator.evaluate_placement`＋`placement_bonus`，去 `(+)` 後綴取真名）／`best_attack`（每個我方棋子跑 `AIEvaluator.evaluate_attack`＋`attack_bonus`，負分或加成後≤0 者跳過）＋內部 `Placement`／`Attack` 結果類（對齊 Python dataclass）。子策略為 inner class（`WhiteStrategy` 純基底、`RedStrategy`/`BlueStrategy`/`GreenStrategy`/`OrangeStrategy`/`BossStrategy`），逐行翻譯 Python `campaign/ai_strategies/*`，加成常數全讀 `core.balance.ai("strategy_bonuses/<color>...")`（不寫死）。`BlueStrategy.expected_tokens_from_attack` 為 static（對齊 Python 模組級函式）。工廠 `AIStrategy.create(stage, balance)` 依關卡建策略並套 `faction_overrides`（thresholds 為基準、覆寫 `attack_min_score`：white10/red12/blue13/orange12/boss13——對齊 Python AIController 建構時 setattr）。GameState→GameCore 對照：`.job_and_color`→`.card_id`、`.numbness/anger`→`is_numb()/is_angry()`、`gs.players_token/players_luck`→`core.players_token/players_luck`、`gs.how_many_token_to_draw_a_card`→`TokenEngine.threshold(core)`、`gs.neutral.on_board`→`core.neutral_pieces`、`gs.board_dict[x,y].occupy`→`core.board.occupy[Vector2i]`、`gs.score`→`core.score`。驗收：新增 `tests/test_ai_strategies.gd`（翻譯 test_campaign_strategies.py 的策略相關案例：red/blue/green/orange/boss 各色 attack_bonus 與 placement_bonus、`expected_tokens_from_attack`、暈眩/APTG 不被選為攻擊者、faction_overrides、基底 best_placement/best_attack 煙霧；AIController/boss_config/deck_builder/campaign_save 案例屬 P10-4/P10-5 未含）；`--import` 後全套件 **1679 passed**（1629→1679，+50 全為新斷言，零回歸；洩漏 39 CanvasItem／78 ObjectDB＝與基準相同，exit 0）。
  - 驗收：翻譯 `tests/test_campaign_strategies.py`。
- [x] **P10-4 AIController**（A §1 決策優先序、節奏、panic、Cyan 升級二段、MOVEO、治療、AI 黃圈事件）— 2026-07-14：新增 `script/ai/ai_controller.gd`（`class_name AIController` extends RefCounted，純邏輯零 Node；決策委派 AIStrategy/AIEvaluator/AIQuery）。`tick(core, now_ms, renderer_busy, paused)` 回傳 0/1 個 GameAction，節奏用累積 msec（turn_start 900／action 650／attack 500／busy_recheck 120，全讀 `ai_delay_ms`）。`_decide_next` 嚴格優先序：升級二段 → moving 目的地 → 啟動移動 → MOVEO → 斬殺攻擊(≥lethal 100) → 治療 → 佈署(含 Cyan toggle_upgrade 二段) → 有效攻擊(panic 門檻) → end_turn。panic：deficit>2 時門檻 max(8, base−(deficit−2)×3.5)。治療 `_best_heal` 走 `estimate_score_per_turn`＋`incoming_damage_at_position` 保命判定。關卡 buff（翻譯自 boss_config.py，讀 `stage_buffs`）：green 起始運氣 65（one-shot）、boss 起手補 4 張＋每上場單位 +1HP（記 instance_id 防重複）、orange 每 3 AI 回合 +1 移動、boss 每 5 回合 +1 治療。AI 黃圈＝`focus_position`/`has_focus`（play/attack 設、否則清）。GameState→GameCore 對照見檔頭（`pending_combat_events`→`event_sink`、`paused`→tick 參數、`mouse_selected`→`has_status("selected")`、`instance_id`→`piece.instance_id`）。未知關卡：Godot 無例外→`is_known_stage()`＋`valid` 旗標＋push_error。驗收：新增 `tests/test_ai_controller.gd`（翻譯 test_ai_controller.py 全 19 案例＋目標圈/關卡 buff 2 案例；boss/green/orange 關卡 buff 經真實 tick 驗證）；`--import` 後 **1730 passed**（1679→1730，+51 全為新斷言，零回歸；洩漏 39 CanvasItem／78 ObjectDB＝與基準相同，exit 0）。**整合待 P10-5**（單人對戰入口接場景後跑實戰）。
  - 驗收：翻譯 `tests/test_ai_controller.py`；整合：White AI 對固定腳本玩家 30 回合內不非法操作。
- [x] **P10-5 單人對戰入口＋對戰場景接 AI** — 2026-07-14：(1) **battle.gd 接 AI**（純加法、對既有本機雙人零影響）：`boot()` 新增選用參數 `ai_stage`；非空且為合法關卡（AIController.KNOWN_STAGES）時 `_setup_ai()` 建 `AIController` 控制 player2 並 `set_process(true)`。新增 `_process()`：僅在「AI 回合 且 非動畫忙碌 且 未結束」時呼叫 `_ai.tick(core, Time.get_ticks_msec(), _busy)`，把回傳的 0/1 個 GameAction 經既有 `_do` 分派（節奏/合法性全由 AIController 負責，A §1）；`_persist_draw` 依 `_ai.focus_position` 畫黃色 AI 目標圈（`_refresh_ai_focus` 僅在 focus 變動時重繪）。本機雙人（`_ai_stage` 空）`set_process(false)`，`_process` 不運作。(2) **主選單入口**：main_menu.tscn 新增「單人對戰（vs CPU）」鈕（六鈕重排 offset），＋ `AIPanel` CPU 選擇面板（白/紅/藍/綠/橙/Boss 六關卡鈕＋返回）；main_menu.gd `_on_single_player` 開面板、`_on_pick_ai(stage)` 仿 `draft._start_battle` 手動 instantiate battle 並 `boot(SP_DECK_P1, SP_DECK_P2, randi(), Balance, stage)`。**設計選擇（v1，視覺/體感待人工過目）**：用固定預設牌組（含 B/G/C/DKG 顯四資源列），AI 關卡色只決定「策略/難度」不決定牌組；玩家執先手 P1、CPU 執後手 P2；BP-vs-AI（需 AI 選秀）留待後續。驗收：新增 `tests/test_ai_battle_integration.gd`——**White AI（player2）對固定腳本玩家跑完整一局，全程 0 非法操作（dispatch 全成功）、有實際出招、對局正常推進（分勝負或 ≥30 回合）**（即 P10-4 內文延後的整合驗收）＋ focus 追蹤；`test_menu_flow` 節點樹加 SinglePlayerBtn/AIPanel/六 CPU 鈕/AIBackBtn＋面板開關切換。`--import` 後 **1749 passed**（1730→1749，+19 全為新斷言，零回歸；洩漏 39 CanvasItem／78 ObjectDB＝與基準完全相同，零新洩漏，exit 0）。**注意**：battle 場景層接線（instantiate battle 驗 `_ai`/`set_process`）刻意不寫 headless 測——每個 `BattleScene.instantiate()` 固定洩漏 ~1 CanvasItem/2 ObjectDB（未進場景樹的 harness 侷限，非產品洩漏），為守「零新洩漏」改由【人工】體感驗收確認薄膠水。
  - 目標：主選單「單人對戰」→ 選 AI 色系/難度 → 預設牌組或 BP → 對戰；AI tick 於 process、等動畫規則同 A §1（`is_busy` 即 renderer_busy）。
  - 驗收：【人工】打 White AI 一局：會出牌/攻擊/移動/治療，節奏自然（回合開始有停頓）。

## Phase 11 對局功能

- [x] **P11-1 計時器** — 2026-07-14：新增 `script/view/countdown_timer.gd`（`class_name CountdownTimer`，純邏輯零 Node、`advance(delta)` 可 headless 測；關閉或秒數≤0 時 start 不啟動、advance 恆 false＝「關閉時無作用」）。**設定**：`SettingsStore` 擴充 4 鍵（`turn_timer_on`/`turn_seconds`、`draft_timer_on`/`draft_seconds`，預設關；`save_settings` 改收 Dictionary 全量寫回，向後相容未提供鍵沿用預設）；主選單設定面板加兩循環鈕（回合計時：關/30/45/60/90；選秀計時：關/30/45/60；面板加高、六選單鈕已於 P10-5 重排）。**BP 選秀計時**：`DraftDispatcher.auto_fill_and_advance(state, pool)`（純核心）把當前可編輯玩家補到可進階最低張數（從 pool 挑首個合法卡、過既有 add_card 規則＝守同名上限）再 advance；draft.gd `_process` 倒數、逾時呼叫之（補到 done 則開戰，否則重啟下一階段倒數），階段標籤顯示 ⏳ 剩餘秒，手動 advance 亦重啟倒數。**對戰回合計時**：battle.gd `_apply_settings` 配置、`_process` 的 `_tick_turn_timer` 只在「人類回合、非動畫忙碌、未結束」倒數（AI 回合不計時、換手重啟、逾時自動 `end_turn`），`_counts_text` 顯示回合剩餘秒；`set_process(_ai != null or _turn_timer.enabled)`——計時關閉時 set_process(false)，對既有本機雙人零影響。驗收：新增 `tests/test_countdown_timer.gd`（關閉無作用/秒數0不啟動/倒數/逾時恰一次/fraction/remaining/重啟/stop）＋`test_draft` 加逾時自動補牌（三階段補到門檻並前進、done、不超同名上限、已達門檻只前進不過量補）＋`test_menu_flow` 設定 round-trip 擴 4 鍵、node tree 加兩鈕、循環鈕開/關/繞一圈。`--import` 後 **1792 passed**（1749→1792，+43 全為新斷言，零回歸；洩漏 39 CanvasItem／78 ObjectDB＝與基準完全相同，零新洩漏，exit 0）。**視覺/體感待人工過目**（設定面板兩循環鈕、draft 階段倒數逾時自動補牌手感、battle 回合倒數與逾時 end_turn、剩餘秒顯示）。
  - 目標：BP 選秀計時（每階段限時，逾時自動進入下一階段/自動補牌——draft 已預留 `timer_mode` 開關）
    ＋對戰回合計時（可選，逾時自動結束回合）；設定頁加開關與秒數。
  - 驗收：headless 測倒數與逾時行為（含關閉時無作用）；【人工】體感。
- [ ] **P11-2 對戰紀錄與回放**（待辦：回放；原 P6-3 選做轉正）
  - 目標：對局全程錄 JSONL（seed＋雙方牌組＋GameAction 流＝可完整重現）；
    回放模式：終局畫面/主選單可載入紀錄，逐步（單步/連續/倍速）重播。
  - 步驟：core 已事件化——錄 action 流＋seed 即可；回放＝把 action 流餵回新 GameCore、
    照正常事件→動畫管線播放；存 `user://replays/`。
  - 驗收：headless：錄一局→回放，最終分數/統計完全一致（決定性由 RngService seed 保證）；
    【人工】播放一局操作順手。

## Phase 12 連線版本（待辦：連線版本；取代 D8「本輪不做」→ 現在做，見 D17）

> 工程最大、對核心穩定度要求最高，故排最後。先規格後實作。

- [ ] **P12-1 連線規格文件（新增 `10_連線版本.md`）**
  - 內容：架構選型（Godot high-level multiplayer / ENet；**主機權威**：host 跑 GameCore，
    client 送 GameAction、收 GameEvent——D1 事件模型天生適配）；房間/大廳流程（先區網直連＋IP，
    再評估中繼）；BP 連線化（DraftAction 同步＋對方選牌遮蔽）；斷線重連與觀戰是否納入首版；
    **不與 Python 版互通**（跨引擎自製 TCP 協定不划算，D17）。
  - 驗收：文件經使用者確認後，把實作拆為 P12-2…P12-n 追加到本清單。
- [ ] **P12-2+ 依規格實作**（佔位，P12-1 確認後展開）

## Phase 13 爬塔模式（維持擱置，D11）

> 規格已備於 `07_爬塔模式與新功能.md`（T）。等 Phase 8–12 交付後再解凍；
> 屆時承擔教學功能（取代已移除的戰役模式，D10）。

---

## 行為疑義登記表（執行中發現 Python 行為與規格不符時記錄於此）

| 日期 | 發現 | 處理 |
|---|---|---|
| （範例）2026-07-10 | Brown TANK 設定鍵不存在（B1） | 按 01 §12 裁定實作 |
| 2026-07-10 | Godot `_spawn_card`（P1-2）順序為 append→occupy→deploy，與 Python `factory.spawn_card`（deploy→occupy→append）不同；deploy 時本子是否在場/格是否佔用有別，影響 SPB 佈署自我計數（多打 1 次）。 | 修正 Godot 為 deploy→occupy→append 對齊 Python（Python 為準）。既有 White/Red 無 deploy 能力，不受影響。 |
| 2026-07-11（P7-6） | end_game 圖表框 ColorRect（不透明）掛在 `_hud`(CanvasLayer)，渲染於折線所在 `_chart_layer`(Node2D) 之上，遮蔽折線（P2-5 既有 z-order 問題，非 Python 相關）。 | **已於 P8-6（2026-07-13）修正**：ChartFrame 移出 HUD 到基礎場景、樹序置於 ChartLayer 之前，渲染於折線之下。 |

## 給 Python 端的回報清單（不由本專案修改）

1. `card_setting.json` Green APT 的 `luck_increase` 未被程式使用。
2. 建議把 05 §2.3 表列的硬編碼常數移入 `config/setting.json`，兩引擎即可全量共用。
