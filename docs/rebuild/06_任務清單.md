# 06 任務清單（主執行文件）

> 執行規則：**由上往下、一次一個任務**。每個任務格式：目標 → 步驟 → 驗收。
> 完成後把 `[ ]` 改 `[x]` 並在行尾加一句備註（日期＋特殊處理）。
> 規格出處縮寫：R=01規則、C=02卡牌、A=03AI、D=04架構、J=05JSON、T=07爬塔。
> 驗收指令統一為（本機 Godot 路徑已確認）：
> `& "d:\Code\Games\Godot_v4.7-stable_win64.exe" --headless --path "D:\Code\Games\AfternoonBrainStorm\AfternoonBrainstorming_godot" -s tests/run_tests.gd`
> （Godot 4.7 stable；PowerShell 用 `&` 呼叫。換機器時更新此路徑。）

---

## Phase 0 地基（資料與測試設施）

- [x] **P0-1 建立 v2 目錄骨架與測試 runner** — 2026-07-10：目錄骨架（.gitkeep 佔位）＋ `tests/run_tests.gd`（SceneTree，內嵌 TestContext 斷言工具 eq/ok/fail）＋ `test_smoke.gd`。驗收：正常 `1 passed` exit 0、故意錯 exit 1，皆通過（Godot 4.7）。
  - 步驟：建 `script_v2/`、`scenes_v2/`、`data/balance/`、`tests/`、`tools/` 目錄（D §2）。
    寫 `tests/run_tests.gd`：繼承 SceneTree，掃 `tests/` 下 `test_*.gd`，每檔實例化呼叫 `run(t)`，
    `t` 為斷言工具（`t.eq(a,b,msg)`、`t.ok(cond,msg)`），統計結果，失敗 `quit(1)`。
    寫一個自證測試 `test_smoke.gd`（1+1=2）。
  - 驗收：headless 跑 runner 輸出 `1 passed` 且 exit 0；故意寫錯時 exit 1。
- [x] **P0-2 同步腳本 sync_balance** — 2026-07-10：`tools/sync_balance.ps1`（UTF-8 BOM 存檔以相容 PS5.1）複製 5 個 config JSON → `data/balance/`，產生 `_meta.json`（version 4.3.0.0、hash 6263139bb55e）。`--check` 未變時 exit 0。data/balance 入版控。
  - 步驟：照 J §2.1 寫 `tools/sync_balance.ps1`（來源路徑預設 `..\AfternoonBrainstorming\FOS brainstorming\config`，
    參數可覆蓋）。執行一次，確認 `data/balance/` 出現 5 個 JSON + `_meta.json`。
  - 驗收：`_meta.json` 含 source_version=`4.3.0.0` 與 12 碼 hash；`--check` 模式在檔案未變時 exit 0。
- [x] **P0-3 BalanceDB autoload** — 2026-07-10：`script_v2/data/balance_db.gd`（於 _init 載入，autoload `Balance` 已註冊 project.godot）。card_id 由「職業碼+色碼」資料驅動組合，避免解析歧義。schema 驗證缺鍵 push_error。驗收 test_balance_db.gd 全過（套件 86 passed）。
  - 步驟：照 J §2.2 實作 `script_v2/data/balance_db.gd`，註冊 autoload `Balance`。
    schema 驗證缺鍵時 push_error。提供 stats/param/attack_types/color_rgb/ai/data_version。
  - 驗收：新測試 `test_balance_db.gd`：`stats("ADCW").health==5`、`stats("TANKBR").health==20`、
    `param("SPW","extra_score",0)==1`、`attack_types("HF")=="small_cross small_x"`、
    `ai("thresholds/attack_min_score")==15.0`、10 色 × 各職業全部可查（Purple 只查 4 張）。
- [x] **P0-4 card_text.json（顯示文字）** — 2026-07-10：`tools/gen_card_text.gd`（一次性工具，清 description.json 尾逗號）產生 `data/card_text.json`（82 卡）。白/紅/綠/藍/橘沿用 description.json 名稱與描述（描述空則退回 hint）；缺色名稱依 02 §對照表（蒼鬱/靛青/緋紫/褐鏽/魅紫＋職業）；hint 一律為 Python card_hints。BalanceDB 增 `text(card_id)`。驗收：全卡 name 非空、hint 抽查 5 張對齊（套件 169 passed）。
  - 步驟：建 `data/card_text.json`：每個 card_id 一筆 `{name, description, hint}`。
    來源：合併現有 `setting/description.json`（白/紅/綠/藍/橘可沿用其中文名與描述風格）＋
    Python `card_hints.json`（攻擊模式與能力說明）。缺的色（DarkGreen/Cyan/Fuchsia/Brown/Purple）
    先以 card_hints 文字為 description，名稱按 C 文末對照表命名。BalanceDB 增加 `text(card_id)`。
  - 驗收：測試遍歷 BalanceDB 全卡 id 都取得非空 name；hint 與 Python card_hints 一致（抽查 5 張）。

## Phase 1 規則核心（GameCore，純邏輯無畫面）

- [x] **P1-1 基礎狀態與回合引擎** — 2026-07-10：新增 script_v2/core 十檔（game_config/board_state/piece_state/player_state/rng_service/game_event[class GameEventV2 避開舊碼衝突]/statistics/game_action/action_result/turn_engine/game_core）。settle 用「非 numbness=1 分」基本版；狀態統一收進 statuses 字典。BalanceDB 增 job_of/color_code_of。驗收 test_core_turns.gd 六項全過（套件 195 passed）。**注意**：新增 class_name 檔後，headless 需先跑一次 `--import` 重建 global class cache，runner 才認得（`.godot/` 為 gitignore 快取，換機器須重跑 import）。
  - 步驟：實作 `game_config.gd`（J §2.3 常數表）、`board_state.gd`（4x4 occupy）、`piece_state.gd`
    （D §3 欄位，statuses 字典）、`player_state.gd`（deck/hand/draw/discard/revealed_deck、
    draw_card 含洗牌重抽規則 R §3）、`rng_service.gd`（seed 注入）、`game_event.gd`（D §4 全事件）、
    `statistics.gd`（R §11 StatType + score_history）、`game_action.gd`、
    `turn_engine.gd` + `game_core.gd`：setup/initialize（R §3 開局）、turn_start、end_turn（settle 計分順序）、
    logic_step（回收與 card_to_draw）、勝負判定。settle 先用「非 numbness=1 分」基本版（能力掛勾 P1-3 補）。
  - 驗收：`test_core_turns.gd`：開局手牌 3/3、p1 攻擊次數 1/p2 為 0；end_turn 後輪替正確；
    numbness 棋子首次 settle 得 0 分且之後解除；分數 ±10 觸發勝負；skip_turn_draw 生效；
    摸完牌庫會洗棄牌堆再抽。
- [x] **P1-2 行動分派與移動** — 2026-07-10：game_core 新增 `dispatch()` 與行動處理（_play_card/_heal_card/_move_card/_spawn_card/_spawn_cube/_toggle_upgrade；attack 僅前置守衛，combat 留 P1-3）。兩段式移動用 `selected` status 模型化 mouse_selected，失敗不退點（B5）。PieceState 增 `movable`；BalanceDB 增 CUBE 無色別名（沿用 White/CUBE 數值）。驗收 test_core_actions.gd 全過（套件 233 passed）。
  - 步驟：`dispatch()` 支援 R §4 全部 action（play_card 含 HEAL/MOVE/MOVEO/CUBES 分支與棋子生成佔格、
    heal 溢出轉盾、spawn_cube、toggle_upgrade 只改名）。移動兩段式照 R §4（含失敗不退點 B5）。
  - 驗收：`test_core_actions.gd`：非當前玩家行動被拒；攻擊次數不足被拒；
    移動 8 鄰限制/佔格失敗/點數消耗；HEAL +6 與溢出 //2 轉盾；CUBES 給 2 顆；MOVEO 回合結束消失。
- [x] **P1-3 傷害管線與攻擊佇列 + 能力系統骨架** — 2026-07-10：新增 `script_v2/core/combat.gd`（CombatV2：detection R§5、十步 damage_calculate R§6、pending_attacks drain R§7、動畫延遲游標 R§10）與能力系統 v2（`ability/`：TriggerV2 全表、AbilityV2/AbilityEffectV2/AbilityContextV2、AbilityComponentV2 含 silence/grant/dispatch_mod/any_true/collect_field、AbilityRegistryV2、cards/white.gd 註冊 White 8 張骨架）。game_core 接上全鉤子（ON_DEPLOY/REFRESH/SETTLE/UPDATE/DIE、move_broadcast、after_movement、combat 各步）；settle 改走 ON_SETTLE。component.source 用 WeakRef 避免循環洩漏。drain_events 重置 `_attack_anim_cursor`。驗收 test_combat_pipeline.gd＋test_ability_system.gd 全過（套件 266 passed，exit 0，無洩漏）。**注意**：新增 class_name 後 headless 須先 `--import` 重建 global class cache。
  - 步驟：`combat.gd` 完整實作 R §6 十步管線（含護盾三分支、pending_death、can_be_killed）、
    R §5 detection（nearest/farthest 平手 rng 隨機）、R §7 pending_attacks drain、
    animation delay 游標（R §10）。同時實作 D §5 能力系統骨架：Trigger enum 全表、Ability、
    AbilityEffect、AbilityContext、AbilityComponentV2（含 **silence/grant API**）、ability_registry
    （先只註冊 White 8 張）、ON_SETTLE 計分掛勾（D §5.4）、status API（D §6）。
  - 驗收：`test_combat_pipeline.gd`：護盾恰好抵銷/溢出/無盾三情境的統計數字正確；
    擊殺觸發 killed/been_killed 順序；death 事件 delay 正確；攻擊 numbness 者無效且不耗次數；
    追加攻擊經佇列不遞迴（用假能力 enqueue 驗證）；
    `test_ability_system.gd`：silence 後觸發不執行、grant 後新能力生效、clear 恢復。
- [x] **P1-4 White 全卡 + 特殊卡** — 2026-07-10：`ability/cards/white.gd` 填入 APW 麻痺（ON_ABILITY_HIT）、APTW 最近友方+自身 +ATK 護盾（ON_ABILITY_HIT，用 CombatV2.detection nearest 撈友方）、SPW ON_SETTLE +extra_score（numb 時維持 0）；ADCW/TANKW/HFW/LFW/ASSW 純數值（ASS 先攻由 make 處理）。特殊卡 CUBE/CUBES/HEAL/MOVE/MOVEO 行為 P1-2 已在 game_core 完成、無 native 能力（CUBE 恆 0 分結構上由「neutral 不參與 settle」保證）。驗收 test_card_white.gd 8 項全過（套件 274 passed，exit 0）。
  - 步驟：`ability/cards/white.gd` 組裝 C White 8 張；CUBE/CUBES/HEAL/MOVE/MOVEO 完成；
    APW 麻痺、APTW 護盾、SPW 2 分。
  - 驗收：翻譯 Python `tests/test_card_white.py` 全部案例通過。
- [x] **P1-5 Red 全卡** — 2026-07-10：`ability/cards/red.gd`（RedCardsV2）填入全 8 卡。共用輔助 `_allied_sprs`（我方場上 SPR，鏡射所有紅色增益）＋ `_nearest_allies`（CombatV2.detection nearest 排除自己）。ADCR/LFR/APTR 造成傷害後滾雪球（ON_ABILITY_HIT）；APR 麻痺+偷取 100% ATK；TANKR 被攻擊後最近友方+SPR 護盾（ON_BEEN_ATTACKED，兩迴圈獨立）；ASSR 斬殺後（ON_KILLED）；HFR 三能力：ON_ABILITY_HIT 自損+滾雪球+怒氣、CAN_BE_KILLED 怒氣不死身、ON_SETTLE 怒氣得 0 清怒氣（clear_numbness=True 語意，numbness 由 settle_piece 後清）。SPR 無能力純承接。驗收 test_card_red.gd 35 項全過（套件 309 passed，exit 0，無洩漏）。**注意**：新增 class_name RedCardsV2 後 headless 須先 `--import` 重建 global class cache。
  - 驗收：翻譯 `test_card_red.py` 全數通過。
- [x] **P1-6 Blue 全卡 + TokenEngine** — 2026-07-10：新增 `script_v2/core/token_engine.gd`（TokenEngineV2：add/got_token/gain；門檻讀 setting.json how_many_token_to_draw_a_card=3；got_token 為「單次 if」非 while——每次至多換 1 抽）＋ `ability/cards/blue.gd`（BlueCardsV2 全 8 卡：ADCB 斬殺 got_token 一次＋token_draw 麻痺解除/否則排追加攻擊；APB 麻痺+token_gain 次 got_token；TANKB/LFB/ASSB 加 token；HFB extra_damage=token 數；APTB extra_damage=armor//divisor＋造成傷害獲等量 token＋after_token 加盾；SPB 佈署爆發+清佇列）。**修正**：`game_core._spawn_card` 改為「deploy → occupy → append」以對齊 Python `factory.spawn_card`（deploy 時本子尚未在場、格未佔），影響 SPB 自我計數（記於行為疑義登記表）。ON_TOKEN_GAINED/ON_TOKEN_DRAW 觸發點沿用 P1-3 既有 enum。驗收 test_card_blue.gd 全過（套件 333 passed，exit 0）。**注意**：新增 class_name 後 headless 須先 `--import` 重建 global class cache。
  - 驗收：翻譯 `test_card_blue.py`；追加：token=2 時 APB 攻擊產生 2 次 got_token 只換 1 抽 +剩 1 球。
- [x] **P1-7 Green 全卡 + LuckEngine** — 2026-07-10：`ability/cards/green.gd`（GreenCardsV2 全 9 註冊：8 卡 + LUCKYBLOCK）。共用 static `lucky_effects(core,target,ap,ap_target,tank)` 完整翻譯 Python：好運（運氣+1，隨機 護盾+4/ATK×2/排攻擊/moving/四斜角生 LUCKYBLOCK）、壞運（運氣-1，隨機 護盾歸0/麻痺/HP半/ATK半/HP-2）；`not ap_target and roll≤運氣` 判定、`ap_target` 恆走壞運（B2）、`tank` 好運跳過、`ap` 自身壞運不罰且好運不生方塊。卡：ADCG 同行列空格擲生成、APG 麻痺+目標壞運+自身好運、TANKG 被攻擊反擊壞運、HFG 打 LUCKYBLOCK 加運+再生、LFG 斬 LUCKYBLOCK 追打最近敵方(玩家)+機率加攻擊次數、ASSG 斬殺自方+5/敵-5、APTG 禁攻(ATTACK_OVERRIDE)+回合開始小十字生成、SPG 佈署加運氣+每超10點生方塊(排除自身格)。LUCKYBLOCK been_killed → 對攻擊者 lucky_effects + 攻擊者方 APTG +1 盾。新增 ATTACK_OVERRIDE 佈線（AbilityComponentV2.dispatch_attack_override + CombatV2.attack 前置攔截，供 APTG 禁攻、P1-8 ADCO 覆寫）。BalanceDB 增 LUCKYBLOCK 無色別名（沿用 Green/LUCKYBLOCK 數值）。驗收 test_card_green.gd 20 項全過（含固定運氣值斷言好運/壞運/三旗標分支；套件 353 passed，exit 0）。**注意**：新增 class_name GreenCardsV2 後 headless 須先 `--import` 重建 global class cache。
  - 驗收：翻譯 `test_card_green.py`；用固定 seed 斷言好運/壞運分支。
- [x] **P1-8 Orange 全卡** — 2026-07-10：`ability/cards/orange.gd`（OrangeCardsV2 全 8 卡）。橘色主題＝機動。共用 `AttackThenMoveEffect`（ATTACK_OVERRIDE：照常 launch_attack，成功則 set_moving——供 ADCO/HFO/LFO）＋ `AddMoveoEffect`（給手牌一張 MOVEO——APO on_refresh、TANKO been_attacked）。ADCO 移動後再 launch_attack；HFO 移動後 extra_damage+1＋怒氣（damage_bonus 沿用 base pipeline 自動加 extra_damage，Python Hf.damage_bonus 與預設相同故不設 MOD）、ON_SETTLE 清 extra_damage/怒氣；LFO 移動後對最近敵方（僅對手棋子，不含中立，get_opponent_cards 語意）打 ATK（ability=True）；ASSO 移動後怒氣、ON_KILLED set_moving＋怒氣時攻擊次數+1 清怒氣、ON_SETTLE 清怒氣；APTO 移動後 armor+1→armor//2 轉 damage（取餘）＋ON_MOVE_BROADCAST 我方移動給移動者與自身各+1 盾；SPO ON_MOVE_BROADCAST 我方移動對最遠敵方（含中立）打 move_strike_damage。驗收 test_card_orange.gd 24 項全過（套件 377 passed，exit 0）。**注意**：新增 class_name OrangeCardsV2 後 headless 須先 `--import` 重建 global class cache。
  - 驗收：翻譯 `test_card_orange.py`。
- [x] **P1-9 DarkGreen 全卡 + TotemEngine** — 2026-07-10：新增 `script_v2/core/totem_engine.gd`（TotemEngineV2：`engrave(core,owner,times)` 對 players_totem 加 `times × coefficient^(場上 SPDKG 數)`，係數讀 DarkGreen/SP/engraved_totem_coefficient=2，整數次方）＋ `ability/cards/dark_green.gd`（DarkGreenCardsV2 全 8 卡）。ADCDKG/ASSDKG ON_UPDATE 設 `extra_damage=圖騰//divisor`（4/8；damage_bonus 沿用 base pipeline 自動加）；APDKG 麻痺+刻印5；TANKDKG 被攻擊刻印3；HFDKG ON_ABILITY_HIT 自療1、ON_UPDATE HP≤4 時 extra_damage=2、ON_REFRESH 自傷2+刻印2；LFDKG ON_DEPLOY 對 small_cross 敵方(含中立)打圖騰//4、ON_REFRESH 自傷2、ON_ABILITY_HIT 刻印1；ASSDKG ON_KILLED 自身HP歸0(自殺)+刻印7；APTDKG ON_UPDATE extra_damage=圖騰//2、MOD_DAMAGE_BONUS 副作用刻印 armor//2(回傳原值不重複加)、ON_AFTER_DAMAGE armor+=value//2；SPDKG 無能力(僅作倍率來源)。ability_registry 併入 DarkGreen。**注意**：自傷（attacker==victim）的 damage_calculate 會把自己壓入 hit_cards 形成自我循環參考（Godot refcount 無法回收），HFDKG/LFDKG on_refresh 自傷後 `hit_cards.clear()` 修正（Python 靠 GC 不受影響）。驗收 test_card_dark_green.gd 16 項全過（套件 393 passed，exit 0，無洩漏）。**注意**：新增 class_name 後 headless 須先 `--import` 重建 global class cache。
  - 驗收：翻譯 `test_card_dark_green.py`。
- [x] **P1-10 Cyan 全卡 + CoinEngine** — 2026-07-10：新增 `script_v2/core/coin_engine.gd`（CoinEngineV2：`gain(core,owner,value)` 累積封頂 COIN_MAX=50，對齊 Python get_coins 硬編碼上限）＋ `ability/cards/cyan.gd`（CyanCardsV2 全 8 卡）。升級系統：PieceState.make 增 `upgrade` 參數（升級版改用 upgrade_health/upgrade_damage）；GameCore._spawn_card 傳入 upgrade 並在 create→（新增）`_cyan_price_check`→deploy→occupy→append，付不起則生成失敗不扣費不佔格；折扣＝cost − cost_reduction × 我方升級版 SPC 張數。升級差異於效果內以 `ctx.source.upgrade` 執行期分流。卡：ADCC 造傷+2$／升級 ATTACK_OVERRIDE 二連擊；APC 麻痺+3$／ON_DEPLOY 基礎無視麻痺攻 2 次、升級隨機指派我方 nearest/farthest 攻擊者代打 APC 最近目標；TANKC 被攻擊+2$／升級 ON_DEPLOY 設怒氣→BLOCK_DAMAGE 格擋首擊（整段管線取消、清怒氣）；HFC 造傷+2$／升級 ON_BEEN_KILLED 進怒氣+ATK+2、CAN_BE_KILLED 怒氣不死、ON_SETTLE count 機制（怒氣turn得0並清，靠 PieceState.counters 存 count）；LFC 造傷+2$／升級 ON_REFRESH 隨機五模式；ASSC 斬殺+6$／升級 ON_DEPLOY 怒氣+extra_damage=2、MOD_DAMAGE_BONUS 用後歸零（extra_damage 由管線自動加，effect 僅重置不重複加）；APTC 回合開始+4$／升級 MOD_DAMAGE_REDUCE 金幣//5 上限3；SPC 佈署+10$／升級折扣走 price_check。PieceState 增泛用 `counters` 暫存字典。ability_registry 併入 Cyan。驗收 test_card_cyan.gd 40 項全過（含升級數值、二連擊、price_check 足夠/不足/SPC折扣、金幣上限；套件 433 passed，exit 0）。**注意**：新增 class_name 後 headless 須先 `--import` 重建 global class cache。
  - 驗收：翻譯 `test_card_cyan.py` + `test_ai_cyan_upgrade.py` 中屬於卡牌行為的部分（AI 決策/`_cyan_upgrade_price` 屬 Phase 3，此處只做卡牌與 price_check 核心）。
- [ ] **P1-11 Fuchsia 全卡 + ShadowEngine**（對稱位生成、鏡像攻擊用本體 ATK、LFF 雙重命中補刀、
  APTF priority20 攔截、TANKF 佔格、ASSF 連環生成、SPF 給最遠紫紅卡掛鏡像）
  - 驗收：翻譯 `test_card_fuchsia.py`。
- [ ] **P1-12 Brown 全卡 + BrownEngine（＝內建沉默的第一個使用者）**（B1 裁定：TANKBR 用
  turn_start_health_loss=4；SPBR 沉默全隊 Brown；APTBR 給全敵護盾）
  - 驗收：Python 無 brown 測試 → 自寫：SPBR 攻擊後 ADCBR 攻擊不再自麻、場上 SPBR 死亡後下回合恢復、
    TANKBR 兩回合沒被打掉 8 血、HFBR 一次攻擊耗 2 次數、LFBR 擊殺送對面 2 分、ASSBR 擊殺後我方下回合不抽。
- [ ] **P1-13 Purple 4 卡**（APP 驅散、TANKP 反移動、HFP 攻擊次數、ASSP 抽牌公式含上限）
  - 驗收：翻譯 `test_card_purple.py`。
- [ ] **P1-14 核心整合回歸**
  - 步驟：寫 3 個「整局腳本測試」：白鏡像對局照腳本打 20 步斷言中間狀態；
    藍 vs 綠含 token/luck 交互；固定 seed 全隨機打到分出勝負（煙霧測試，斷言不崩潰、統計非空）。
  - 驗收：全測試套件綠燈；`game_core` 全程無任何 Node 依賴（grep 檢查 `get_tree|load("res://scenes`）。

## Phase 2 表現層（本機對戰可玩）

- [ ] **P2-1 PieceView + 佔位美術**
  - 步驟：照 D §7.1 做 `piece_view.tscn`；PlaceholderShape 用 C 附錄頂點表 ×格寬、
    Polygon2D 填色（擁有者以外框色 p1 紅 / p2 藍區分，本體填職業色）、中央 Label 顯卡名縮寫、
    角落顯 HP/ATK/armor（extra_damage 以 `+n` 顯示）、StatusIcons 對應 numbness/anger/moving
    （沿用 `img/UI/buff/` 現有圖示）。
  - 驗收：demo 場景擺出 10 色 × 8 職業 + CUBE/LUCKYBLOCK/SHADOW 一覽，肉眼可辨識（截圖存 docs/）。
- [ ] **P2-2 AnimationSet 與 CombatScheduler**
  - 步驟：照 D §7.2/7.3 實作 fallback 動畫（撲擊 0.32s 節奏對齊 R §10）、飄字、死亡淡出、
    移動 tween、STATUS/RESOURCE 事件反映到 UI；做 ADC 「準備（後拉）→ 射出三角形箭矢投射物 → 命中閃光」
    示範以驗證插槽；提供動畫開關（瞬時模式）。
  - 驗收：手動腳本場景播放一次多目標攻擊：三個目標依 0.32s 間隔逐一中彈；關動畫後同結果瞬時呈現。
- [ ] **P2-3 對戰場景 battle.tscn（本機雙人）**
  - 步驟：棋盤繪製與座標換算、選取/出牌/攻擊/移動（兩段式）/治療/放方塊/升級切換/結束回合輸入流，
    全部行動經 `GameCore.dispatch`；HUD 照 D §7.4 清單；攻擊範圍預覽（含鏡像）；card_hints 提示浮窗與開關。
  - 驗收：人工對局檢查表（做成 `docs/rebuild/驗收_對戰.md` 勾選）：R §4 每個行動至少操作一次、
    numbness 規則體感正確、分數與勝負畫面正確。
- [ ] **P2-4 選秀 BP 場景**
  - 步驟：照 R §9 實作三階段 BP：展示館分頁（每色一頁，Purple 只 4 張）、魔法列、
    加/退卡、張數上限提示、階段前進、完成後帶牌組進 battle。
  - 驗收：完整走一次 BP → 對戰；非法操作（超過 2 張同名等）被拒且有提示。
- [ ] **P2-5 主選單 + 設定 + 終局統計**
  - 步驟：主選單（本機對戰/戰役[佔位]/爬塔[佔位]/設定/離開，角落顯示 balance 版本）；
    設定頁（提示開關、動畫開關，存 `user://settings.json`）；
    終局畫面：勝者 + 每回合分數折線 + 主要統計長條（KILLED/DAMAGE_DEALT/SCORED 前幾名），
    畫圖用 `draw_rect/draw_line` 自繪即可。
  - 驗收：整條流程 選單→BP→對戰→終局→回選單 不中斷；設定重啟後保留。

## Phase 3 CPU AI

- [ ] **P3-1 ai_query 移植**（A §2 全函式，純 static）
  - 驗收：翻譯 `tests/test_ai_query.py` 全數通過。
- [ ] **P3-2 ai_evaluator 移植**（A §3–§5 全公式，常數走 BalanceDB）
  - 驗收：翻譯 `tests/test_ai_evaluator.py` 全數通過。
- [ ] **P3-3 Strategy 基底 + 5 色與 boss 策略**（A §6 加成表 + faction_overrides 套用）
  - 驗收：翻譯 `tests/test_campaign_strategies.py`。
- [ ] **P3-4 AIController**（A §1 決策優先序、節奏、panic、Cyan 升級二段、MOVEO、治療、AI 黃圈事件）
  - 驗收：翻譯 `tests/test_ai_controller.py`；整合測試：White AI 對固定腳本玩家 30 回合內不非法操作。
- [ ] **P3-5 對戰場景接 AI**（mode=campaign：玩家=player1，AI tick 於 process，等動畫規則同 A §1）
  - 驗收：人工打 White 關：AI 會出牌、攻擊、移動、治療，節奏自然（回合開始有停頓）。

## Phase 4 戰役模式

- [ ] **P4-1 戰役資料與存檔**（ai_decks 六關牌組寫入 `data/balance/`？——**不**，牌組屬 campaign_setting 範疇但
  Python 寫死於 `ai_decks.py` → 移植為 `script_v2/modes/campaign/campaign_decks.gd` 常數並註記出處；
  存檔 `user://campaign_progress.json`：cleared 關卡清單）
  - 驗收：單元測試：解鎖顏色計算對照 A §6（含 boss 通關全解鎖）。
- [ ] **P4-2 關卡選單 + 戰前資訊 + 戰役組牌**（六關列表含難度標籤與 stage buff 說明文字；
  組牌頁沿用 BP 展示館但過濾解鎖色；預設牌組=White 套牌）
  - 驗收：通關 white 後 red 關組牌頁出現紅色頁籤；boss 前只有 W/R/B/G/O。
- [ ] **P4-3 stage buffs 生效**（A §6：初始運氣/起手牌/單位+1HP/週期移動/治療，AI 單位上場補 HP 的
  maintain 邏輯）
  - 驗收：boss 關 AI 單位 HP 比表定 +1；green 關 AI 初始運氣 65（測試直讀 core 狀態）。
- [ ] **P4-4 勝利記錄與流程閉環**（勝→記 cleared→回關卡選單；敗→重試選項）
  - 驗收：全六關可依序解鎖通關（可用 debug 勝利鍵輔助人工驗收，須留開關）。

## Phase 5 爬塔模式（endless 移植，規格見 T）

- [ ] **P5-1 RunState + 樓層生成**（T §2–§4：seed 決定性 RNG、floor_spec、牌組分層生成、AI 效果縮放）
  - 驗收：翻譯 T §8 的決定性測試（同 seed 同樓層 → 同 spec）。
- [ ] **P5-2 效果合併與戰鬥建構**（T §5 merged_player_effects/battle_effects；EndlessAIController：
  雙方 buff 維護、消耗品、玩家 effects；戰後 temp_deck 消耗結算 p1_consumption）
  - 驗收：單元測試：遺物+詛咒+消耗品疊加結果、mutation both_ 前綴、win_threshold 覆寫。
- [ ] **P5-3 爬塔畫面組**（選單/樓層預覽/獎勵三選一/商店/事件/失敗結算；存檔 `user://endless_save.json`
  相容 T §7 schema）
  - 驗收：完整跑 1–6 層（含一次商店）；中途退出再進度續跑。
- [ ] **P5-4 GameCore.clone + Claude 策略**（A §7；clone 深拷貝正確性最重要）
  - 驗收：clone 後改副本不影響本體（全欄位抽查）；10 層 boss 用 claude 策略能在合理時間（<2s/步）出手。
- [ ] **P5-5 新功能挑選實作**（從 T §9 建議清單挑 1–2 個低成本項，與使用者確認後執行）

## Phase 6 收尾

- [ ] **P6-1 刪除舊碼**（`script/`、`scenes/cards|component|Resource...`、`img` 內確認無引用的資源；
  `script_v2/scenes_v2` 轉正為 `script/scenes`；project.godot autoload 清理）
  - 驗收：全專案無 parse error；全測試綠；主流程人工走一遍。
- [ ] **P6-2 README 重寫**（架構圖、平衡同步工作流、測試指令、佔位美術替換指南）
- [ ] **P6-3（選做）JSONL 對戰紀錄與重播**（core 已事件化，錄 action 流即可重播）

---

## 行為疑義登記表（執行中發現 Python 行為與規格不符時記錄於此）

| 日期 | 發現 | 處理 |
|---|---|---|
| （範例）2026-07-10 | Brown TANK 設定鍵不存在（B1） | 按 01 §12 裁定實作 |
| 2026-07-10 | Godot `_spawn_card`（P1-2）順序為 append→occupy→deploy，與 Python `factory.spawn_card`（deploy→occupy→append）不同；deploy 時本子是否在場/格是否佔用有別，影響 SPB 佈署自我計數（多打 1 次）。 | 修正 Godot 為 deploy→occupy→append 對齊 Python（Python 為準）。既有 White/Red 無 deploy 能力，不受影響。 |

## 給 Python 端的回報清單（不由本專案修改）

1. `cards/card_brown.py:93` `self_health_loss` → 應為 `turn_start_health_loss`（現況會 KeyError）。
2. Green APG 對目標 `AP_target=True` 使目標必吃壞運（若非設計意圖需修 `lucky_effects` 的第一個條件）。
3. `card_setting.json` Green APT 的 `luck_increase` 未被程式使用。
4. 建議把 05 §2.3 表列的硬編碼常數移入 `config/setting.json`，兩引擎即可全量共用。
