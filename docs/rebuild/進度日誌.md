# 重構進度日誌

> 每次自動執行在最上方追加一段。格式：日期時間 / 完成任務 / 驗收結果 / 問題與待決。

---

## 2026-07-10（自動執行 · P1-13 完成）

**分支**：`godot-rebuild`（承接 P1-12，工作區乾淨；基線 484 passed）。

**本次完成任務**
- **P1-13 Purple 4 卡**：新增 `script_v2/core/ability/cards/purple.gd`（`PurpleCardsV2`），registry 併入。
  紫色僅實作 4 張（APP/TANKP/HFP/ASSP），對齊 Python `card_purple.py` 只 register 這 4 張
  （ADC/LF/APT/SP 未註冊、balance 為 0、選秀不出現）。
  - **APP（驅散）**：`ON_DEPLOY` 對最近敵方（對手場上 health>0）驅散——`armor=0`、`damage=original_damage`（不麻痺）；
    `ON_ABILITY_HIT` 攻擊命中時麻痺 + 驅散。
  - **TANKP（反移動）**：`ON_MOVE_BROADCAST` 若移動者為敵方 → 對其 `damage_calculate(move_strike_damage=2)`（清 hit_cards）。
  - **HFP（攻擊次數）**：`ON_REFRESH` 以自身 attack_types 對對手場上偵測敵數，`敵數 // 3` 加到 `number_of_attacks`。
  - **ASSP（爆抽含上限）**：`ON_KILLED` 抽 `min(敵方場上數 − 我方場上數 − unit_gap(2), 上限 12)` 張（>0 才抽）；
    因 `ON_KILLED` 早於回收，被殺者仍計入敵方數（對齊 Python）。

**驗收**：`tests/test_card_purple.gd` 13 項全過（翻譯自 Python `test_card_purple.py` 全部案例，
追加 APP 佈署驅散與「填滿棋盤敵15我1 → 抽牌達上限 12」邊界）。全套件 **497 passed，exit 0，無洩漏**。

**問題 / 待決**：無。Phase 1 卡牌全部完成；**只剩 P1-14 核心整合回歸**（整局腳本測試 + `game_core` 無 Node 依賴 grep 檢查），
之後即可進入 Phase 2 表現層。行為疑義登記表本次無新增。

---

## 2026-07-10（自動執行 · P1-12 完成）

**分支**：`godot-rebuild`（承接 P1-11，工作區乾淨；基線 458 passed）。

**本次完成任務**
- **P1-12 Brown 全卡 + BrownEngine（內建沉默第一個使用者）**：新增 `script_v2/core/ability/cards/brown.gd`（`BrownCardsV2` 全 8 卡），並於 `ability_registry.gd` 併入。
  - **沉默模型（本任務核心）**：受 Python `effects_disabled` 影響的能力一律掛 `Tag.FACTION`；
    SPBR 攻擊命中時（`ON_ABILITY_HIT`，本身不掛 FACTION 故不受影響）自亮怒氣，並對我方其他 Brown 卡
    呼叫 `silence_tag(FACTION)`。每張 Brown 卡另掛一個「還原」能力（`ON_REFRESH`，`Tag.PASSIVE`、
    priority −100，不受沉默）——回合開始若我方場上已無 SPBR 則 `clear_silence()`，對齊 Python
    `BrownCard.on_refresh`（沉默隨 SPBR 存活而持續、SPBR 死亡後下回合恢復）。
  - **各卡**：ADCBR `ATTACK_OVERRIDE`(FACTION) 攻擊後自麻（沉默→退回普通攻擊、不自麻）；
    APBR 造成傷害後對手抽 1；TANKBR `ON_BEEN_ATTACKED` 記旗標 + `ON_REFRESH` 未被攻擊自傷 4
    （B1 裁定 `turn_start_health_loss`；自傷經傷害管線、清 `hit_cards` 斷自我循環參考、旗標於自傷後重置以對齊 Python）；
    HFBR `ATTACK_OVERRIDE`(不掛 FACTION、內部判沉默) 設 `attack_uses` 沉默=1／否則=2；
    LFBR 斬殺→對面得 2 分；ASSBR 斬殺→`skip_turn_draw`；APTBR `ON_DEPLOY`(PASSIVE，不受 effects_off)
    全敵 +2 盾 + `ON_ABILITY_HIT`(FACTION) 最近友方 +1/+1、若也是 Brown 巨人再 +1/+1；SPBR 見上。
  - **PieceState.make**：新增讀取 balance `attack_uses`（預設 1），供 HFBR=2。
  - **已知微差異（非驗收情境）**：TANKBR 的還原(PASSIVE) 與自傷(FACTION) 同屬 `ON_REFRESH`，
    因 `AbilityComponentV2._active_for` 在 `run()` 起始即快照有效清單，沉默於同一次 refresh 解除後，
    自傷會延到下一次 refresh 才生效（Python 為同回合即恢復並自傷，差 1 回合）。

**驗收**：`tests/test_card_brown.gd` 26 項全過（含 6 項任務指定驗收：SPBR 攻擊後 ADCBR 不再自麻、
SPBR 死亡後恢復、TANKBR 兩回合 −8、HFBR 一次攻擊耗 2 次數、LFBR 對面 +2 分、ASSBR 跳過抽牌）。
全套件 **484 passed，exit 0，無洩漏**（`--import` 重建 class cache 後執行）。

**問題 / 待決**：無。P1 只剩 P1-13（Purple 4 卡）與 P1-14（核心整合回歸）。行為疑義登記表本次無新增。

---

## 2026-07-10（自動執行 · P1-11 完成）

**分支**：`godot-rebuild`（承接 P1-10，工作區乾淨；基線 433 passed）。

**本次完成任務**
- **P1-11 Fuchsia 全卡 + ShadowEngine**：新增 `script_v2/core/ability/cards/fuchsia.gd`（`FuchsiaCardsV2` 全 8 卡 + `SHADOW` 衍生物）。
  - **鏡像架構決策**：SHADOW 不進 `on_board`/`neutral_pieces`（不可被當敵方目標、不參與計分/回收），
    只存在於 `linker.shadows`。鏡像「代打」＝從鏡像位置 `CombatV2.detection` 判定目標後，
    以本體為 attacker 呼叫 `CombatV2.launch_attack(custom_targets)`，使所有攻擊鉤子
    （ON_ABILITY_HIT/ON_KILLED/ON_AFTER_DAMAGE…）自然落在本體上（對齊 Python
    `Shadow.attack → self.linker.launch_attack(...)`），無需為鏡像另寫一套攻擊管線。
  - **PieceState 擴充**：`shadow_attack_types` 欄位 + `get_shadow_attack_types()`（空則沿用本體 attack_types）；
    `set_linker/get_linker`（WeakRef 反向參考，打斷 `linker.shadows`(強)→shadow→linker 的循環洩漏）；
    `make_shadow()` 靜態工廠（HP1/ATK0/非暈眩/掛 SHADOW 能力）。
  - **各卡**：ADCF/HFF＝ATTACK_OVERRIDE 本體+鏡像都打（本體無目標時僅鏡像命中亦算成功、消耗攻擊次數）；
    APF＝佈署生鏡像+光環麻痺（ON_DEPLOY/ON_REFRESH 使站鏡像格上的敵人麻痺）+攻擊附帶麻痺；
    TANKF＝鏡像實體佔格（ON_UPDATE set_occupied、ON_DIE 若格空則釋放）；
    LFF＝鏡像 attack_types=nearest、本體先打鏡像再打、被兩者都命中且存活的目標補一次本體 ATK；
    ASSF＝ON_KILLED 於受害者位生「不可移動」鏡像（攻擊前快照 temp 清單使本回合新鏡像不代打、越殺越多）；
    APTF＝MOD_FIELD_INTERCEPT(priority 20) 我方站鏡像格受傷時傷害減半(進位)+本體獲減免量(捨去)護盾，
    另 SHADOW 掛 BLOCK_DAMAGE（linker=APTF 時 linker 獲 value//2 護盾，對齊 Python `Shadow.damage_block`）；
    SPF＝ON_DEPLOY 最遠我方紫紅卡(非 SPF)於 SPF 對稱位掛不可移動鏡像。
  - 移動同步：所有本體 ON_AFTER_MOVEMENT 使「可移動」鏡像鏡射到本體新位置的對稱位。
  - `ability_registry.gd` 併入 `FuchsiaCardsV2.registrations()`。

**驗收結果**
- `tests/test_card_fuchsia.gd`（翻譯自 Python `test_card_fuchsia.py`，25 項斷言全過）。
- 全套件：**458 passed，exit 0**，無記憶體洩漏 / orphan / script error。
- 流程：新增 `class_name FuchsiaCardsV2` 後先 `--import` 重建 global class cache，再跑 runner。

**問題 / 給人類的待決事項**
- 無新增行為疑義（Fuchsia 各能力數值皆硬編碼於 Python，不讀額外 param，無需 JSON 同步變更）。
- `Shadow.damage_block` 在實際對戰中不可達（鏡像不進敵方目標池、無法被直接攻擊），
  僅為對稱保留；本專案以 SHADOW 的 BLOCK_DAMAGE 能力保留該行為並供測試直接驗證。
- 下一個任務：**P1-12 Brown 全卡 + BrownEngine**（內建沉默的第一個使用者）。

---

## 2026-07-10（自動執行 · P1-10 完成）

**分支**：`godot-rebuild`（承接 P1-9，工作區乾淨；基線 393 passed）。

**本次完成任務**
- **P1-10 Cyan 全卡 + CoinEngine**：
  - 新增 `script_v2/core/coin_engine.gd`（`CoinEngineV2.gain`，金幣上限 50，對齊 Python get_coins）。
  - 新增 `ability/cards/cyan.gd`（`CyanCardsV2`）全 8 卡；升級與否於效果內以 `ctx.source.upgrade` 執行期分流。
  - **升級系統核心**：
    - `PieceState.make` 增 `upgrade` 參數：升級版改讀 `upgrade_health`/`upgrade_damage`；新增泛用 `counters` 字典（HFC count）。
    - `GameCore._spawn_card` 傳入 upgrade，並在 create→`_cyan_price_check`→deploy→occupy→append
      （對齊 Python factory.spawn_card 順序）。price_check 失敗（金幣不足）→ 生成失敗、不扣費、不佔格、牌留手上。
      折扣 = cost − cost_reduction × 我方場上升級版 SPC 張數。
  - 各卡：ADCC 造傷 +2$／升級 ATTACK_OVERRIDE 二連擊；APC 麻痺 +3$／佈署基礎無視麻痺攻 2 次、升級代打；
    TANKC 被攻擊 +2$／升級怒氣格擋首擊（BLOCK_DAMAGE 整段跳過）；HFC 造傷 +2$／升級被殺進怒氣多活一回合
    (+ATK2、can_be_killed=false、settle count 得 0)；LFC 造傷 +2$／升級每回合隨機換攻擊模式；
    ASSC 斬殺 +6$／升級首擊 extra_damage=2（MOD_DAMAGE_BONUS 用後歸零，加值由管線自動處理）；
    APTC 回合開始 +4$／升級受傷減免 金幣//5（上限 3）；SPC 佈署 +10$／升級折扣走 price_check。

**驗收**
- `... -s tests/run_tests.gd` → **433 passed**，exit 0（P1-9 為 393，本次 +40）。
- 新測試 `tests/test_card_cyan.gd`：翻譯 Python `test_card_cyan.py` 全部卡牌行為（ability 金幣、麻痺、
  TANKC 格擋、HFC 被殺/不死/致命不發 death、ASSC 一次性加傷、APTC 減免與上限、SPC 佈署金幣）
  + 追加核心佈線（升級數值、ADCC 二連擊、APC 基礎佈署攻擊、金幣上限、price_check 足夠/不足/SPC 折扣）。
- `test_ai_cyan_upgrade.py` 屬 AI 決策（`_decide_next` / `_cyan_upgrade_price`），歸 Phase 3；本任務只落實
  卡牌能力與 price_check 核心，AI 側待 P3-4。

**問題 / 待決**：無。行為對齊 Python，無新行為疑義。HFC 的 `count` 以 `PieceState.counters` 忠實實作
（怒氣復活回合得 0 分，下次回收才真正死亡）。

---

## 2026-07-10（自動執行 · P1-9 完成）

**分支**：`godot-rebuild`（承接 P1-8，工作區乾淨）。

**本次完成任務**
- **P1-9 DarkGreen 全卡 + TotemEngine**：
  - 新增 `script_v2/core/totem_engine.gd`（`TotemEngineV2`，純靜態）：`engrave(core, owner, times)` 對
    `players_totem[owner]` 增加 `times × (coefficient ^ 場上 SPDKG 數)`；係數讀 `DarkGreen/SP/engraved_totem_coefficient`=2，
    以整數次方避免浮點誤差。即每有一張 SPDKG 在場，刻印量翻倍（2^n）。
  - 新增 `script_v2/core/ability/cards/dark_green.gd`（`DarkGreenCardsV2`，全 8 卡）：
    - ADCDKG / ASSDKG：`ON_UPDATE` 設 `extra_damage = 圖騰 // divisor`（4 / 8）；damage_bonus 沿用 base pipeline（步驟 4 自動加 extra_damage）。
    - APDKG：`ON_ABILITY_HIT` 麻痺目標 + 刻印 5。
    - TANKDKG：`ON_BEEN_ATTACKED` 刻印 3。
    - HFDKG：`ON_ABILITY_HIT` 造成傷害後自療 1；`ON_UPDATE` HP≤4 時 extra_damage=2；`ON_REFRESH` 自傷 2 + 刻印 2。
    - LFDKG：`ON_DEPLOY` 對 small_cross 敵方（含中立）打 圖騰//4（ability=True，會連帶觸發自身刻印）；
      `ON_REFRESH` 自傷 2；`ON_ABILITY_HIT` 刻印 1。
    - ASSDKG：`ON_KILLED` 自身 HP 歸 0（自殺）+ 刻印 7。
    - APTDKG：`ON_UPDATE` extra_damage=圖騰//2；`MOD_DAMAGE_BONUS` 副作用刻印 armor//2（回傳原值，不重複加 extra_damage）；
      `ON_AFTER_DAMAGE` armor += value//2。
    - SPDKG：無主動能力，僅作為刻印倍率來源。
  - `ability_registry` 併入 DarkGreen。

**修正（Godot 記憶體管理，非平衡差異）**
- 自傷情境（`damage_calculate` 的 attacker == victim，HFDKG/LFDKG on_refresh）會把自己壓入 `hit_cards`，
  形成 `piece.hit_cards → piece` 自我循環強參考。Godot RefCounted 無循環偵測 → exit 時洩漏（Python 靠 GC 不受影響）。
  於自傷後補 `src.hit_cards.clear()` 修正（該情境非攻擊序列，清空語義正確，對齊「攻擊後 clear hit_cards」慣例）。

**驗收**
- `& Godot_v4.7 --headless --path <godot> -s tests/run_tests.gd` → **393 passed**，exit 0，無洩漏（P1-8 為 377，本次 +16）。
- 新測試 `tests/test_card_dark_green.gd`：翻譯 Python `test_card_dark_green.py` 全案例，追加：ADCDKG/APTDKG extra_damage
  經傷害管線生效（APTDKG 驗證不重複加值）、LFDKG 佈署 small_cross 傷害、SPDKG 在場使刻印 ×2。
- 新增 `class_name TotemEngineV2 / DarkGreenCardsV2` 後先跑 `--import` 重建 global class cache。

**問題 / 待決**：無。行為對齊 Python，無新增行為疑義；既有 Python 端回報清單不變。

---

## 2026-07-10（自動執行 · P1-8 完成）

**分支**：`godot-rebuild`（承接 P1-7，工作區乾淨）。

**本次完成任務**
- **P1-8 Orange 全卡**：新增 `script_v2/core/ability/cards/orange.gd`（`OrangeCardsV2`，全 8 卡），主題＝機動。
  - 共用效果：
    - `AttackThenMoveEffect`（ATTACK_OVERRIDE）：照常 `CombatV2.launch_attack`，成功則 `set_moving(true)`，回傳成功與否
      （決定是否消耗攻擊次數）。供 ADCO/HFO/LFO 使用，完整對齊 Python `Adc/Hf/Lf.attack`。
    - `AddMoveoEffect`：給擁有者手牌一張臨時卡 `MOVEO`（出牌即消失、回合結束清）。APO `on_refresh`、TANKO `been_attacked` 共用。
  - 各卡：
    - ADCO：攻擊後獲得移動；`ON_AFTER_MOVEMENT` 再 launch_attack 一次（連鎖攻擊，會 drain 佇列）。
    - APO：`ON_ABILITY_HIT` 麻痺目標；`ON_REFRESH` 發 MOVEO。
    - TANKO：`ON_BEEN_ATTACKED` 發 MOVEO。
    - HFO：攻擊後移動；`ON_AFTER_MOVEMENT` extra_damage +move_damage_gain(1) 且進怒氣；`ON_SETTLE` 清 extra_damage/怒氣。
      **damage_bonus 沿用 base pipeline**（`combat.gd` 步驟 4 已自動 `value + extra_damage`），Python `Hf.damage_bonus` 恰為預設值，故不設 MOD。
    - LFO：攻擊後移動；`ON_AFTER_MOVEMENT` 對最近敵方（**僅對手棋子、不含中立**，對齊 Python `get_opponent_cards`）造成自身 ATK 傷害（ability=True）。
    - ASSO：`ON_AFTER_MOVEMENT` 進怒氣；`ON_KILLED` set_moving + 怒氣時攻擊次數 +attack_gain_per_kill(1) 並清怒氣；`ON_SETTLE` 清怒氣。
    - APTO：`ON_AFTER_MOVEMENT` armor +move_armor_gain(1) 後 armor//2 轉 damage（armor 取餘）；
      `ON_MOVE_BROADCAST` 我方棋子移動（含自己，排除本子自身）給移動者與自身各 +1 護盾。
    - SPO：`ON_MOVE_BROADCAST` 我方棋子移動（含自己）對最遠敵方（含中立，`get_side_cards(owner,True)` 語意）造成 move_strike_damage(3)。
  - `ability_registry` 併入 Orange。

**驗收**
- `& Godot_v4.7 --headless --path <godot> -s tests/run_tests.gd` → **377 passed**，exit 0（P1-7 為 353，本次 +24）。
- 新測試 `tests/test_card_orange.gd`：翻譯 Python `test_card_orange.py` 全案例，追加：ADCO 攻擊獲得移動 + 實際移動觸發連鎖攻擊、
  HFO extra_damage 經傷害管線生效與結算清除、ASSO 怒氣/無怒氣斬殺攻擊次數差異、APTO/SPO move_broadcast 整合。
- 新增 `class_name OrangeCardsV2` 後先跑 `--import` 重建 global class cache 才能被 runner 認得。

**問題 / 待決**：無。行為完全對齊 Python，無新增行為疑義；既有 Python 端回報清單不變。

---

## 2026-07-10（自動執行 · P1-7 完成）

**分支**：`godot-rebuild`（承接 P1-6，工作區乾淨）。

**本次完成任務**
- **P1-7 Green 全卡 + LuckEngine**：
  - 新增 `script_v2/core/ability/cards/green.gd`（`GreenCardsV2`）：8 卡 + LUCKYBLOCK 共 9 註冊。
  - 共用 static `lucky_effects(core, target, ap, ap_target, tank)` 完整翻譯 Python `GreenCard.lucky_effects`：
    - 判定 `not ap_target and rng(1,100) ≤ players_luck[target.owner]` → 好運，否則壞運。
    - 好運：運氣 +1，隨機一項 {護盾+4 / ATK×2 / 排入一次攻擊(經佇列) / moving / 四斜角空格生 LUCKYBLOCK}。
    - 壞運：運氣 -1，隨機一項 {護盾歸 0 / 麻痺 / HP//2 / ATK//2 / HP-2(HP≥2 才扣)}；扣血發 hurt 事件。
    - 旗標：`ap_target` 恆走壞運分支（R §B2 裁定）；`tank` 好運分支立即返回（不加運氣、無效果）；
      `ap` 自身壞運直接返回（不扣運氣）、好運 case5 不生方塊。
  - 各卡：ADCG 攻擊時同行同列空格 50% 生成；APG 麻痺+目標壞運+自身好運；TANKG 被攻擊反擊壞運；
    HFG 打 LUCKYBLOCK → 運氣+5+隨機空格再生；LFG 斬 LUCKYBLOCK → 對最近玩家敵方造成 ATK 傷害(無附帶)+25% 攻擊次數+1；
    ASSG 斬殺 → 自方+5/敵方-5；APTG 禁攻 + 回合開始小十字生成；SPG 佈署 +10 運氣 + 每超 10 點生方塊(排除自身格)。
  - LUCKYBLOCK `been_killed`：對攻擊者發動 lucky_effects + 攻擊者方每張 APTG +1 護盾。
  - **新增 ATTACK_OVERRIDE 佈線**：`AbilityComponentV2.dispatch_attack_override`（回傳 null=照常 / bool=覆寫結果）+
    `CombatV2.attack` 前置攔截。APTG 禁攻回傳 false（不消耗攻擊次數）；為 P1-8 ADCO 攻擊覆寫鋪路。
  - `BalanceDB._build_cards` 增 LUCKYBLOCK 無色別名（沿用 Green/LUCKYBLOCK 數值），`ability_registry` 併入 Green。

**驗收**
- `& Godot_v4.7 --headless --path <godot> -s tests/run_tests.gd` → **353 passed**，exit 0（P1-6 為 333，本次 +20）。
- 新測試 `tests/test_card_green.gd`：翻譯 Python `test_card_green.py`（APG 麻痺 / HFG 對 LUCKYBLOCK 加運與否 /
  ASSG 雙方運氣 / APTG 生成 4 塊 / SPG 佈署加運與生成）+ 追加：固定運氣值(100/0)斷言好運/壞運分支、
  ap_target/tank/ap 三旗標行為、LUCKYBLOCK 被殺回饋(運氣+APTG 盾)、LFG 斬殺追打、APTG 禁攻。
- 新增 `class_name GreenCardsV2` 後先跑 `--import` 重建 global class cache 才能被 runner 認得。

**問題 / 待決**：無。行為完全對齊 Python，無新的行為疑義；既有 Python 端回報清單不變。

---

## 2026-07-10（自動執行 · P1-6 完成）

**分支**：`godot-rebuild`（承接 P1-5，工作區乾淨）。

**本次完成任務**
- **P1-6 Blue 全卡 + TokenEngine**：
  - 新增 `script_v2/core/token_engine.gd`（`TokenEngineV2`，純靜態）：
    - `add(core, owner, n)` 純加 token；`got_token(core, owner)` 一次「獲得 token」事件——
      先對我方所有藍卡跑 `ON_TOKEN_GAINED`（after_token），再檢查門檻；
      **門檻檢查是單次 `if` 非 `while`**（每次 got_token 至多換 1 抽，對齊 Python），
      達門檻則扣門檻、`card_to_draw+1`、`TOKEN_USE` 統計、對藍卡跑 `ON_TOKEN_DRAW`（token_draw）。
    - `gain(core, owner, amount, times)` 便捷：加 amount 並觸發 times 次 got_token
      （多數藍卡 amount==times；ADCB 例外：加 2 只觸發 1 次）。
    - 門檻讀 `Balance.setting("how_many_token_to_draw_a_card")`（config/setting.json=3）。
  - 新增 `script_v2/core/ability/cards/blue.gd`（`BlueCardsV2`），併入 registry：
    - **ADCB**：`ON_KILLED` 加 2 token got_token 一次；`ON_TOKEN_DRAW` 麻痺→解麻痺、
      否則 `CombatV2.enqueue_attack`（追加攻擊經佇列、不遞迴）。
    - **APB**：`ON_ABILITY_HIT` 目標麻痺 + 加 2 token（got_token 2 次）。
    - **TANKB**：`ON_BEEN_ATTACKED` 加 1 token；**LFB**：`ON_ABILITY_HIT` 加 1 token；
      **ASSB**：`ON_KILLED` 加 2 token（三者共用 `GainTokenGainEffect`）。
    - **HFB**：`ON_UPDATE` `extra_damage = 我方 token 數`（damage_bonus 由管線步驟 4 自動加 extra_damage，
      無需自訂 MOD——已核對 Python `Hf.damage_bonus` 僅重述預設 `value+extra_damage`）。
    - **APTB**：`ON_UPDATE` `extra_damage=armor//divisor`；`ON_AFTER_DAMAGE` 獲得等量 token；
      `ON_TOKEN_GAINED` 自身 +1 護盾。
    - **SPB**：`ON_DEPLOY` 對隨機存活敵方重複（我方場上數+棄牌堆數）次各造成 1 傷，最後清空 `pending_attacks`。

**修正（Python 對齊）**
- `game_core._spawn_card` 由「append→occupy→deploy」改為「**deploy→occupy→append**」，對齊 Python
  `factory.spawn_card`：deploy 執行時本子尚未加入 on_board、目標格尚未標記佔用。
  影響 SPB 佈署自我計數（原本會多算自己一次、多打 1 下）。既有 White/Red 無 deploy 能力，回歸不受影響。
  已記入 06 行為疑義登記表。spawn 事件仍先於 deploy 送出（利於表現層先建立棋子）。

**驗收**
- `& "d:\Code\Games\Godot_v4.7-stable_win64.exe" --headless --path <godot> -s tests/run_tests.gd`
- 新增 class_name 後先 `--import` 重建 global class cache，再跑 runner。
- 結果：**333 passed，exit 0**（P1-5 為 309 → 新增 24 項 Blue 斷言全過）。
- 追加驗收（任務指定）通過：token=2 時 APB 攻擊 got_token 兩次 → 只換 1 抽、剩 1 球；
  ADCB 追加攻擊次目標被打兩次；SPB 佈署對敵造成 count×1 傷並清佇列。

**問題 / 待決**
- 無阻斷問題。下一個任務為 **P1-7 Green 全卡 + LuckEngine**（含 B2 裁定、固定 seed 好運/壞運分支測試）。

---

## 2026-07-10（自動執行 · P1-5 完成）

**分支**：`godot-rebuild`。開工前先把工作區既有變更（`data/balance/*.json`、`docs/rebuild/01`，
經 `--ignore-all-space` 確認為純行尾正規化、內容相同）提交為基線 commit，保持後續 diff 乾淨。

**本次完成任務**
- **P1-5 Red 全卡**：新增 `script_v2/core/ability/cards/red.gd`（`RedCardsV2`），組裝紅色 8 卡，
  並在 `ability_registry.gd` 併入 `RedCardsV2.registrations()`。
  - 共用輔助：`_allied_sprs(core, owner)`（我方場上所有 SPR，承接全部紅色增益的鏡射目標）、
    `_nearest_allies(core, src)`（`CombatV2.detection` nearest，排除自己）。
  - **ADCR / LFR / APTR**：造成傷害後滾雪球（`ON_ABILITY_HIT`）——ADCR 自身 +ATK；
    LFR 自身 +護盾 +ATK；APTR 最近友方 + 自己各 +護盾/+ATK；三者皆鏡射 SPR。
  - **APR**：`ON_ABILITY_HIT` 目標麻痺 + 偷取目標 `attack_steal_rate%`（=100）ATK，
    自身與 SPR +偷取值、目標 -偷取值。
  - **TANKR**：`ON_BEEN_ATTACKED` 最近友方（不含自己）+2 護盾、SPR +2 護盾（兩迴圈獨立，
    對齊 Python：最近友方若恰為 SPR 會疊加 +4）。
  - **ASSR**：`ON_KILLED` 最近友方 +2 ATK、SPR +2 ATK。
  - **HFR**：三能力——`ON_ABILITY_HIT`（自損 1HP、ATK+1、SPR+1，HP 歸 0 進怒氣並飄血事件）、
    `CAN_BE_KILLED`（怒氣時保護不死）、`ON_SETTLE`（怒氣得 0 並清怒氣；numbness 由 `settle_piece`
    於效果後清除，等價 Python `on_settle(clear_numbness=True)`）。
  - **SPR**：無主動能力，純承接。
- 增益量一律取「攻擊者自身」的 `card_setting` 參數（`param(card_id, ...)`）鏡射到 SPR，符合 Python。

**驗收**
- `tests/test_card_red.gd`（翻譯自 Python `test_card_red.py` 全案例，另加任務指定的
  HFR 怒氣不死身 / 0 分結算 / 治療救回 0HP 怒氣者）：全數通過。
- 全套件 `run_tests.gd`：**309 passed**、exit 0，無 script error / 洩漏。
- 新增 `class_name RedCardsV2` 後，headless 先跑一次 `--import` 重建 global class cache，runner 才認得。

**問題 / 待決**：無。行為與 Python 一致，未觸發停止條件；行為疑義登記表無新增。

---

## 2026-07-10（自動執行 · P1-4 完成）

**分支**：`godot-rebuild`。

**本次完成任務**
- **P1-4 White 全卡 + 特殊卡**：
  - `script_v2/core/ability/cards/white.gd` 由骨架填入 White 三張有能力卡：
    - **APW** 攻擊附帶麻痺：`ON_ABILITY_HIT` → `target.set_numb(true)`（Python `Ap.ability`）。
    - **APTW** 攻擊時最近友方（不含自己）與自身各 +ATK 護盾：`ON_ABILITY_HIT`，用
      `CombatV2.detection(core, apt, "nearest", allies)` 撈最近友方（Python `Apt.ability`）。
    - **SPW** 計分：`ON_SETTLE` 修改子，非 numbness → `value + extra_score`（BalanceDB `param("SPW","extra_score")=1`
      → 2 分），numbness → 維持 0（Python `Sp.on_settle`）。
  - ADCW/TANKW/HFW/LFW/ASSW 為純數值（無 native 能力）；ASSW 先攻由 `PieceState.make` 依 ASS 職業處理。
  - 特殊卡 CUBE/CUBES/HEAL/MOVE/MOVEO 行為於 P1-2 已在 `game_core` 完成，無 native 能力；
    「CUBE 恆 0 分」結構上由「neutral 不參與 `_turn_end` settle」保證，無需特殊 on_settle。

**驗收**
- `test_card_white.gd`（翻譯自 Python `tests/test_card_white.py`）8 項全過：APW 麻痺、APTW 自身/友方護盾、
  SPW 非麻痺得 2 分、SPW 麻痺得 0 分並解除。
- 全套件：**274 passed，exit 0**（P1-3 為 266，本次 +8）。

**問題 / 待決**：無。行為與 Python 一致，無新增行為疑義。

---

## 2026-07-10（自動執行 · P1-3 完成）

**分支**：`godot-rebuild`。

**本次完成任務**
- **P1-3 傷害管線與攻擊佇列 + 能力系統骨架**：
  - `script_v2/core/combat.gd`（`CombatV2`，純靜態）：
    - `detection`（R §5）：small_cross / large_cross / small_x / large_x(空) / nearest / farthest，
      並列以 `rng.choice` 隨機挑 1；目標池含 neutral。
    - `damage_calculate`（R §6）：十步管線，護盾三分支（恰抵銷 / 溢出 / 無盾）逐字翻譯 base.py，
      含兩分支 kill 與 after_damage 的先後差異；統計 DAMAGE_DEALT/TAKEN/TAKEN_COUNT/KILLED/DEATH。
    - `launch_attack` / `_launch_attack_impl`（R §7）：`_attack_draining` 旗標 + `pending_attacks` drain，
      追加攻擊排佇列不遞迴；動畫延遲游標 `_attack_anim_cursor`（R §10，多目標 0.32s 間隔、命中 0.55 比例）。
  - 能力系統 v2（`script_v2/core/ability/`，見 04 §5）：
    - `TriggerV2`（觸發點全表）、`AbilityV2` / `AbilityEffectV2` / `AbilityContextV2`。
    - `AbilityComponentV2`：native/granted/silenced，**silence(id)/silence_tag/grant/clear** API +
      `run`（副作用）/`dispatch_mod`（MOD 串接）/`any_true`（布林）/`collect_field`（場地攔截）分派。
      `source` 用 `WeakRef` 避免與 `PieceState.abilities` 的強引用循環洩漏。
    - `AbilityRegistryV2` + `ability/cards/white.gd`：註冊 White 8 張（能力清單骨架，P1-4 填）。
  - `game_core` 接上全鉤子：ON_DEPLOY / ON_REFRESH / ON_SETTLE（settle 改走能力鏈）/ ON_UPDATE /
    ON_DIE / CAN_BE_KILLED（保護語義）/ after_movement / move_broadcast / custom_move；
    `_attack` 實作（成功才扣 attack_uses、記 HIT）；`drain_events` 重置攻擊游標。

**驗收結果**
- 指令：`& "d:\Code\Games\Godot_v4.7-stable_win64.exe" --headless --path <godot> -s tests/run_tests.gd`
- 新增 `tests/test_combat_pipeline.gd`（護盾三情境統計、killed/been_killed 順序、death 事件 delay、
  numbness 攻擊無效不耗次數、追加攻擊佇列不遞迴）與 `tests/test_ability_system.gd`
  （silence by id / by tag、grant/clear_granted、MOD 串接）。
- 結果：**266 passed，exit 0，無 ObjectDB 洩漏**（較 P1-2 的 233 增 33 項）。

**問題 / 給人類的待決事項**
- 無阻斷問題。能力系統目前為骨架：各色實際卡牌能力由 P1-4 起逐色填入 `ability/cards/`。
- 提醒：新增 `class_name` 檔後，headless 驗收前須先跑一次 `--import` 重建 global class cache
  （`.godot/` 為 gitignore 快取，換機器須重跑）。

---

## 2026-07-10（自動執行 · P1-2 完成）

**分支**：`godot-rebuild`。

**本次完成任務**
- **P1-2 行動分派與移動**：`game_core.gd` 新增對外唯一輸入口 `dispatch(GameAction)`（翻譯自
  `battling_dispatcher._execute`）與各行動處理：
  - `_play_card`：HEAL/MOVE/MOVEO/CUBES 魔法計數分支 + 一般棋子 `_spawn_card`（格子有效且空 → 佔格 →
    附 SPAWN 事件；Cyan `(+)` 前綴解析為 upgrade 旗標，價格檢查留 P1-10）。
  - `_heal_card` / `_heal_piece`：+6 HP、溢出 `//2` 轉護盾（見 base.heal）。
  - `_move_card` / `_move_piece`：兩段式移動（啟用扣點 → `selected` 選取 → 目的地），
    8 鄰（切比雪夫 1）限制、目的地須空、失敗不退點（B5）、附 MOVE 事件。
  - `_spawn_cube`：生成 neutral CUBE(4HP/0atk)、扣次數、記 CUBE_USE。
  - `_toggle_upgrade`：Cyan 卡加/去 `" (+)"` 後綴。
  - `attack` 僅保留 dispatch 前置守衛（次數不足 / 非當前玩家被拒）；實際傷害管線與扣次數留 P1-3。
- 佐證改動：`PieceState` 增 `movable` 欄位；`BalanceDB._build_cards` 增 `CUBE` 無色別名
  （沿用 `White/CUBE` 數值，job_of/color_code_of 回空字串，由 `PieceState.make` 以 card_id 當 job）。

**驗收結果**
- `tests/test_core_actions.gd`（新增）涵蓋：非當前玩家被拒、攻擊次數不足被拒、CUBES 給 2 顆 +
  spawn_cube 佔格/失敗、HEAL +6 與溢出轉盾、移動成功/非相鄰失敗/被佔失敗/點數消耗與不退、
  MOVEO 出牌即離手且未出者回合結束消失、toggle_upgrade 後綴切換。
- 全套件 **233 passed，exit 0**（Godot 4.7 stable headless）。

**問題 / 待決**：無。attack 的實際執行、move 後的 after_movement/move_broadcast 廣播鉤子、
deploy 的 ON_DEPLOY 掛勾均為 P1-3 傷害管線與能力骨架範疇，已於程式留註記。

---

## 2026-07-10（自動執行 · P1-1 完成）

**分支**：`godot-rebuild`。

**本次完成任務**
- **P1-1 基礎狀態與回合引擎**：於 `script_v2/core/` 新增純邏輯核心（零 Node 依賴）：
  - `game_config.gd`（J §2.3 硬編碼常數鏡像，附 Python 出處）、`board_state.gd`（4x4 occupy）、
    `piece_state.gd`（棋子純資料 + statuses 字典 + 狀態 API + `make()` 工廠，入場暈眩含 ASS 例外）、
    `player_state.gd`（deck/hand/牌庫/棄牌堆/revealed_deck + `draw_card` 洗牌重抽）、
    `rng_service.gd`（注入種子 + Fisher-Yates shuffle）、`game_event.gd`（class `GameEventV2`，
    避開舊碼 `GameEvent` 衝突）、`statistics.gd`（16 種 StatType + score_history）、
    `game_action.gd` / `action_result.gd`、`turn_engine.gd`（initialize/turn_start/end_turn 權威順序）、
    `game_core.gd`（狀態容器 + setup/logic_step/settle/回收/勝負判定）。
  - settle 採「非 numbness=1 分」基本版（能力 ON_SETTLE 掛勾留待 P1-3）。
  - BalanceDB 小幅擴充：`job_of()` / `color_code_of()`（供 `PieceState.make` 反查職業/色碼）。

**驗收結果**
- `test_core_turns.gd` 六項全過：開局手牌 3/3、攻擊 1/0；end_turn 輪替 + turn_start 抽牌/攻擊；
  numbness 首次結算 0 分並解除、之後得分；分數 ±10 雙向觸發勝負；skip_turn_draw 生效；牌庫空洗棄牌堆重抽。
- 完整測試套件：**195 passed，exit 0**（Godot 4.7 stable，headless）。

**處理重點 / 待決事項**
- **class cache 相依**：新增 `class_name` 腳本後，headless `-s` runner 不會自動掃描註冊全域類別，
  必須先跑一次 `--headless --import` 重建 `.godot/global_script_class_cache.cfg`，否則報
  「Identifier not declared」。`.godot/` 屬 gitignore 快取，換機器/清 build 後須重跑 import。
  → 建議：後續若把驗收包成腳本，於 runner 前加一步 `--import`。目前工作樹快取已更新，測試綠。
- `game_core.gd` 全程無 `get_tree` / `load("res://scenes`；僅在 setup 以 autoload `Balance` 作為
  未注入 BalanceDB 時的預設（資料層，符合 D1）。P1-14 的 grep 檢查不受影響。
- 無規格衝突、無行為疑義；「行為疑義登記表」本次無新增。

---

## 2026-07-10（自動執行 · Phase 0 完成）

**分支**：`godot-rebuild`（自 main 建立）。先將既有未提交變更（docs/rebuild 文件、abilities 原型等）提交為 baseline。

**本次完成任務**
- **P0-1 v2 目錄骨架與測試 runner**：建 `script_v2/`、`scenes_v2/`、`data/balance/`、`tools/` 骨架；`tests/run_tests.gd`（SceneTree，內嵌 TestContext 斷言工具）＋ `test_smoke.gd`。
- **P0-2 sync_balance.ps1**：單向複製 Python `config/*.json` → `data/balance/`，產出 `_meta.json`（version 4.3.0.0、hash 6263139bb55e）；`--check` 模式可用。
- **P0-3 BalanceDB autoload**：`script_v2/data/balance_db.gd`，註冊 autoload `Balance`；查詢 API stats/param/attack_types/color_rgb/ai/setting/data_version；card_id 以「職業碼+色碼」資料驅動組合避免解析歧義。
- **P0-4 card_text.json**：`tools/gen_card_text.gd` 產生 82 卡顯示文字；BalanceDB 增 `text(card_id)`。

**驗收結果**
- 完整測試套件：**169 passed，exit 0**（Godot 4.7 stable，headless）。
- 測試 runner 反向驗證：故意寫錯時 exit 1。
- sync_balance `--check`：一致時 exit 0。

**處理重點 / 決策記錄**
- PowerShell 5.1 無法正確解析 UTF-8（無 BOM）含中文註解的 .ps1 → `sync_balance.ps1` 改存為 **UTF-8 with BOM**。
- `data/balance/*.json` 入版控時 git 會把 CRLF 正規化為 LF；不影響：`--check` 是對 Python 來源計算 hash，Godot JSON 解析亦與行尾無關。
- BalanceDB 以資料驅動組合 card_id，導致 White CUBE → `CUBEW`、Green LUCKYBLOCK → `LUCKYBLOCKG` 這類複合 id。P0-4 已為其補上名稱（方塊 / 幸運箱）與對應 hint。後續 Phase 1 若需純 `CUBE` / `LUCKYBLOCK` id，屆時再於卡牌註冊層處理。
- `setting/description.json` 為非嚴格 JSON（含尾逗號），產生器以 RegEx 清理後解析；該檔屬舊碼，Phase 6 會刪除，屆時 `gen_card_text.gd` 亦一併退場。

**給人類的待決事項**：無。Phase 0 全數通過，可續接 Phase 1（P1-1 基礎狀態與回合引擎，屬大型純邏輯任務）。
